# Okanagan Roam — AI Handoff

This file is the shared coordination point for AI assistants working on this repository.

## Rules

1. GitHub is the source of truth. Always inspect the current repository state before acting.
2. Do not overwrite another assistant's work. Review recent commits and existing changes first.
3. Prefer small, auditable changes with clear commit messages.
4. Never commit secrets, tokens, passwords, API keys, or private credentials.
5. For production/data changes, verify before and after and record the result here.
6. If an action is blocked, document the exact blocker rather than using an unsafe workaround.

## Current Status

* Repository: `okanaganroam/okanaganroam`
* Default branch: `main`
* Shared AI handoff file established: September 8, 2026
* Recent work includes venue enrichment, guarded corrections, duplicate/redirect infrastructure, and enrichment audit tables.
* Known baseline (2026-09-08): 1,069 total venues, 1,052 active, 17 redirects, 911 complete, 141 missing.
* Updated baseline (2026-09-15, verified live against production `/api/venues`): 1,055 active venues, 989 complete (address+lat+lng all present), 66 missing (all three fields missing together in every case — no partial records). Total-including-redirects and redirect count were not reverified this pass (no admin access this session); only the active/complete/missing figures above were independently confirmed.
* Updated baseline (2026-09-22, verified read-only on the live `/data/okanagan.db` and `/api/venues` after the Nature/Viewpoints/Camping write): 1,404 venue rows, 1,365 active, `sqlite_sequence.venues` = 1406; 195 active `type='outdoor'` destinations; activity memberships 328 (hiking 57, winter 51, nature 51, camping 50, viewpoints 43, cycling 33, adventure 18, water 13, fishing 12); `/outdoors` landing lists 204 (outdoor-type rows plus beach-type rows holding a live activity membership).
* **What's On baseline (2026-09-22, verified via public reads after the final approved write):** 638 events (ids 1–638, contiguous), 1,109 scheduled occurrences, 638 event URLs in the sitemap (2,244 total). Batches: canary `whatson-2026-10-canary` (ids 1–10), v1 `whatson-2026-10-v1` (11–92), v2 `whatson-2026-10-v2` (93–626), v3 `whatson-2026-10-v3` (627–638), plus two occurrence-level edits (`whatson-2026-10-v2-edit-1` on #387, `whatson-2026-10-craft-culture-edit-1` on #548). Runtime: Node 24.21.0 / ICU 78.3 / tzdata 2026c on Railway (deployment `772b4bd0`, commit `6568501`) — America/Vancouver is −07:00 for every date from 2026-03-08 onward.

## Authentication / Admin API Notes

* `ENRICHMENT_ADMIN_TOKEN` is read from the Railway environment and is not hardcoded in source.
* Admin mutation routes use `Authorization: Bearer <token>`.
* Missing configuration returns 503; invalid/missing bearer token returns 401.
* Token comparison uses a timing-safe comparison.
* Admin correction/enrichment routes are narrowly scoped and validate request fields before database writes.
* Never record or commit the actual admin token here.

## Latest AI Handoff

### Claude

* Continue recording meaningful production/data work in commits with descriptive messages.
* Before changing production-sensitive code or data, reconcile against the current GitHub state.
* Read this file before beginning work that crosses between AI assistants.
* Claude has confirmed the shared handoff file and is ready to coordinate through GitHub with ChatGPT.
* 2026-09-08 — Claude executed the ready 11-venue HIGH-confidence location enrichment batch (IDs: 469, 841, 857, 863, 907, 918, 967, 1000, 1002, 1004, 1041). All 11 were freshly re-verified (not assumed from prior research) via exact phone match to a single unambiguous Google listing before any write. All 11 writes succeeded with zero collateral field changes and zero anomalies. Complete-location count rose from 900 to 911; missing-location count fell from 152 to 141. Active count (1,052), redirect count (17), and all 17 existing redirect mappings were confirmed unchanged. Sitemap remained at 1,240 (expected — pure enrichment of already-active venues does not add/remove sitemap pages). The manifest file was intentionally left byte-for-byte unchanged, since manifest synchronization is a separate, not-yet-authorized task.
* 2026-09-15 — Claude re-checked current missing-location count directly against live production (`GET https://okanaganroam.com/api/venues?limit=2000`, no admin access needed since address/latitude/longitude are public fields). Active venue count is now 1,055 (up from 1,052 on 2026-09-08 — some venues were likely added between sessions). Of those, 989 have complete address+lat+lng and 66 are missing all three (down from 141 missing on 2026-09-08, indicating substantial enrichment progress happened since that note was last updated, though it wasn't logged here at the time). Redirect count and grand total (including redirects) were not reverified this pass. Note: an initial check via a summarizing web-fetch tool undercounted this (reported only 2 missing) because it truncated/summarized the ~1,055-row JSON response before counting — the 66 figure above comes from downloading the full response and counting programmatically, and should be treated as the reliable one.
* 2026-09-18 — Claude completed a full re-audit of all 38 active venues still missing address/latitude/longitude (independently re-derived from live production, not assumed from the stale 66-count above). Successfully enriched 5 of the original set via the guarded `/admin/enrich-venue` endpoint (IDs 95, 154, 535, 536, 724 — individually phone- or place-verified, all writes confirmed with zero collateral changes). Of the 38 still missing: 19 are confirmed MOBILE/ROAMING (no fixed public-facing premises; excluded from enrichment) and 19 are UNRESOLVED under current free-tool methodology (a mix of confirmed-fixed-address-but-no-verifiable-coordinate, genuine multi-branch/identity ambiguity, and insufficient evidence). Current WRITE-READY count is 0 — no further safe writes remain under this methodology. Full per-ID classifications and reasoning are recorded in this session's conversation history (not duplicated here); see the Open Tasks section below for the current, accurate summary replacing the stale 66-count guidance. ID 158 was initially misclassified WRITE-READY (bistro address found via business-level research) before a targeted identity check caught that this specific record represents the food truck, not the separate bistro entity — corrected to MOBILE/ROAMING. ID 337 was similarly initially misclassified before a targeted check surfaced unresolved multi-entity/multi-domain ambiguity (a same-named national wholesale brand vs. the local farmers-market-stall business this record's own description matches) — left UNRESOLVED rather than guessed. Active count (1,042), complete-location count (1,004), phone-populated count (681), and sitemap remained unchanged throughout (pure read-only research except the 5 confirmed writes).
* 2026-09-19 — Claude made the approved homepage footer (navy background, gold uppercase headings/dividers, two Regions subcolumns, gold Trip button) the single canonical footer across every normal public page type: `/`, `/browse`, `/trip`, venue pages, region pages, category pages, guide pages, event pages, and the events index. Previously `/trip` carried the correct footer markup but only two stale, hand-copied CSS patch rules (so it rendered unstyled), and every venue/region/category/guide/event/events-index page rendered a completely different, minimal one-line `siteFooter()` (`Okanagan Roam · okanaganroam.com`) instead of the real footer at all. Implementation: extracted all footer + floating Trip-button CSS into one new shared function, `renderCanonicalFooterStyles()`, now called by the homepage's, `/trip`'s, and the SEO pages' stylesheets alike, so a future footer change only has to happen in one place; replaced the six `siteFooter()` call sites (`renderGuidePage`, `renderRegionPage`, `renderCategoryPage`, `renderVenuePage`, `renderEventPage`, `renderEventsIndexPage`) with the same `renderHomeFooterHTML(true)` the homepage and `/browse` already use. `renderGuideFooterHTML()` (the small "Browse Okanagan Roam by guide" SEO cross-link block, hidden on `/` and visible on `/browse`) was deliberately left untouched and separate — it is not the visual site footer and was out of scope. Visual testing surfaced one real bug beyond the original plan: venue/region/category/guide/event pages set `body{max-width:900px}` for their own content, which was also boxing the footer into a centered navy rectangle instead of a true edge-to-edge band like the homepage's; fixed with the standard `width:100vw; margin-left/right: calc(50% - 50vw)` full-bleed technique on `.home-footer`, confirmed as a no-op on `/` and `/browse` (byte-identical rendered homepage output before/after). Desktop verification was done via real screenshots (isolated local instance, real project data) confirming the footer/Trip button on the homepage, `/trip`, and a venue page. Mobile verification was code-level only, not a fresh live screenshot — the browser tool's viewport resize did not take effect in this environment (window resized but the content viewport stayed at desktop width); confirmed instead that the mobile `@media` rules are byte-for-byte identical to the version already visually approved in the prior footer commit (`5082610`), and that the one new rule added is viewport-width-independent by construction — a live mobile check is still recommended before this is treated as fully closed. Tests: 214/215 passing, the sole failure being the same pre-existing `EADDRINUSE` on the one HTTP-integration test that needs port 3001 while the required-to-stay-running dev server occupies it (unrelated to this change, present before it too). Committed as `73a4906a2fcd772ee2d17ff00b532bdb89767970` ("refactor: use canonical footer across public pages"), pushed to `main`, and auto-deployed by Railway (deployment `a24c977e-0ce9-4409-9785-275bc063def7`, status SUCCESS). Production re-verified after deploy: homepage still 200 with its footer unchanged, and the canonical footer confirmed present (correct markup + full CSS, zero `site-footer` occurrences) on `/trip`, a real venue page, a region page, and the events index.
* 2026-09-19 — Claude completed the Golf category content update: all **49 active Golf venues** (39 outdoor courses + 10 indoor/simulator venues) had their `description` field researched, rewritten to the new Okanagan Roam "sell the experience" standard, human-approved, and written to production. **Process:** live inventory pulled read-only from `GET /api/venues?type=golf` (exactly 49, matching expectation); four parallel research passes against official venue sites, Golf Canada, regional tourism organizations and local news, followed by a targeted second verification pass on 16 venues whose driving-range / putting-green / chipping-area status was unknown or conflicting, and a final editorial pass. Rule applied throughout: practice facilities were only stated when confirmed by an official-tier source (official site, Golf Canada, official tourism org); unknown/conflicting items were simply omitted rather than asserted (e.g. no facilities claimed for Orchard Greens, Pine Hills, St. Andrews by the Lake, Sumac Ridge, Kelowna Springs; The Rise's directory-sourced "driving range" was dropped after reviews indicated warm-up nets only). Confirmed absences were kept as useful information (Shannon Lake: hitting cage + putting green, no range; Tower Ranch: no on-site range, partners with the off-site World Beat Family Golf range; Sunset Ranch: hitting nets). **Notable factual corrections surfaced by research:** Kelowna Springs has operated as a 9-hole course (former front nine) since the 2025 season with the City of Kelowna acquiring the remaining holes via land exchange — older 18-hole directory data is stale; Orchard Greens' restaurant is now Miss Pat's Kitchen (official contact page), not the Bunkhouse Bar & Grill; Kelowna G&CC is "established 1920" per its official site (not 1899). **Write mechanism:** the existing bearer-token-protected `PUT /api/venues/:id` (`updateVenue()`) with a body containing only `description`; token read from the Railway production environment via the linked CLI into a chmod-600 scratch file that was never printed and was deleted after the run. Per-record guards: live record re-fetched and byte-compared to a before-snapshot immediately before each write, `type='golf'` and `redirect_to IS NULL` asserted, post-write field-by-field comparison; a no-op authenticated `PUT {}` (200, unchanged) and an unauthenticated PUT (401) were run first, then a single canary (ID 1089), then the remaining 48. **Reconciliation (live production, before vs. after):** active venues 1,091 → 1,091 with an identical ID set; 49/49 descriptions changed and 49/49 byte-equal to the approved manifest; 0 other fields changed on the 49 (only the automatic `updated_at` moved, on exactly 49); 0 non-Golf venues changed (all 1,042 byte-identical); no golf-type venue exists outside the 49; all 49 still active; no duplicate `(region, type, slug)`. **Verification:** `/golf` 200 with the Golf Courses / Indoor Golf & Simulators split at exactly 39 / 10 and all 49 names present; all 11 regional Golf pages 200; all 49 individual venue pages 200 with new copy rendering; homepage fetched before/after — only Cloudflare's per-request email-protection hash differs, no content change. Tests (run from a temp copy of the repo so the harness's delete-and-reseed of `okanagan.db` could not touch the file the running dev server has open): 221/222 passing, the sole failure being the known pre-existing `EADDRINUSE :::3001` on the HTTP-routes integration test. Local `okanagan.db` untouched; no code/UI/homepage/asset/route/schema/test changes; nothing deployed (data-only change via the live API). **Important code constraint discovered (not changed):** `isIndoorGolfVenue()` in `server.js` classifies a golf venue as indoor purely by regex `/simulator|indoor golf/i` on the free-text description, and this drives the Courses vs. Indoor split on `/golf` and `/<region>/golf`. Three outdoor drafts (Spallumcheen Championship, Spallumcheen Executive, Vernon G&CC) originally mentioned a "Trackman simulator" and would have been misfiled; the word was removed. Kelowna G&CC (winter full-swing simulator) and The Rise (Dante's Golf Lounge) also have simulators that must stay out of their descriptions while this logic stands — a structured field or explicit ID list is recommended as a separate architecture decision. **Open items:** Simplex Sportszone bay count — the owner's own TripAdvisor management reply says "7 Studios, 2 with Trackman 4 technology and 5 multi sports and golf" (reconciling the DB's "7" with a secondary "4 Trackman studios" figure as a subset count), but the official site refused connections throughout, so the live description deliberately states no bay count. Three facility claims remain review/directory-corroborated rather than official (Sunset Ranch nets, Tower Ranch nets, Wow Golf's mat range). The approved 49-description manifest and full before/after snapshots are in that session's scratchpad only, not the repo.
* 2026-09-19 — Claude implemented **Beaches Phase 2** (taxonomy, routes, data model, rendering, tests) in `server.js`, `db.js`, `tests/server.test.js`; **no production or local DB changes, no descriptions written, nothing deployed.** **Inventory (Phase 1, owner-approved after a two-pass reconciliation against municipal / regional-district / BC Parks sources):** 63 INCLUDE, 3 HOLD (Canoe Beach — RDNO confirms existence and dog rule only; Christie Memorial Park — absent from the BC Parks API and RDOS GIS, rdos.bc.ca unreachable; Cottonwood Park, Osoyoos — washroom hours only), 20 EXCLUDE with research preserved (incl. Beechnut Park and Casa Loma Dock Park — small neighbourhood accesses with accessibility features; Sutherland Bay Park — City lists it as a boat launch with a "beach area" but no swimming area; boat launches, street-end accesses, out-of-region North Westside parks, Enderby river parks). Region decisions: Traders Cove RP, Raymer Bay RP and Bear Creek PP → `west-kelowna`; Okanagan Lake PP → `summerland`; Kickininee PP → `penticton`; Mabel Lake PP → `lumby`; Vaseux Lake PP → `okanagan-falls`. The approved manifest (`beaches_inventory_manifest_approved.json`, v4) and final table live in the session scratchpad, not the repo. **Taxonomy:** `type='beach'`, slug `beaches`, labels Beach/Beaches, `SCHEMA_TYPE_MAP` → `Beach`, accent gradient = tokens `--ref-navy`/`--ref-navy-deep`, hidden-gem image `/images/mood/beaches.webp`, in `ALL_REGIONS_CATEGORIES`. **Routes:** `/beaches`, `/:region/beaches`, `/:region/beaches/:slug` (existing generic dispatch; sitemap and region-hub cards follow automatically). **Rendering:** Beaches reuse the deployed Golf design system by admitting `THEMED_CATEGORY_TYPES {golf, beach}` at each existing Golf gate — homepage header + `app.css`, name-as-link cards with the "View details →" cue, Favorite + Add to Trip on cards, the site-wide `#tripTray`, and the venue CTA row (Visit Website / Get Directions / Call only when the field exists — nothing fabricated; beaches carry no phone). Golf markup, class names and CSS text are unchanged (`body.golf-page` kept; beach pages are `golf-page beach-page`); beach card CSS is derived mechanically from the Golf rules at render time. The only byte-level change on non-beach SEO pages is three added rules in the shared inline stylesheet (`.venue-hero-beach`, `.related-card-beach`, `.compact-band-beach`), owner-approved. **Temporary conditions:** new `advisories` collection (kind `advisory`, bootstrapped in `db.js` like Local Favourites, **no schema change**, no members seeded); membership via the audited `POST /admin/collection-membership` with the official wording in `note`; renders a "Check before you go" notice on the card and venue page and is excluded from trip `discovery` kinds; removal restores the page byte-for-byte. Intended first uses: Rotary Beach Park (Kelowna) swimming advisory and Kalamoir RP partial wildfire closure, both verified 2026-09-19. **Trip planner:** `TRIP_PLANNER_EXCLUDED_TYPES {beach}` — not an interest (400), no `/trip` chip, never an itinerary candidate; beach/swimming vocabulary stays in the parser's unsupported list; Favorite / Add to Trip / tray still work (name-keyed localStorage). **Homepage freeze:** `/` is byte-identical before/after beach rows + an advisory exist (verified against a pre-change baseline on the local inspect instance and enforced by a new isolated-child-process test); no tile, count, mood-card, footer, markup, CSS, JS or image changes; homepage files untouched. `/browse` shows beaches under "All" exactly as it shows Golf (accepted). **Tests:** 253 total, 252 pass; the one failure is the pre-existing HTTP-routes test (binds port 3001 → `EADDRINUSE` while the local inspect server runs; on a free port it fails on the same pre-existing Osoyoos "Trip Golf Course" region-selector assertion as HEAD) — deliberately not fixed in this change. **Local inspection only:** a scratchpad copy of the DB serving `localhost:3001` holds 63 beach rows with clearly labelled placeholder descriptions and the two advisories; the repository's `okanagan.db` was verified unchanged by hash. **Next, each a separate approval:** Phase 3 content (63 descriptions in a reviewable `beaches_content_manifest_v1.json` with `fact_keys_used` validation, Golf-style), then the production data write via `POST /api/venues` (explicit slugs, all amenity booleans 0, addresses/coordinates only where officially stated) plus the two advisory memberships, then homepage integration.
* 2026-09-21 — Claude completed the **Winter category audit and data write**. **Research (read-only, bounded single requests — the earlier stalled batch-fetch approach was not repeated):** an Okanagan-wide sweep of ski resorts (all four existing records 1233/1240/1234/1235 audited for 2026/27 pricing: Big White and Apex publish 2026/27 lift rates, Baldy still shows 2025/26, SilverStar's store is JS/403-only), Nordic centres, municipal/community skating and drop-in hockey in every major community, snowmobile clubs and tour operators, ice fishing, dog sledding, ice climbing (no commercial operator exists; Big White's ice tower is no longer offered), tubing, snowshoe/fat-bike trails and recurring winter events. Coordinates were taken only from operator/club-published figures or named OpenStreetMap feature matches; street-level geocodes were discarded. Owner reviewed all candidates and approved 19 HIGH new venues (C-8 Kal Tire Place, C-14 Summerland Arena and C-22 Caravan Farm Theatre held; 10 MEDIUM candidates held: Oliver & District Arena, Similkameen Rec Centre rink, Princeton Arena, Hunters Range trailhead, RDCO Joe Rich/Scotty Creek rinks, District Wine Village rink, Penticton Lakeside Resort rink, Echo Lake PP ice fishing, Big Bear Tours dog sledding). **Write (batch `winter-2026-09-v1`, via the bearer-guarded `POST /api/venues` and `POST /admin/collection-membership`; the token was injected into the executor's process environment by `railway run` and never written to disk or printed):** guard checks first (unauthenticated POSTs → 401; authenticated request with a disallowed key → 400, no write), then a canary, then the rest in manifest order. **Verified results:** 19 new `type='outdoor'` Winter venues, IDs **1318–1336** (1318 Rutland Arena, 1319 Kelowna Memorial Arena, 1320 MNP Place, 1321 Stuart Park Outdoor Rink, 1322 Mount Boucherie Arenas – Jim Lind Arena & Royal LePage Place, 1323 Winfield Arena, 1324 Vernon Recreation Complex – Priest Valley Arena & Centennial Outdoor Rink, 1325 Nor-Val Sports Centre, 1326 Enderby Arena, 1327 Pat Duke Memorial Arena, 1328 McLaren Park Arena, 1329 Penticton Outdoor Skating Rink (Gyro Park), 1330 Sun Bowl Arena, 1331 Peachland Outdoor Ice Rink (Mountain View Park), 1332 Jeffers Park Skating Rink, 1333 Graystokes Snowmobile Area – Kelowna Snowmobile Club, 1334 Vernon Snowmobile Association – Silver Star Trails [region `silverstar`], 1335 Okanagan Rec Rentals – Snowmobile Tours, 1336 Rodney's REEL Outdoors – Ice Fishing Charters); 8 of the 19 deliberately carry null coordinates (1320, 1322, 1325, 1329, 1331, 1334, 1335, 1336) because no precise public pin was verified — do not backfill them with guesses. 22 `activity_winter` memberships (the 19 new venues plus existing cross-lists 1280 Predator Ridge Trails, 1278 Crawford Trails – Myra-Bellevue PP, 1220 Johns Family Nature Conservancy RP, each with an approved `note`); 1 Graystokes (1333) → `activity_adventure`; 1 Rodney's REEL (1336) → `activity_fishing` (deliberately **not** `activity_water`). Winter members 12 → 34; Adventure 17 → 18; Fishing 11 → 12 (the public `/outdoors/fishing` page renders 10 because two members, 1158 and 1192, are beach-type and the activity pages only render `type='outdoor'` — pre-existing behaviour); outdoor venues 106 → 125; total active venues (`/api/venues`) 1,276 → 1,295; sitemap 1,515 → 1,534 (+19). All 1,276 pre-existing venue rows byte-identical before/after (the three cross-listed venues changed only in collection membership); `PRAGMA integrity_check` = ok on the live `/data/okanagan.db` (read-only via `railway ssh`, node:sqlite; `sqlite_sequence.venues` = 1336; exactly 24 `venue_enrichment_log` rows for the batch); live API reconciliation against the before-snapshot passed with no differences beyond the 19 additions; all 19 venue detail pages, all 12 affected `/{region}/outdoors` pages, `/outdoors`, `/outdoors/winter|adventure|fishing` verified 200; homepage byte-identical (ignoring Cloudflare's per-request email-protection hash), `/golf` (39/10 split, 49 links), `/beaches` (79 links), `/browse`, `/trip`, `/events` verified unaffected; no duplicate names or addresses among the new records (two pairs share a municipal skating page as `website`, expected); no 5xx or application errors in the container log. **No code changes, no migrations, no existing-venue field edits, no deployment** — the Winter data was written directly to production through the live API and verified there; the repository's `okanagan.db` is unchanged. Note the render-gate constraint discovered during the audit: `getOutdoorActivityVenues()` joins only `type='outdoor'` rows, so beach/golf-type venues added to an activity collection (e.g. Bear Creek PP, Kalamalka Lake PP, Ellison PP for winter) will not appear on `/outdoors/<activity>` without a code change — a separate decision. Research files (`FINAL_REPORT.md`, `winter_write_manifest_v2.json`, `write_log.json`, before/after snapshots) live in that session's scratchpad, not the repo.

### ChatGPT

* Review the latest commits and repository state before proposing or making changes.
* Use this file to leave concise coordination notes when a task crosses between assistants.
* Do not overwrite Claude's work; inspect the current GitHub state first.
* Current GitHub integration status: ChatGPT can inspect the repository but its write attempts currently return HTTP 403 (`Resource not accessible by integration`).
* 2026-09-21 — Claude completed the **Nature & Wildlife audit and data write** (batch `nature-2026-09-v1`, same guarded procedure as Winter: `railway run` token injection, 401/400 guard checks, canary, manifest order, field-by-field verification). Owner-approved **11 new `type='outdoor'` venues, IDs 1337–1347** (1337 Kangaroo Creek Farm [kelowna — region by physical location], 1338 Allan Brooks Nature Centre, 1339 Nk'Mip Desert Cultural Centre, 1340 Vaseux Lake Wildlife Centre & Migratory Bird Sanctuary, 1341 Kingfisher Interpretive Centre, 1342 Munson Pond Park, 1343 Maude-Roxby Wetland (Bird Sanctuary), 1344 Rotary Marsh Park, 1345 Robert Lake Regional Park, 1346 BX Creek Wetland Boardwalk & Vernon Heronry, 1347 Chichester Wetland Park), plus 3 existing cross-lists into `activity_nature` (1280 Predator Ridge Trails, 1256 Middleton Mountain, 1269 International Hike and Bike Trail — BC Bird Trail evidence). Nature members 30 → 44; a new `/okanagan-falls/outdoors` hub appeared (+1 sitemap URL beyond the 11 venue URLs). 15 MEDIUM candidates held (Adams Bird Sanctuary, Coldstream Creek kokanee, Cools Pond, Penticton Creek kokanee, Peach Cliff, farms/agritourism, mobile tour operators, EECO as a standalone); Bishop Wild Bird Sanctuary, Summerland Trout Hatchery (visitor centre closing 2026), Commonage Road (private) excluded. Pre-existing rows byte-identical; integrity ok; homepage byte-identical. Research in that session's scratchpad (`nature-wildlife/`).
* 2026-09-21 — Claude completed the **Viewpoints & Lookouts audit and data write** (batch `viewpoints-2026-09-v1`). **5 new venues, IDs 1348–1352**: 1348 Anarchist Mountain Lookout (osoyoos; OSM viewpoint node; eastbound-only hairpin pullout — the MOTI rest area 3.7 km up is described inside it, not a second record), 1349 Spotted Lake (kłlilx'w) Viewpoint (osoyoos; roadside viewing only — sacred Syilx site on Osoyoos Indian Band land, no access beyond the fence; conservative wording approved), 1350 Kalamalka Lake Lookout (Highway 97 Rest Area) (region **coldstream** by physical location; MOTI rest-area layer pin), 1351 Dilworth Mountain Park (kelowna), 1352 Rocky Ridge Park (Turtle Mountain) (vernon; **null coordinates** by decision — street-level geocode only; parking cost recorded as UNKNOWN); plus cross-list 1230 Grey Canal Trail → `activity_viewpoints`. Viewpoints 33 → 39. Held: Mount Conkle cross-list (later approved 2026-09-22), Peachland Lookout, Oyama Lookout, Peach Cliff, McIntyre Bluff, Trepanier Creek Dam viewpoint, Kalamalka Lake PP lookout trails (beach-type at the time). Research in `viewpoints-lookouts/`.
* 2026-09-21 — Claude completed the **Camping audit and data write** (batch `camping-2026-09-v1`). **31 new venues, IDs 1353–1383**: BC Parks campgrounds 1353 sx̌ʷəx̌ʷnitkʷ (Okanagan Falls) PP Campground and 1354 Inkaneep PP Campground; municipal 1355 Peach Orchard Campground; Recreation Sites and Trails BC sites 1356 Evely, 1357 Noisy Creek, 1358 Cottonwood Beach, 1359 Hidden Lake, 1360 McCulloch Reservoir, 1361 Hydraulic Lake (same water body as 1360, separately managed sites ~1.5 km apart — keep both), 1362 Island Lake (Dee Lake chain), 1363 Swalwell (Beaver) Lake, 1364 Sugar Lake 2 Mile, 1365 Idleback Lake, 1366 Lambly (Bear) Lake, 1367 Cooke Creek (Shuswap River) — RSTBC site points converted from BC Albers (EPSG:3005) via the Sites & Trails API; private/commercial 1368 Hiawatha RV Park, 1369 Wright's Beach Camp, 1370 South Beach Gardens RV Park, 1371 Barefoot Beach Resort, 1372 Nk'Mip Campground & RV Resort, 1373 Cabana Beach Campground & RV Park, 1374 Riverside RV Park & Campground, 1375 Naramata Centre Campground, 1376 Wood Lake RV Park & Marina, 1377 Peachland RV Park, 1378 Cedar Falls Campground, 1379 Lake Front RV Park (Swan Lake) (mapped in OSM under its former name Silver Star Campground & RV Park), 1380 Okanagan Glamping Co., 1381 Gold Panner Campground, 1382 Mabel Lake Resort & Marina, 1383 Island View RV Resort; plus 6 existing lodge/resort cross-lists into `activity_camping` (1309 Postill Lake Lodge, 1310 Beaver Lake Mountain Resort, 1311 Dee Lake Wilderness Resort, 1312 Chute Lake Lodge, 1313 Oyama Lake Lodge, 1314 Headwaters Family Camp). Camping 2 → 39; `/outdoors/camping` went live (crossed `MIN_ACTIVITY_VENUES`, +1 sitemap URL). Beach- and golf-type records (the eight beach-type provincial park campgrounds and 1102 Twin Lakes Golf Course & RV Park) were deliberately **not** touched in this write. Held: Swan Lake RV Park, Dutch's, Echo Lake Resort, Illahie Beach, Oxbow, Brookvale, Walton's, Oasis, Lakeside Resort Oliver, Upper BX Creek, Conkle Lake PP (outside region slugs), Darke/Eneas Lakes PPs (rustic), small RSTBC sites beside existing lodges. Excluded: Holiday Park (residential), Ley's, Gallagher Lake, Loose Bay (farm-worker housing), closed/OHV rec sites, Bear Creek Recreation Site (wildfire damage). Research in `camping/`.
* 2026-09-21 → 2026-09-22 — Claude audited the Camping/activity renderer and, on approval, shipped **commit `e508a16` "Outdoors: let activity pages list beach-type members (type allowlist)"** (Railway deployment `94f408c3`, SUCCESS). `getOutdoorActivityVenues()` and `getOutdoorActivityCounts()` in `server.js` now join `v.type IN (OUTDOOR_ACTIVITY_VENUE_TYPES)` where `OUTDOOR_ACTIVITY_VENUE_TYPES = ['outdoor', 'beach']` (exported; **golf deliberately excluded** — whether 1102 Twin Lakes belongs in Camping is an open editorial decision, and 1102 has no activity memberships). Membership is the source of truth for *which* destinations belong to an activity; the allowlist only says which record types are eligible. Effect on deploy: hiking 53 → 57, nature 42 → 44, fishing 10 → 12 (the previously invisible beach-type members Ellison, Fintry, Kekuli Bay, Kalamalka Lake PP – Cosens Bay, Mabel Lake, Vaseux Lake, sw̓iw̓s); camping stayed 39; sitemap URL set unchanged; homepage, `/golf`, `/beaches`, `/trip` byte-identical. Cards, hrefs and JSON-LD already key off each venue's own type, so a beach row renders as a beach (`/{region}/beaches/{slug}`). New regression test in `tests/server.test.js` (beach fixture with a membership is listed and counted; winery/golf memberships are ignored). Known pre-existing failing test 221 ("HTTP routes… selector must NOT list a region with zero golf venues") reproduces on untouched HEAD and was not fixed; the runner also does not exit after the last test (open handles) — kill it after the summary.
* 2026-09-22 — Claude shipped the **Outdoors explorer landing** — **commit `d556721`** (Railway `b8d4ea03`, SUCCESS) — and the three missing activity-card images — **commit `698a350`** (Railway `39aca62a`, SUCCESS). `/outdoors` is now Choose Region(s) → Choose Activity(s) → Results within the existing design system: the nine data-model activities render as one 3 × 3 image-card grid (2 across on phones) and each live card is a multi-select **filter toggle** (`aria-pressed`, teal ring/"Selected" badge) driven by the same inline script and `?regions=a,b&activities=x,y` URL state (pushState/popstate) as the region pills, with a small "Guide →" link to the existing `/outdoors/<slug>` page; the "View All Outdoor Activities" reveal and the Boating/Climbing placeholder tiles are gone. Regions OR, activities OR, AND between groups (unchanged predicate, server pre-render + client filtering). Results show "N of T outdoor destinations", removable Regions / Activities tag rows and small "Clear all" controls; landing result cards clamp to ~3 lines (activity/region pages keep 4). **Landing universe = activity universe:** new `getOutdoorLandingVenues()` (outdoor-type rows plus allowlisted-type rows holding a live activity membership) is used only by the `/outdoors` route, so card counts, filtered results and activity pages agree (total 172 → 179 at the time, the seven beach-type provincial parks); `getVenuesByCategory()` and `/:region/outdoors` untouched. Beach card CSS loads on the landing only when a beach card is present; the shared card-engagement script (`themedCardHolderSelector`) selects every allowlisted card type on outdoor surfaces (clamp/Read more, impressions, favourite/trip aria mirroring) — Golf/Beach scripts byte-identical. Card art: `public/images/outdoors/{nature,viewpoints,camping}.webp` (1376×768 lossy WebP, Pillow q80, from Gemini-generated JPEGs; the card renderer resolves art by card key so no code change). Tests: assertions updated, one explorer test added (10 behaviours). Deferred by decision: hiding the per-card "Guide →" link below 600px (one-line CSS), `activity_adventure` cross-lists for 1394/1395.
* 2026-09-22 — Claude completed the **Winter resort activity audit and data write** for SilverStar, Big White, Apex and Baldy (batch `winter-resorts-2026-09-v2`; Manifest v2 approved with structural decisions: standalone venues only for experiences with their own identity/location/ticketing/operator; no generic "Adventure Park" records; night skiing, terrain parks, guided tours, Polar Passage, Baldy cat skiing/tubing lane/disc golf kept as enrichment of the resort records; only operator-labelled **2026/27** prices appear in public text — 25/26 and unlabelled prices omitted). **17 new `type='outdoor'` Winter venues, IDs 1384–1400**: SilverStar 1384 Tube Town – SilverStar Tube Park (incl. Mini Snowmobiles; OSM "Tube Town Carpet" pin), 1385 Brewer's Pond Skating – SilverStar (incl. Bear Byway loop), 1386 SilverStar Cross-Country Ski Trails (55 km resort side; moderate overlap with 1236 Sovereign Lake noted in both texts), 1387 SilverStar Snowshoe Trails, 1388 SilverStar Fat Bike Trails (+`activity_cycling`), 1389 Ravenwood Snowshoe Dinner Tours – SilverStar (resort tour page body was unpublished at write time — description kept general; re-check when 26/27 details post); Big White 1390 Big White Tube Park (Happy Valley), 1391 Happy Valley Ice Skating Rink – Big White, 1392 Big White Nordic & Wilderness Trails (+`activity_cycling`; 2026/27 guided snowshoe tour prices included), 1393 Twinkling Trail – Big White, 1394 Candle Creek Kennels – Dog Sled Tours at Big White (separate business; 2026/27 $399/sled), 1395 Outback Snowmobile Tours – Big White (separate East Kelowna business; prices unlabelled → omitted), 1396 Big White Horse-Drawn Sleigh Rides & Dining Tours (Dec 14 2026–Mar 30 2027 prices included; operator not named by the resort); Apex 1397 Adventure Skating Loop & Shinny Pond – Apex and 1398 Tim Hortons Tube Park – Apex (2026/27 ticket-page prices included); Baldy 1399 Baldy Snowshoe Trails ("about 13 km per the resort" — sources disagree 5/10/13) and 1400 McKinney Nordic Ski Club – Snowshoe & Cross-Country Trails (region **baldy**: RSTBC REC31792 and OSM place it in Kootenay Boundary RD Area E, the resort's electoral area, 6.5 km before the resort on the Baldy road; wording states ~5 km maintained snowshoe trails and ~9 km XC trails **ungroomed since the groomer retired after 2023/24** — never "20 km groomed"). Coordinates only from named OSM features or the RSTBC site point (1384, 1385, 1389, 1400); null elsewhere. **5 enrichments via authenticated `PUT /api/venues/:id`** (description only; 483 also `website`): 1240 SilverStar (outdated 69 km/551 m/70 km stats replaced with 3,304 acres/133 runs/760 m/55 km Nordic, My1Pass, night skiing, Mountain Hosts, season dates; summer passage retained), 1233 Big White (night-skiing scale, Polar Passage 26/27, free tours, season dates), 1234 Apex (Fri/Sat night skiing & tubing, four terrain parks, Skate Shack), 1235 Baldy (cat skiing, tubing lane, winter disc golf, night ops, 13 km snowshoe), 483 Paradise Camp (same place as the Snow Cat Dinner Tours — backside lodge, OSM pin matches; **not** duplicated; type `cafe`, so it cannot render on `/outdoors/winter`; its `description_fr` still carries the old soup-shop text). Winter 34 → **51** (silverstar 3 → 9, big-white 1 → 8, apex 2 → 4, baldy 1 → 3); cycling 31 → 33; adventure unchanged (optional cross-lists for 1394/1395 not applied — owner's call); rows 1381 → 1398. Flags left open: **1335** Okanagan Rec Rentals – Snowmobile Tours is region `vernon` (HQ address) although the tours run only at SilverStar — do not move it until the public meeting point is confirmed; the 1394/1395 adventure cross-lists; 1389 re-check. Excluded as historical/unverified: SilverStar sleigh rides, Big White ice tower, McKinney "20 km groomed XC", Snowflake Loop, Baldy snowmobiling, hockey-game excursions, After Dark, spas/yoga. Research in `winter-resorts/`.
* 2026-09-22 — Claude completed a **Nature & Wildlife / Viewpoints & Lookouts / Camping enrichment write** (batch `nvc-2026-09-v1`), built on the beach-type allowlist. **6 new `type='outdoor'` venues, IDs 1401–1406**: 1401 Cools Pond Park (vernon; RDNO park, BC Bird Trail stop; null coordinates by decision), 1402 Summerland Ornamental Gardens (summerland; volunteer-run botanical/xeriscape garden, admission by donation with $5 suggested — never a fee), 1403 White Lake Grasslands Protected Area (okanagan-falls; BC Parks conservation area — text states undeveloped, unmarked trails, map-and-compass skills, limited pullout parking, White Lake Trail access only, and that the McIntyre Bluff / Covert Farms access has been closed since 2020 — do not promote it), 1404 Echo Lake Resort (lumby; 17 sites, tent vs powered RV ≤30 ft, May 1–Thanksgiving), 1405 Dutch's Campground (coldstream; tents and RVs, open all year, winter camping by application; no site counts/hookups published), 1406 Oxbow RV Resort (penticton; **RV-only, no tent sites**, short-stay; the operator's 2023 rates were not used). **16 memberships on existing records** (all confirmed absent before): `activity_camping` → 1134 Bear Creek PP, 1208 Fintry PP, 1140 Ellison PP, 1150 Kekuli Bay PP, 1165 Okanagan Lake PP, 1195 sw̓iw̓s PP, 1158 Mabel Lake PP, 1192 Vaseux Lake PP (all from the BC Parks API: 143 / 190+3 group / 71 / 76 / 175+8 walk-in / 41 / 114+group / 12 FCFS sites); `activity_nature` → 1134, 1208, 1146 Kalamalka Lake PP – Cosens Bay (only one of the park's two beach records was cross-listed, on purpose), 1353; `activity_viewpoints` → 1140, 1146, 1134, 1284 Mount Conkle Park. **8 append-only description enrichments** (existing text preserved verbatim, approved sentences appended, `website` untouched): 1134, 1208, 1140, 1150, 1165, 1158, 1192, 1146. Held by decision, research kept in the scratchpad (`nvc/`): Coldstream Park – Kokanee Viewing and Adams Bird Sanctuary (MEDIUM); Bluenose Mountain, Peachland Lookout, Oyama Lookout, Peach Cliff, Trepanier Dam viewpoint; the unreachable/placeholder private campgrounds and Conkle/Darke/Eneas. Nature 44 → **51**, Viewpoints 39 → **43**, Camping 39 → **50**; memberships 306 → 328; rows 1398 → **1404** (active 1,365, `sqlite_sequence` 1406); outdoor-type 189 → 195; `/outdoors` landing 196 → 204 (six new rows plus 1134 and 1165 newly holding a live membership); sitemap 1,600 → 1,606; homepage, `/golf`, `/beaches` byte-identical; exactly the 8 enriched pre-existing rows changed (description + updated_at only); integrity ok; 22 `venue_enrichment_log` rows for the batch. **No code changes and no deployment** for the Winter-resort or NVC data writes; the repository's `okanagan.db` is unchanged (the test suite writes fixtures into that tracked file — restore it with `git checkout -- okanagan.db` after running tests).
* 2026-09-22 — Claude built and shipped **What's On (events) v1**, Steps 1–9f, under the frozen design in the session scratchpad (`whatson-research/WHATSON_TECHNICAL_DESIGN.md`; not in the repo). **Code (commit `dd6a044`, Railway `1fb4c359`; then `6568501` Node 24 runtime, Railway `772b4bd0`):** `db.js` — additive `events` columns (`status`, `event_confidence`, `source_type`, `source_name`, `source_url`, `source_checked_at`, `venue_name_text`, `valley_wide`), new tables `event_occurrences` (materialised local dates; unique on `(event_id, start_date, COALESCE(start_time,''))`), `event_categories` (1–3 per event, position 0 primary), `event_enrichment_log`. `server.js` — America/Vancouver local-date helpers (`todayLocal`, `dateWindowForPreset`, `toVancouverIso`, expiry from `substr(end_datetime,1,10) < todayLocal`; no hard-coded offsets), data layer (`validateEvent*`, `findDuplicateEventCandidates` §12 rules `exact_slug` / `same_source_and_date` / `fuzzy_same_day`, `createEvent`, `updateEvent`, `replaceEventCategories`, `upsertEventOccurrences` [idempotent on date+time; never edits an existing row], `setEventOccurrenceStatus` [`last_occurrence` refusal], `recomputeEventSpan`), bearer-guarded write API (`POST /api/events`, `PUT /api/events/:id`, `PUT …/categories`, `POST …/occurrences`, `PATCH …/occurrences/:occId` [status only]; 503 when the token is unset, 401 on bad token, 400 `Unexpected field(s)`), public reads (`GET /api/events?when=today|this-weekend|this-week|this-month|from=&to=` ≤366 days, `&regions=`, `&categories=`; `GET /api/events/:id` public shape — full record only with a valid bearer; `GET …/occurrences` scheduled only), event detail page `/:region/events/:slug` (scheduled-only route; cancelled/postponed → 404; expired → 200 + noindex; JSON-LD SportsEvent/MusicEvent/Festival/Event with subEvents; `Listed by <source>` line for non-official sources), `/whats-on` shell date step (preset chips + custom range, all window cards rendered, region/category filters, `valley_wide` events match every region), sitemap event URLs (scheduled with ≥1 scheduled occurrence and not expired). Tests: 347 (346 pass; the one failure is the pre-existing golf-selector assertion). **Data (production writes via `railway run` token injection only; token never printed):** canary 10 events / 79 occurrences (ids 1–10), v1 82 / 193 (11–92; UBCO Heat rebuilt from the published goheat.ca schedule pages after the `.ics` was found to encode a PST fall-back — 32 of 52 times were an hour late), v2 534 / 760 (93–626; the 623-row VERIFY audit resolved to READY 534 / HOLD 39 / EXCLUDE 54 after six owner-reviewed duplicate dispositions), v3 12 / 75 (627–638; final 33-row HOLD audit: READY 12, OWNER DECISION 5, EVIDENCE NEEDED 6, EXCLUDE 10). Edits: #387 Ella's Naughty Drag Brunch Oct 11 sitting moved to 11:30–13:30 (new occurrence 1033 labelled "Honey's Birthday Brunch with Ella & Honey"; old 10:30 occurrence 646 cancelled, audit kept); #548 Craft Culture Holiday Market gained Dec 5 and Dec 6 10:00–17:00 (occurrences 1109, 1110) from the organiser page. Sources of record: HockeyTech `modulekit` schedule feeds (the public client-side keys are read from each league site's own statview scripts rather than recorded here; WHL season 295 — Rockets team 204, Vees 277; BCHL season 72 — Warriors 19, Vipers 21; BC Hockey Conference [formerly KIJHL] client `bchc` season 2 — Coyotes 14; all rows `timezone: America/Vancouver` with `-07:00` offsets through March 2027; neutral-site Nisku AB showcase games excluded), goheat.ca per-team schedule pages, Tourism Kelowna per-event pages (Akamai-gated — Chrome only; sitemap.xml lists event URLs; page script vars `dates`/`time`/`recurrence`), Destination Osoyoos (Vercel checkpoint — Chrome; schema.org Event block per occurrence), Tourism Vernon (`<time datetime>` pairs + address block), Visit Penticton (`nm_event` pages with per-date rows), Visit Westside (WordPress; "Date List" pages are not machine-parseable), Visit Oliver (Tribe REST `wp-json/tribe/events/v1/events`), and the official organiser pages named per event. Every write was rehearsed first with the app's own `createEvent` in a scratch DB (copies of `server.js`/`db.js` in the scratchpad, seeded with the live venue snapshot and the production-equivalent event set) and reconciled afterwards through public reads only. Research/manifest/executor artifacts live in the session scratchpad (`whatson-research/import/`: `import_manifest.json`, `review.csv`, `VERIFY_AUDIT_REPORT.md`, `HOLD_AUDIT_REPORT.md`, `step9/*_write_log.json`) — nothing there is in the repo.
* 2026-09-22 — Claude shipped the **What's On category tile art** — **commit `1ba5e38`** (Railway `cb24656f`, SUCCESS). Twelve 1376×768 lossy WebP files under `public/images/whats-on/` (`events-festivals`, `live-music`, `sports-recreation`, `arts-culture`, `food-drink-events`, `markets-fairs`, `family-kids`, `nightlife`, `wineries-wine-events`, `holiday-seasonal`, `workshops-classes`, `community-events`; 182–343 KB each), encoded with Pillow (quality 90, method 6) from the owner's 1376×768 source JPEGs — `sips` on this Mac can read but not write WebP, and no cwebp/sharp is installed. The existing renderer (`whatsOnCategoryImagePath`, `server.js`) picked them up with no code, layout, styling, category-order or homepage change; the only code diff is the What's On shell test, whose pre-art assertions now expect one `/images/whats-on/<key>.webp` per category key in the approved order and no `whatson-category-card-noart` tile. Suite 347 → 346 pass (the pre-existing golf-selector failure remains). Smoke-checked on an isolated preview copy (scratch DB with the 638 production-equivalent events): 12 images load at natural size in order, selected/unselected states, region, category and date filtering unchanged, no horizontal overflow at 1600 px or 390 px; verified live after deploy (all 12 assets 200 `image/webp`, `/whats-on` shows the 12 tiles, 0 no-art tiles, 375 cards in the default window, homepage untouched). `public/images/whats-on/README.md` still describes the spec and can stay as the format reference.
* 2026-09-22 — Claude ran a **read-only SEO audit** (technical, page inventory, search intent, event/venue SEO, content opportunities) and then inspected **Google Search Console**. There is no GSC connector, CLI or credential in this environment; access came from the owner's own signed-in Chrome session, read-only (no indexing requests, no settings touched). The `sc-domain:okanaganroam.com` property is not accessible; the **`https://okanaganroam.com/` URL-prefix property is**. Measurements (Aug 30 – Sep 19, 2026, Web search): **35 clicks, 6.95K impressions, 0.5% CTR, average position 11.8**; mobile 18/4,950/0.4%/8.4, desktop 15/1,910/0.8%/20.9. Indexing (through 9/18): **414 indexed, 822 "Discovered – currently not indexed"** (666 venue, 75 region/category, 62 guide, 19 region URLs; no event or hub URLs). Sitemap `/sitemap.xml`: submitted Aug 31, **last read Sep 21, Success, 1,515 discovered pages** — i.e. before the 639 event URLs existed. Breadcrumbs 31 valid, review snippets 2, Core Web Vitals "No data". Queries are almost entirely **branded venue-name lookups** in Kelowna and Vernon ("room 272 kelowna" 99 impressions/0 clicks, "kin and folk kelowna" 63/0, "elora restaurant & bar vernon menu" 81/1); generic category intent ranks far back ("kelowna restaurants" pos 44, "kelowna wine" pos 73). **799 queries / 3,902 impressions sit in positions 4–20 and return 15 clicks**, and **55 pages have ≥25 impressions with zero clicks**. By page type only **venue pages** (363 of them, 31 clicks / 6,481 impressions), the homepage (4/175, pos 52.8), 2 region, 11 region/category and 18 guide pages have ever appeared; **event, What's On, Outdoors, Golf, Beaches and activity pages had never appeared** (events were published the same day; the hubs were missing from the sitemap). Full audit and the prioritised roadmap live in the session transcript, not the repo.
* 2026-09-22 — Claude shipped **SEO Phase 1 — sitemap discovery** — **commit `4776d5e`** "SEO: add core discovery hubs to sitemap" (Railway **`daab6e06-a552-47ec-a0c0-7ccb5abfd4c7`, SUCCESS / Online**). `/whats-on`, `/outdoors`, `/golf` and `/beaches` were live, self-canonical, indexable and linked from the header/footer, but none was listed in `sitemap.xml` — and `/whats-on` is the only internal parent of all 639 event detail URLs. Adds exactly those four `<url>` entries after the homepage entry (`/whats-on` daily 0.9; the other three weekly 0.8). **`/trip` deliberately excluded**; the retired standalone `/events` stays excluded (it 301s to `/whats-on`). Sitemap **2,244 → 2,248 URLs**; event detail URLs remain **639**; venue 1,365 / region/category 130 / guide 80 / region 20 / activity 9 / homepage 1 all unchanged; zero duplicates. Proven by generating the sitemap from old and new code against the same database: 4 URLs added, none removed, every pre-existing URL unchanged and in the same order, and the homepage plus each affected page byte-identical. Tests: the sitemap test now asserts each hub appears exactly once and `/trip` does not; two frozen-homepage baseline assertions that used the broad `/beaches/` and `/outdoors/` regexes to mean "no beach/outdoor venue URLs yet" were scoped to region-prefixed URLs. **Homepage unchanged.**
* 2026-09-22 — Claude shipped **SEO H1 Step 1 — event internal linking** — **commit `fc6f57c`** "SEO: add related event internal links" (Railway **`91135192-784d-4d17-9dfe-1e7c4fc64520`, SUCCESS / Online**), **`server.js` only, no database, schema or event-data change**. Before this, event pages were dead ends: all 639 linked to their region page, none linked to another event. Adds a compact **"More events in {Region}"** section plus a "See what's on in {Region}" link to the filtered view. `listRelatedEventsInRegion()` (exported) applies the sitemap's publication rules — `status='scheduled'`, at least one scheduled occurrence, not expired on the Okanagan local date — restricted to the same region, never the event itself, ordered by next scheduled date; variety takes one event per primary category first, then fills remaining slots by date; the section is omitted entirely when the region has no other publishable event, so it self-manages expiry and new arrivals. Verified on production by crawling all 639 pages: **638 of 639 carry the block** (the exception is `/baldy/events/larix-trail-race-2026` — Baldy has a single event, correctly no block), **maximum 4 links** (4×629, 3×4, 2×3, 1×2, 0×1), **~2,500 new contextual internal links**, **0 self-links**, **0 cross-region links**, **0 links to expired/cancelled/postponed events** (fixture-proven: cancelled, postponed, occurrence-cancelled and expired events are never linked, while an event ending today still is), links use the direct canonical `/{region}/events/{slug}` URLs, and 625 of 636 multi-link blocks show a distinct primary category per link (the 11 repeats are resort regions whose inventory is genuinely single-category). Event **canonical, robots and JSON-LD unchanged** (2 blocks per page, self-canonical, 0 noindex); **homepage unchanged** (0-line diff) and `/whats-on`, `/outdoors`, `/golf`, `/beaches`, the sitemap and every other page type byte-identical old-vs-new on the same database. Production after the deploy: **639 events / 1,133 scheduled occurrences / 2,248 sitemap URLs**.
* 2026-09-22 — Claude shipped **SEO H1 Step 2 — region-page event blocks** — **commit `50eaada`** "SEO: add upcoming events to region pages" (Railway **`da8263d2-fb6d-4734-bdca-c32f8d681274`, SUCCESS / Online**), **`server.js` + `tests/server.test.js` only; no database, schema, event-data, styling or route change.** Region hub pages were the thinnest indexable page type (117–205 words) and linked to **zero** events. Adds an **"Upcoming events in {Region}"** block between the category `card-grid` and the guide links, listing up to **3** events as direct canonical `/{region}/events/{slug}` links with a `{next date} · {primary category}` meta line, plus one secondary **"See what's on in {Region} →"** link to `/whats-on?regions={region}`. New exported helper `listUpcomingEventsForRegion(region, {limit, now})` applies the **same publication predicate as the sitemap and `listRelatedEventsInRegion()`** — `status='scheduled'`, at least one scheduled occurrence, not expired on the Okanagan local date — restricted to `events.region` (so a valley-wide event belonging to another region does not count), ordered by next scheduled occurrence then name. A `MIN_REGION_EVENTS = 2` threshold returns `[]` below two publishable events, so sparse regions render **nothing at all** and the block self-manages as events expire or arrive. Reuses the `.related-section` / `.related-grid` / `.related-card` / `.related-meta` CSS the page already loads — **no new CSS, classes, routes or indexable URLs**. **Live result:** blocks on **15 of 20 regions** — 3 links each on kelowna, vernon, penticton, west-kelowna, oliver, lake-country, osoyoos, big-white, naramata, armstrong, okanagan-falls, silverstar, summerland and peachland; **2 on coldstream** (its entire publishable inventory); **no block on apex, baldy, enderby, kaleden and lumby**, whose rendered pages are **byte-identical** to the pre-deploy version. Totals: **44 event links + 15 secondary links = 59 new internal links**, all unique, all HTTP 200, all self-canonical (44/44), **0 off-region links, 0 query-string links, 0 links to an expired, cancelled or postponed event** (cross-checked against the live publishable set of 639). Note the pre-implementation audit had sized this at 16 qualifying regions including baldy — baldy's second "event" was the **valley-wide** Okanagan Fall Wine Festival, which the region-scoped predicate correctly excludes, leaving it with one event and no block; coldstream qualified instead. **Verification:** old-vs-new render on an identical 639-event database left `/`, `/whats-on`, `/outdoors`, `/outdoors/hiking`, `/golf`, `/beaches`, `/trip`, `/sitemap.xml`, `/robots.txt`, category, venue and event detail pages **byte-identical**, with **zero removed lines on all 20 region pages** (purely additive). The `${eventLinks}` insertion is written conditionally so non-qualifying regions do not even gain a whitespace line. Tests: **348 total, 347 pass**, the one failure being the known pre-existing golf-selector assertion in "HTTP routes: region, category, venue, guide, and 404 all respond correctly"; the new test covers 3-link/2-link/1-link/0-link thresholds, next-date ordering, cancelled/postponed/expired exclusion, canonical URLs, the secondary link, and page integrity, and cleans its fixtures up in FK-safe order (log → categories → occurrences → events). Post-deploy production: **639 events / 1,133 scheduled occurrences / 2,248 sitemap URLs** (sitemap diffed against a pre-deploy copy: **0 differing lines**), **homepage byte-identical**. One caveat on method: the live site's byte signature rotates per request because Cloudflare re-encodes obfuscated email addresses, so live baselines must normalise `email-protection#…` **and** `data-cfemail="…"` before hashing. **Deliberately not fixed here:** the mid-span date display (3 events — #93 Skillful, #632 Joining Hands, #638 Penticton Beer Week — show a past start date in both this block and the live event-page block); it is a separate, unapproved change.
* 2026-09-22 — Claude ran the **H1 Step 3A venue_id enrichment** — **data only, batch `whatson-2026-09-venue-link-1`; no commit, no code change, no deployment**. A read-only audit of the 616 events carrying only `venue_name_text` found **16 exact venue-name matches** (verified against each live venue page's `<h1>`, with JSON-LD `location.name` equal to `venue_name_text` on all candidates); **13 were SAFE** and **44 events** were owner-approved. Three venues went to HOLD and stay text-only: **Vows & Vines Vineyard** (#232, #274 — events filed under `penticton`, venue address is Summerland) and **Evolve Sparkling House** (#227 — event `summerland`, venue on Naramata Rd) would both be rejected by the write gate's `venue_region_mismatch`; **Vernon Golf & Country Club** resolves to two active venue pages in one region. Two event-level HOLDs: **#299** (`venue_name_text` "Hester Creek Estate Winery" but the title says "at Terrafina", which has its own venue page) and **#231 OROLO** — not a casing issue: the title names **Chronos Winery**, a different existing venue, so it is an event-data defect worth fixing separately. Region-suffix stripping (21 names / 75 events) and partial matching (45 / 115) were tested and **rejected**; warning cases: *Grizzli Winery, West Kelowna* → a `kelowna` venue, and *Socialē on Lakeshore* → a Penticton restaurant while the event is `okanagan-falls`. **Writing `venue_id` alone is impossible**: the frozen model enforces `venue_id` XOR `venue_name_text` (`validateEventVenue`), and `updateEvent()` re-validates the merged row — a rehearsal of the venue_id-only form failed 44/44 with `venue_ambiguous`. The owner therefore approved the two-field form `{venue_id, venue_name_text: null}`, which is lossless here because all 44 text values were byte-identical to the venue row's name. Written through `PUT /api/events/:id` via `railway run` (token never logged): **44/44 returned 200**, each changing exactly `venue_id + venue_name_text`. Reconciled by re-crawling all 639 event pages: **23 → 67 events with a real `venue_id`**, **616 → 572 text-only**, occurrences under a real venue **46 → 99**, venue pages with >=1 upcoming event **6 → 18**, >=2 **2 → 7**, >=3 **2 → 4**. A 639-row fingerprint (`id|slug|region|start|end|time|categories|occurrences|name`) was **identical before and after** — no other field moved anywhere in the corpus; totals held at 639 / 1,133 / 1,365; sitemap unchanged (0-line diff); the five HOLD/OROLO events verified still text-only. Note the audit's "47 occurrences under a real venue" baseline was an arithmetic slip — the true figure is 46, so 46 + 53 = 99. Event pages for the 44 now render the venue as a link, existing renderer behaviour driven by data.
* 2026-09-22 — Claude shipped **SEO H1 Step 3 — venue-page event blocks** — **commit `e5cb16f`** "SEO: add upcoming events to venue pages" (Railway **`ce5c12ff-bd0e-4185-915b-98309f241930`, SUCCESS / Online**), **`server.js` + `tests/server.test.js` only; no database, data, schema, route, sitemap or CSS change**. Venue detail pages linked to zero events. Adds an **"Events at {Venue}"** block of up to **3** events, each a direct canonical `/{region}/events/{slug}` link with a `{next date} &middot; {primary category}` meta line, placed before the broader "Other {category} in {Region}" and "More to explore" sections and reusing the `.related-section`/`.related-grid`/`.related-card` styles the page already loads. New exported helper `listUpcomingEventsAtVenue(venueId, {limit, now})` applies the **same publication predicate as the sitemap and the event/region blocks** — `status='scheduled'`, at least one scheduled occurrence, not expired on the Okanagan local date — ordered by next scheduled occurrence. The relationship is **`events.venue_id` only, never venue name**, so nothing depends on name similarity; a `MIN_VENUE_EVENTS = 2` threshold omits the block entirely below two events. **Live: exactly 7 venue pages** carry it — The Hub on Martin, Big White, SilverStar and Sonora Room capped at 3; Kelowna Beer Institute, Howling Bluff and Skaha Beach at 2 — for **18 new internal links**, all unique, all HTTP 200, all self-canonical (18/18), **0 query-string links, 0 self-links, 0 links to an expired, cancelled or postponed event**. The 11 single-event venues render **byte-identical** pages. Verified old-vs-new on an identical 639-event database: homepage, `/whats-on`, `/outdoors`, `/golf`, `/beaches`, `/trip`, region, category and event pages, sitemap and robots all byte-identical, and a **125-page venue sweep** found only the one expected difference; post-deploy, all 18 production baselines re-checked UNCHANGED, sitemap 2,248 with a 0-line diff, production steady at 639 events / 1,133 occurrences. Tests: **349 total, 348 pass** (the failure is the pre-existing golf-selector assertion); the new test covers 0/1/2/3+ event thresholds, next-date ordering, cancelled/postponed/expired exclusion, canonical links and `venue_id`-only linkage. **This completes H1: event -> related events (`fc6f57c`), region -> upcoming events (`50eaada`), venue -> events at this venue (`e5cb16f`).**
* 2026-09-22 — Claude fixed the deferred **H1 mid-span date display** — **commit `e49e7fe`** "SEO: fix mid-span event date labels" (Railway **`a1d6c109-7830-406c-8443-4753155c68a6`, SUCCESS / Online**), **`server.js` + `tests/server.test.js` only; no database, data, occurrence, span, slug, category, sitemap, schema, JSON-LD, What's On or homepage change**. **Root cause was display, not data:** the three H1 list helpers select the next scheduled occurrence that has not *finished* (`MIN(start_date) WHERE end_date >= today`), which for a multi-day occurrence already under way is one that **started in the past**; all three render sites then printed that occurrence's start date. On 2026-09-22 exactly three events were affected, each a single in-progress span: **#93 Skillful** (Jun 13 – Oct 25), **#632 Joining Hands** (Sep 11 – Sep 29) and **#638 Penticton Beer Week** (Sep 18 – Sep 27). Adds one shared formatter, **`upcomingEventDateLabel(nextDate, nextEndDate, now)`** (exported), used by **H1 Step 1, Step 2 and Step 3** in place of the duplicated `formatLocalDateShort()` call: a mid-span occurrence renders **`Until {end date}`**, everything else renders the start date exactly as before. Each helper also selects the end date of the *same* chosen occurrence (`next_end_date` → `nextEndDate`). Built on the existing `todayLocal()`/`formatLocalDateShort()` Okanagan local-date logic — no new date handling. **Selection and ordering are unchanged** (`nextDate` still reports `2026-06-13` for #93); this is wording only. Live labels, confirmed on production after the deploy: #93 `Sat Jun 13` → **`Until Sun Oct 25`**, #632 `Fri Sep 11` → **`Until Tue Sep 29`**, #638 `Fri Sep 18` → **`Until Sun Sep 27`** (the #93 card reads the same on the Step 1 event-page block). Post-deploy smoke check: an ordinary single-date card still reads `Sat Sep 26`, all 13 pre-deploy page baselines re-checked UNCHANGED, sitemap 2,248 with a 0-line diff, production steady at 639 events / 1,133 scheduled occurrences, homepage 200 on five consecutive checks. The other **636 events keep their previous label**; old-vs-new on a production-equivalent 639-event database changed only **3 of 20 region pages** (kelowna, vernon, penticton — the affected events' regions) and **0 of 7 venue-block pages**, with the homepage, `/whats-on`, `/outdoors`, `/golf`, `/beaches`, `/trip`, sitemap, robots, category pages and the affected events' own detail pages all byte-identical. Tests: **350 total, 349 pass**, the one failure being the known pre-existing golf-selector assertion; the new test pins the three real spans, the unchanged single-date/future/future-multi-day cases, both boundaries (a span starting today keeps its start date, a span ending today still reads "Until") and that all three helpers carry the end date. This closes the item deferred during H1 Steps 2 and 3.
* 2026-09-22 — Claude added the **Pinterest domain verification tag** — **commit `9c5c775`** "Add Pinterest domain verification meta tag" (Railway **`39495743-ace4-47f0-b313-6a91a2d5c3d1`, SUCCESS / Online**), **`okanagan.html` only, one line; `server.js` untouched**. The exact owner-supplied tag `<meta name="p:domain_verify" content="a670359177b913f74bd852f6b3fce40f"/>` now sits directly beneath the existing `google-site-verification` and `msvalidate.01` tags in the homepage head. **Architecture note worth keeping — there is no single global `<head>` on this site.** `server.js` emits **eleven** separate head blocks: nine are built by the shared `pageHead()` helper (`server.js:6841` — guide, region, region/category, activity, all-regions hub, What's On, venue, event and the legacy events index), `renderTripPlannerPage()` deliberately bypasses it (`server.js:9075`), `render404Page()` has its own, and **the homepage is not server-rendered at all** — `/`, `/okanagan.html` and `/browse` read the static `okanagan.html` verbatim (`server.js:9889`) and only rewrite the footer and a few `href`s. Pinterest verifies against the **root domain page**, so putting the tag in `pageHead()` would have touched all 2,248 server-rendered pages **and still failed to verify**, because `pageHead()` never reaches `/`. `okanagan.html` was therefore the only correct file, and it already carried the Google and Bing verification tags as precedent. **Verified:** the rendered homepage differs from the previous version by exactly that tag and its newline (85,839 → 85,913 bytes; 1 line added, 0 removed); canonical, both pre-existing verification tags, all 10 OG tags, all 4 Twitter tags and both JSON-LD blocks unchanged; `/whats-on`, `/outdoors`, `/golf`, `/beaches`, `/trip`, `/robots.txt`, region, category, venue, event, activity and guide pages all byte-identical; sitemap 2,248 with a 0-line diff. The tag appears on **0** server-rendered pages and, as expected, also on **`/browse`** (same static file). Tests: **350 total, 349 pass** (the failure is the pre-existing golf-selector assertion); no frozen-homepage baseline test tripped, since those assert on layout, sections, footer and links rather than head metadata. **No design, layout, styling, imagery, content, functionality, database, route, canonical, sitemap, robots or structured-data change.**
* 2026-09-23 — Claude added **three new Oliver venues from the District Wine Village discovery audit** — **data only, via `POST /api/venues` through `railway run`; no code change, no commit, no push, no deployment**. Context: an owner-approved read-only Valley-wide badge/discovery audit found that District Wine Village (100 Enterprise Way, Oliver) has **five tenants already listed** (#821 Canter Cellars, #834 Gneiss Wines, #876 Tender Hope Winery, #878 The Opera Room, #884 Winemaker's Cut) and four that were not. A follow-up verification pass confirmed each as a **distinct, currently operating business** with its own name, website and tasting programme, and confirmed **no duplicate or redirect** existed under any other name (checked against all 1,365 venues; no fuzzy matching used). Added: **#1407 Apricus Cellars** (`oliver`/`winery`, apricuscellars.com — one of the only wineries in Canada with primarily Indigenous ownership, on the traditional, unceded territory of the Syilx Okanagan Nation), **#1408 Foolish Wine** (`oliver`/`winery`, foolishwine.com, 250-404-1042 — founded by the Coghill and Main families of Neighbourhood Brewing) and **#1409 Neighbourhood Outpost** (`oliver`/`brewery`, neighbourhoodbrewing.com — the Penticton brewery's R&D brewhouse and tasting room; a **second location**, not a duplicate of #1000 Neighbourhood Brewing, matching the existing two-listing precedent for Okanagan Virtual Golf #1126/#1132). Descriptions are experience-led and built only from verified research — no invented awards, hours, amenities, food, dog access or superlatives; hours are described as *seasonal* rather than stated, because they rotate monthly. **Homepage-safety note worth keeping:** `renderGuideFooterHTML()` (`server.js:5332`) embeds live region+badge **counts** and is injected into the frozen homepage, so a new venue with any amenity boolean set to 1 would change `/`. All three were therefore created with **all 12 amenity booleans at 0** — the same discipline used for the Beaches import, and also the honest value since none of those amenities was verified. **Reconciliation:** active venues **1,365 → 1,368**, max venue ID **1,406 → 1,409**, sitemap **2,248 → 2,251 URLs** (URL-level diff: exactly 3 added, 0 removed, 0 duplicates), Oliver wineries **46 → 48**, Oliver breweries **2 → 3**, events/occurrences unchanged at **639 / 1,133** on the comparison window. All three pages return **HTTP 200** with correct self-referencing canonicals, no robots noindex, and the description rendering in both the page body and the meta description. **Homepage byte-identical** (normalised md5 unchanged, 0-line diff). **No badges applied** — all three render HiddenGem=0, LocalFav=0, DogFriendly=0. Existing venues, memberships, descriptions, badges and event records were not modified, and no repository change was required. **Official District Wine Village dog policy (recorded, not applied):** the Village FAQ states *"Yes! Dogs are welcome but must remain on leash and we ask that you keep them out of the water features and off the furniture"*, presented as site-wide, with the homepage separately noting no dogs at concerts. That establishes dogs on the **shared Village grounds, on leash** — but **indoor access at each individual business is unverified** (none of the tenants' own pages mentions pets), so **no Dog Friendly badge was applied** to the three new venues. The same policy would support grounds-level badges on the five tenants already listed, which remains an owner decision.
* 2026-09-23 — Claude imported **Field of Screams XIII: The Unlucky (#640)** from the fall 2026 seasonal research sweep — **data only, via `POST /api/events` through `railway run`, batch `whatson-2026-09-fall-seasonal-1`; no code change, no commit, no push, no deployment**. **#640** · region **`vernon`** · `venue_name_text` "Historic O'Keefe Ranch, 9380 Hwy 97 N, Vernon" · categories Holiday & Seasonal + Events & Festivals + Arts & Culture · source_type `official_organizer`, [fosokanagan.com](https://fosokanagan.com/), checked 2026-09-23. **Imported as ONE date-range occurrence: 2026-09-25 → 2026-11-01, start 18:30**, because the organizer publishes a **season range, not individual nightly dates**. This matches how #4 Okanagan Fall Wine Festival and #144 Sparkling Hill Oktoberfest are already stored. **No nightly occurrences were inferred.** **Verification note:** the event had been on HOLD in the prior audit because third-party calendars disagreed (FestivalSeekers/GlobalNews said Sept 26 – Nov 2; a local roundup said Sept 25 – Nov 1). The organizer's own site — found only indirectly, via a 2024 West Coast Traveller article citing the domain, since neither the aggregators nor Tourism Armstrong Spallumcheen link to it — states **"Sep 25 – Nov 01, 2026"** on both its homepage and its ticket-prices page, and Okanagan Edge (published 2026-09-10) independently reported *"starting Friday, Sept. 25"*. Sept 25 2026 is a Friday, cross-checked against McMillan Farms' official "Opening Saturday September 26th". The aggregators were wrong by one day. **Deliberately NOT encoded as occurrences,** because none of it establishes a nightly schedule: early-season pricing Sept 25 – Oct 4; weekly Wednesday discounts; premium nights Oct 16, 17, 23, 24 and 30; parking opens 18:00; mazes and check-in open 18:30; "not recommended for children under 12". The four 2026 mazes are The Village, Distortion, Chocolate Factory and Vintage Hotel. **Region decision (owner-made, and irreversible — `updateEvent` rejects region changes as `region_immutable`):** filed `vernon` rather than `armstrong`, because the venue's postal address is Vernon BC V1H 1W9, local press calls it "Vernon's Field of Screams", and it matches our own precedent for out-of-town Vernon-postal venues (#144 Sparkling Hill, #139 Predator Ridge). O'Keefe Ranch's own site gives its address as Spallumcheen, which is not one of our 20 region slugs. **Production result:** **1 event created, 1 occurrence, 0 venues created, 0 badges applied, 0 code changes.** Events **639 → 640**, scheduled occurrences **1,133 → 1,134**, sitemap **2,251 → 2,252 URLs**, venue URLs unchanged at 1,368. Live page HTTP 200, self-canonical, no robots noindex, `"@type":"Event"` JSON-LD and BreadcrumbList present, three category chips rendering, discoverable via `/whats-on?regions=vernon`; **homepage byte-identical**. Rehearsed offline first on a production-equivalent copy, which predicted the same id, slug, span and occurrence. **Baseline note for whoever reads the counts:** production's local date is America/Vancouver, so a query windowed from 2026-09-23 returns 631/1,121 while the server's own "today" is still 2026-09-22 and the true totals are 639/1,133 (640/1,134 after this write). Eight late-September events had not expired; they simply fell outside that query window.
* 2026-09-23 — Claude built a **repeatable Pinterest content workflow** and generated the first full batch under it — **content and documentation only; no code, no database, no deployment; nothing was published at the time of this entry — publication followed the same day, see the publication entry below.** **Skill:** created `.claude/skills/weekly-pinterest/SKILL.md` (project-level, untracked at time of writing), a PREVIEW-by-default workflow covering plan read → next Pin numbers → batch drafting with destination/board/category/visual rotation → URL verification → Gemini prompts → image QC → PREVIEW stop → PUBLISH only on explicit instruction → record. It encodes the hard safety rules (no credentials, no board creation, no Promote Pin, no CAPTCHA bypass, no AI-disclosure changes, no invented URLs) and the Pin #38 ordering lesson (Gemini → QC → save → Pinterest → draft → publish; never build a Pinterest draft then navigate away, because clipboard paste only reaches Chrome's active tab). **Batch #39–52 (14 Pins):** drafted across all 10 boards with no town used twice (Kelowna deliberately held to one slot, Vernon not repeated after #38); all 21 candidate routes re-verified HTTP 200 before drafting and `/hidden-gems`, `/keremeos`, `/princeton` reconfirmed 404 and avoided. All 14 images generated through the Gemini browser workflow on **3.6 Flash**, each verified at **848 x 1264** from the downloaded file, hash-checked, and saved to `pinterest-assets/pin-0039.jpg` … `pin-0052.jpg` (untracked, deliberately not committed). #46, #47 and #49 were regenerated once to fix overlay typography (dark banner, three-line wrap, opaque label boxes); the final v2 prompts and final image hashes are recorded, and the discarded hashes named. **Recorded in `docs/PINTEREST_CONTENT_PLAN.md`:** per-Pin number, topic, board, destination URL with its 200 verification, title, description, exact overlay text, the final Gemini prompt actually used, image path, dimensions, size, MD5, status and notes, plus batch-level verification and caveats. All 14 were recorded as generated and awaiting approval at this point, and Pin #38's record was preserved byte-for-byte. **All 14 were subsequently published on 2026-09-23 — see the publication entry below.** **Technical findings worth carrying forward:** (1) the image element in Gemini's DOM is a **downscaled 687 x 1024 preview** — the downloaded file is 848 x 1264, so QC must read the saved file, never the DOM (an early report of a resolution regression was wrong for this reason, and the model comparison it triggered was moot: Flash-Lite, 3.6 Flash and 3.1 Pro all produce the same 848 x 1264); (2) picking “newest file in `~/Downloads`” is unsafe — a failed download silently yielded a leftover Pin #38 file byte-identical to `pin-0038-v2-848px.jpg`, so downloads must be **timestamp-gated** and hash-verified (caught and corrected; only that one file was affected); (3) Gemini's Send and Download buttons frequently ignore synthetic JS clicks and often need a repeat real click, and the screenshot coordinate frame shifts between 1564x784 / 1503x812 / 1440x722 mid-session, so coordinates must be recomputed from a fresh screenshot rather than reused; (4) an errant click during a timed-out batch opened Gemini's image editor on #49 — backed out without saving, asset unaffected. **Overlay prompt lesson:** to keep typography consistent the prompt must (a) forbid every backing shape (banner, bar, box, card, pill, badge, label, plate, frame), (b) forbid darkening any rectangular region, allowing only a drop shadow that hugs each letter, and (c) state the overlay is exactly two unbroken lines with the emoji inline at letter height; without all three Gemini reverts to banner or label-box treatments. **Left open:** #44's 🌅 renders as a bordered tile and #51's 🥾 sits at the start of line 2 instead of the end of line 1 (wording exact in both; owner decision pending), and Pins 1–37 were never imported into the repo so this batch is confirmed non-duplicating only against #38 — #47 (fall events) and #49 (viewpoints) are the likeliest overlaps with the earlier broad-topic Pins. Pinterest was never opened during generation; no board was created or modified; no AI-disclosure setting was touched; `okanagan.db` unchanged.
* 2026-09-23 — Claude **published Pinterest batch #39–52 — all 14 Pins are live.** Three things happened between generation and publication. **(1) Live-account overlap audit (read-only).** The historical inventory, previously undocumented, was harvested from the account's Created feed: **37 Pins**, roughly 26 fall-themed topical Pins plus ~11 broad board-cover Pins. Two batch Pins were classified TRUE DUPLICATE against it — **#45** (existing *Okanagan Golf | Golf Courses & Scenic Views in BC*: same `OKANAGAN GOLF` overlay headline, same `/golf` destination, same green-plus-lake concept) and **#47** (two existing fall-events Pins, *Fall Events in the Okanagan* and *Okanagan Fall Events & Festivals | What's On This Autumn*, plus an evergreen sibling, all on `/whats-on`). **#49 Viewpoints was checked and cleared** as RELATED BUT DISTINCT. The duplicate-risk caveat about Pins 1–37 is therefore **closed**. **(2) Replacements (commit `b5ae623`).** #45 became **Kelowna Golf** → `/kelowna/golf`, a distinct server-rendered page listing 20 verified courses (the largest of the 11 regional golf pages); #47 became **Okanagan Live Music** → `/whats-on?categories=live-music`, the site's largest event category at 140 events. Note that filter is applied **client-side** — it works for a visitor but the page's title and meta stay generic, so it is not a separate page for SEO. **(3) Publication.** Each Pin was published as an individual transaction through the organic pin builder — image uploaded via the file input, exact board/title/description/destination taken from the committed plan, fields reviewed, Publish clicked once, then confirmed on the account before moving to the next. A final sweep found **14/14 live, 0 missing**, every title matching the plan. Live Pin IDs run **1124422232001517062** (#39) to **1124422232001518662** (#52); the full per-Pin URLs are recorded in `docs/PINTEREST_CONTENT_PLAN.md` (commit `c7a0091`). **Mark as AI-Modified was OFF on every Pin and never touched**; no board was created or modified, no existing Pin edited or deleted, **#38 was not republished**, and no account setting was changed. **Operational note for next time:** the `_created` feed frequently returns an empty DOM for 20–40 seconds after a publish and needs a scroll to lazy-load — several verification reads had to be retried, but no Pin was ever actually in doubt. `pinterest-assets/` remains untracked by design; no code, database, homepage or deployment change occurred.
* 2026-09-23 — Claude completed the **Adventure category audit and its two approved writes (data only; no code, no deployment).** **Venue added:** **#1410 Okanagan Parasail**, created via the bearer-guarded `POST /api/venues` and given an `activity_adventure` membership via `POST /admin/collection-membership`; the Adventure collection went from **18 to 19 members**. Production details for #1410 were verified after the write and the venue is **live**. All 12 `BOOL_FIELDS` were written as 0 so `renderGuideFooterHTML()` could not move any homepage badge count. **Phones added:** verified phone numbers were written for the **8 owner-approved Adventure venues** — **#1242** ZipZone Peachland, **#1300** Okanagan Skydive, **#1302** Paraglide Canada, **#1303** Okanagan ATV Tours, **#1304** Full Cyrcle Adventures, **#1305** Sun Hills Riding Centre, **#1306** Inspire Trails – Myra Canyon Ranch, **#1308** LocoLanding Adventure Park. Each number was confirmed against the operator's own published contact information before the write; `phone` was the only field sent. **Deliberately not changed:** the audit also surfaced Adventure records with missing coordinates, missing phone numbers, and one incomplete address (#1307), plus open resort/park taxonomy questions. **None of those were touched** — they remain exactly as they were, and **no speculative coordinates were added** (nothing was geocoded, inferred, or guessed). The owner's instruction was explicitly to finish Adventure rather than pursue perfect research coverage, so **Adventure was considered complete at this point and work moved on**; the remaining gaps stay open for a future, separately-approved pass. The token was injected only via `railway run` and was never printed or written to disk. The repository's `okanagan.db` was not modified.
* 2026-09-23 — Claude **corrected the homepage Wine routing and shipped a dedicated valley-wide `/wineries` hub** — **commit `ed91d4a`** "feat: add dedicated valley-wide /wineries hub" (Railway deployment **`3dbe1623-025e-4504-b05f-075c0510302d`, status SUCCESS**). **The bug:** the homepage Wine mood card and the footer Wine link resolved through `bestRegionForType` to `/oliver/wineries`, showing only Oliver's wineries. An intermediate fix (`057be1d`) repointed both at `/browse?types=winery` — valley-wide, but a generic browse listing rather than a Wine experience. The owner rejected that, so **`/browse?types=winery` was replaced by the dedicated `/wineries` hub**; both the **homepage Wine mood card** and the **homepage footer Wine link** now point at **`/wineries`**. **The hub:** `/wineries` is an Okanagan-wide Wine/Wineries hub that lists **all 204 wineries in the valley** before any region is chosen. It is **winery-only** — it does not show restaurants, cafés, breweries, pubs, cocktail lounges or any other venue type. It uses the same **regional-selector behaviour as `/beaches`**: the selector's options are plain links to the 14 existing `/{region}/wineries` pages, so choosing a region filters the results down to that region's wineries. `/wineries` was **added to the sitemap** beside `/beaches`, `/golf` and `/outdoors`. **Hub-only theming (the key constraint):** `winery` was deliberately **NOT** added to the global `THEMED_CATEGORY_TYPES`, because that would have restyled the 14 live winery region pages and 204 live winery venue pages. Instead a new `HUB_ONLY_THEMED_TYPES` set is consulted **only** by `renderCategoryAllRegionsPage()`, which threads an explicit `themed` override down into the shared helpers (`golfEngagementHeadHtml`, `themedBodyClassAttr`, `golfCardEngagementScriptHtml`, `venueCardHtml`); every one of those helpers still defaults to `usesThemedCategoryLayout()`, so no other caller's output can move. The hub therefore renders the Beaches-style presentation (`<body class="golf-page winery-page">`) while **existing regional winery pages and individual winery pages keep their original styling** — verified live as `<body>` with no class and zero `venue-card-cue` markers. **Approved back-links:** adding `winery` to `ALL_REGIONS_CATEGORIES` also switches on the shared category back-link, so regional winery pages gained **`← All Wineries`** (→ `/wineries`) and individual winery pages gained **`← {Region} Wineries`** (→ `/{region}/wineries`). This was flagged as a deviation from "preserve those pages exactly" before any commit and was **explicitly approved** by the owner; it is one added `<a>` element per page and nothing else changed on them. **Live verification (production, after deploy):** `/wineries` returns **HTTP 200**; 204 winery cards and **0** cards of any other type; all 14 `/{region}/wineries` pages return 200 and their card counts **sum to exactly 204**, with no non-winery venue links on any of them; `/wineries` present in `/sitemap.xml`; homepage mood card and footer both resolve to `/wineries`; a byte diff of the live homepage before vs. after the deploy shows **exactly the two Wine `href` values changed and nothing else**; byte diffs of a live winery region page and a live winery venue page show only the one added back-link plus the shared footer link. **`okanagan.db` was unchanged** (0 diff lines, clean in `git status`). **Tests: 350 pass / 1 fail of 351.** The Wine tests pass — `Mood cards: Wine links to the dedicated valley-wide /wineries hub, not a single region` and the new `Wine hub: /wineries is enabled, winery-only, valley-wide, and themed for the hub ONLY`. **The single remaining failure is the known pre-existing golf-selector failure, test #281** (`HTTP routes: region, category, venue, guide, and 404 all respond correctly`, asserting `selector must NOT list a region with zero golf venues in this fixture set`) — **unrelated to Wine**, and proven pre-existing by running the same suite against HEAD on a free port, where it fails as `#280` with byte-identical error text (the number shifts by one only because this change adds a test). Note for future runs: earlier `EADDRINUSE :::3001` reports on this test were a leftover test process holding the port, not a code fault — kill stray `tests/server.test.js` processes before trusting that failure mode. Four Wine assertions were updated in `tests/server.test.js`; one of them (inside #281) previously asserted `/wineries` was a 404 and was repointed to `/restaurants`, which remains a genuine `ALL_REGIONS_CATEGORIES` allowlist guard.
* 2026-09-23 — Claude **added the winery engagement controls** — **commit `49c4211`** "feat: add winery engagement controls" (Railway deployment **`b413eaef-6911-4b35-b9e5-572c6a7caff3`, status SUCCESS**). **How the gap was found:** a read-only audit of the `/wineries` section checked the Favorite / Add to Trip controls on every winery surface instead of assuming the shared card component carried them. It did not. `venueCardHtml` gated `cardActions` on the same flag as the visual theme (`const isGolf = themed`), and `renderVenuePage` gated the CTA-row buttons on `usesThemedCategoryLayout()`. Because `winery` lives in `HUB_ONLY_THEMED_TYPES` and not in `THEMED_CATEGORY_TYPES`, only the hub was themed — so **the hub had the controls on all 204 cards while all 14 region pages and all 204 venue pages had neither button**, even though golf and beach region/venue pages did. This was a direct, unforeseen consequence of the hub-only theming approved in `ed91d4a`: preserving the winery pages visually also withheld their engagement controls. **The fix (option (a), "controls without restyling"):** a new `ENGAGEMENT_ONLY_TYPES` set (`{'winery'}`) plus `usesEngagementControls()` marks types that get the controls on their region and venue pages **without** the themed presentation. `venueCardHtml` gained an `actions` option **defaulting to `themed`**, so every pre-existing caller renders byte-identically and only the two winery surfaces opt in; `liAttrs` is now emitted when either `isGolf` or `actions` is true, because the engagement script locates a card by `data-venue-category`. Button styling comes from a new `renderEngagementControlStyles()` injected **only** on engagement-only pages — **nothing was added to the shared `SEO_PAGE_CSS`**, so no other category's bytes moved. **Key enabler:** `golfFavTripScriptBody()` already had a **standalone branch** for when `app.js` is absent, writing to the same `okanaganFavorites` / `okanaganTrip` localStorage keys the homepage Trip Planner reads — so winery pages need **no `app.js`, no `app.css`, and no trip tray**, which is exactly what made "controls without restyling" achievable. **Live verification after deploy:** all **14** winery region pages carry the controls on **204/204** cards with `golf-page=0`, `venue-card-cue=0`, `tripTray=0`, `app.js=0`; **14/14** sampled winery detail pages (one per region) return 200 with both buttons and the CTA-row engagement attributes, unthemed; and the homepage, the `/wineries` hub, `/kelowna/golf` and `/kelowna/restaurants` were captured live **before and after** the deploy and are **byte-identical** (homepage md5 `94eb3d64a2d3d8ecc22ce7b5cc5c369d`, hub `74ef9f63caeff2fcc15488af0e4df851`). The winery pages changed **by addition only** — the sole "removed" lines were `<li class="venue-card">` and `<div class="venue-cta-row">` gaining attributes, plus Cloudflare's `email-decode.min.js` tag shifting line position (present exactly once before and after; it is injected at the end of `<body>`, so added content moves it). **Tests: 351 pass / 1 fail of 352**, the new `Wine engagement: winery region and venue pages carry Favorite / Add to Trip WITHOUT the themed styling` passing; the sole failure remains the pre-existing golf-selector assertion (now `#282`, shifted again by the added test) and was deliberately not touched. **Process note — a real mistake:** during implementation a syntax check was run as `node -e "require('./server.js')"` **from the repository root**, which booted the server against the repo's `okanagan.db` and mutated it (417,792 → 471,040 bytes), violating the standing "never modify `okanagan.db`" rule. It was caught in `git status` before committing and reverted with `git restore okanagan.db`; the md5 returned to `347b535f842883d5d2d264d9ef585479` and the file was confirmed absent from the commit. **Use `node --check server.js` for syntax checks — never `require()` from the repo root.** **Architectural note:** `winery` is now the only category with a third presentation mode — engagement controls but no theme — alongside "themed" and "plain". It is contained behind `ENGAGEMENT_ONLY_TYPES`, but it is added surface area worth knowing about if wineries are ever fully themed. **Still outstanding from the same audit (data, not code):** all **204** wineries are UNRESOLVED on amenity accuracy (current state: dog_friendly 105, patio 145, kid_friendly 61, lake_view 77, great_groups 43, live_music 27, hidden_gem 2, local_favorite 0); **Hidden Gem and Local Favourite are `collections` rows, not boolean columns, and neither has a documented qualifying rule**; and only **2 of 204** wineries have a `website` stored, which blocks the top tier of the evidence hierarchy.
* 2026-09-23 — Claude completed the **Oliver + Naramata winery audit and its two approved production data writes (data only; no code, no commit, no deployment).** **Scope:** the 81 wineries in those two regions (Oliver 48, Naramata 33) out of 204 valley-wide. **Write 1 — 5 approved amenity changes:** **#172** Deep Roots Winery `kid_friendly 0→1` and `lake_view 0→1`, **#843** Lang Vineyards `lake_view 0→1`, **#852** Mocojo Winery `lake_view 0→1`, **#845** Laughing Stock Vineyards `patio 0→1`. Two researched candidates were **deliberately held and NOT written** — #845 and #843 `dog_friendly` — because their only evidence was a local guide rather than an official source; they remain 0 and unresolved. **Write 2 — 46 official winery websites:** 44 written, **2 intentionally skipped** (#1407 Apricus Cellars and #1408 Foolish Wine already held correct official URLs, and the audit did not establish a different one), so all 46 targets now hold a website. Oliver+Naramata coverage went **2 → 46 of 81**; across all 204 wineries, 46 now have a website where 2 did before. The live "Visit Website" button, which could never render for these venues, now does. **Website research method (worth reusing):** the venue records stored almost no websites, so 726 candidate domains were generated from the winery names, every one was fetched, and each was accepted only if the site's **own `<title>` named the winery AND showed wine-specific evidence AND was not parked/for-sale/empty**. That rejection step is the important part — a naive name-to-domain guess would have written **`aasha.com`** (hiring software), **`chainreaction.com`** (bicycles), **`deeprootsmusic.ca`** (a music co-op), **`elephantisland.com`** (horse racing), **`lang.com`** (calendars), **`opera.com`** (the browser) and **`canter.com`** (real estate) as winery sites. An initial looser scoring pass produced 63 "verified" rows and was contaminated by exactly these; it was re-scored strictly, which is why the final figure is lower and trustworthy. **Guards on both writes:** unauthenticated `PUT` → 401 confirmed first; per record, `type='winery'`, `redirect_to IS NULL`, region in {oliver, naramata}, the expected pre-state asserted, a non-empty-website skip, and a post-write field-by-field comparison. `website`, or the single amenity field, was the only key sent in each request. Token injected via `railway run`, never printed or written to disk. **Reconciliation (live API, all 1,369 venues, before vs after):** venue count 1,369 → 1,369 with an identical id set; **49 field changes total — 44 website + 5 amenity — 0 outside the approved sets**; 0 non-winery records changed; 0 out-of-region records changed; 0 stored URLs not `https://`. Tests **351 pass / 1 fail of 352**, the sole failure the pre-existing golf-selector assertion. The repository's `okanagan.db` was **not** modified (md5 `347b535f842883d5d2d264d9ef585479`) — venue data lives in production's `/data/okanagan.db`, and the repo file is a test fixture. **Data-model findings that constrain any future winery amenity work:** the `venues` table has exactly 12 boolean amenity columns (`dog_friendly, vegan, vegetarian, patio, kid_friendly, gluten_free, lake_view, nonalcoholic, sports_tv, live_music, great_groups, happy_hour`), so **Picnic, Food, Tasting Room and Tours cannot be stored without a schema change** — they were requested, found in research, and deliberately not recorded. **Hidden Gem and Local Favourite are `collections`/`collection_items` rows, not boolean columns, and NEITHER HAS A DOCUMENTED QUALIFYING RULE** — in scope there is exactly 1 Hidden Gem (#816 Black Widow Winery) and **0 Local Favourites, which is also 0 site-wide across every category**. No membership was assigned; an objective rule was proposed to the owner and is awaiting approval, deliberately not invented unilaterally. **Still open:** 4 ambiguous domains needing review — **#821** Canter Cellars (`canter.com` is a real-estate firm; rejected), **#72** Black Hills (`blackhillsvineyards.com` and `blackhillswinery.com` both resolve), **#874** Sunrock Vineyards (may be a different winery; Oliver's Sunrock is a Jackson-Triggs vineyard), **#876** Tender Hope (`tenderhopewinery.com` returns "under maintenance"); 27 wineries with no confidently verified website, including 5 whose candidates never responded (#855 Nichol, #237 Gehringer Brothers, #838 Jackson-Triggs & Inniskillin, #390 Manzil at Kismet, #885 vinAmité) — several of these are large, well-known wineries that certainly have sites on domains the name patterns did not generate, so they need targeted per-winery search; 35 of the 81 still have no website; and **73 of the 81 remain UNRESOLVED on amenity accuracy** (only 8 were verified in depth, because with no stored websites each one costs several lookups). The other 123 wineries in the remaining 12 regions have not been audited at all.
* 2026-09-23 — Claude completed the **winery fast-pass batch 3 — Kelowna, Okanagan Falls and Summerland (58 wineries)** — data only, written to **production through the live API**; no code, schema, homepage or deployment change. **Written: 40 field changes across 39 venues** — **39 HIGH-confidence official winery websites** plus **#858 Observatory Wines `lake_view` 0→1** (its own Okanagan Roam description states "stunning lake views" twice). #858 received both a website and the amenity change, which is why 40 changes touch 39 venues. **Deliberately NOT written:** the 3 MEDIUM-confidence websites (#9 8th Generation, #757 Ve Oh Lay Acres, #864 Rigour and Whimsy), the 10 UNVERIFIED domains, the 6 wineries whose candidates never responded, and all 4 MEDIUM amenity candidates (#442 Noble Ridge dog/kid, #125 CedarCreek kid, #606 SpearHead kid — sourced from a single secondary site, left unresolved). **Method:** same strict verification as the Oliver/Naramata batch — 634 candidate domains probed, accepted only when the site's own `<title>` named the winery, showed wine evidence and was not parked. It again rejected unrelated businesses that a name-based guess would have written: `camelot.ca` (fabrics/crafts), `mykitsch.com` (hair care), `sandhill.com` (software) and `view.com` (smart windows). Six domains returned HTTP 200 with an **empty title** (`cedarcreekestate.ca`, `vibrantvinewinery.com`, `ramification.ca`, `seeya.com`, `garnetvalley.ca`, `silkscarf.com`) — these are probably the real wineries but are JS-rendered, so the text-only method cannot confirm them; they need a JS-capable check rather than more searching. **[CORRECTED 2026-09-24: these domains are PARKED (114-byte `/lander` stubs), not JS-rendered, and are NOT the wineries' sites — the "a JS-capable check would convert them" recommendation in this entry is wrong. See the 2026-09-24 parked-domain entry.]** **Verification:** venue count **1,369 → 1,369** with an identical id set; exactly **40 field changes (39 website + 1 lake_view), 0 outside the approved set**; 0 non-winery and 0 out-of-region records changed; 0 website value mismatches; unauthenticated `PUT` → 401 confirmed before writing, and every record guarded on type/redirect/region/empty-website/expected pre-state with a post-write field comparison. **Winery website coverage 46 → 85 of 204** (Kelowna 12/21, Okanagan Falls 14/19, Summerland 13/18). Tests **351 pass / 1 fail of 352**, the sole failure the pre-existing golf-selector assertion; `node --check server.js` and `node --check db.js` both passed (used deliberately instead of `require('./server.js')`, which mutated the repo DB in an earlier session). Live spot-checks passed — "Visit Website" now renders on the sampled venue pages and Observatory shows its Lake View badge. The repository's `okanagan.db` was **not** modified (md5 `347b535f842883d5d2d264d9ef585479`). **Remaining:** 119 wineries still have no website — 19 in these three regions plus **65 across the 9 untouched regions** (penticton 18, west-kelowna 17, osoyoos 11, lake-country 9, peachland 4, kaleden 2, vernon 2, enderby 1, armstrong 1); Penticton tied Summerland at 18 and was not chosen for this batch. Amenity accuracy across the other regions remains unaudited, and the Hidden Gem / Local Favourite rules are still undefined.
* 2026-09-23 — Claude completed **winery fast-pass batch 4 — Penticton, West Kelowna and Osoyoos (46 wineries audited)** — data only, written to **production through the live API**; no code, schema, homepage, commit or deployment change. **Written: 29 field changes across 28 venues** — **28 HIGH-confidence official winery websites** plus **#842 Lakeside Cellars `lake_view` 0→1** (its own Okanagan Roam description says "unbeatable lake views"). #842 received both a website and the amenity change, hence 29 changes across 28 venues. **#412 Mission Hill was stored as `https://www.missionhillwinery.com/`** — the crawl had captured `...missionhillwinery.com:443/`, and the `:443` artifact was stripped before writing. **Three classification corrections were made during the preflight and are worth remembering:** **#866 Roche Wines was demoted out of HIGH** because `rochewinery.com` is *"Roche Sonoma Winery"*, a California winery — the obvious domain belongs to a same-named business and would have been a wrong write; **#491 Pentage was promoted to HIGH** after its stylized "â" in "Pentâge Winery" was stripped by the non-printable filter and caused a false name mismatch; **#441 Nk'Mip Cellars was promoted to HIGH** after the crawl landed on `/About-Us`, with the root `nkmipcellars.com` verified 200. **Deliberately NOT written:** the 3 MEDIUM websites (#468 Over the Fence, #817 Blackwood Lane — whose domain advertises an unrelated appliance distributorship, #140 Chronos), the 12 unverified domains **including 8 that return HTTP 200 with an empty title because they are JS-rendered** (`bordertown.com`, `lariana.com`, `nightbird.com`, `fourshadows.com`, `frind.ca`, `indigenousworldwines.ca`, `littlestraw.com`, `quailsgatewinery.com`), the 3 wineries whose candidates never responded (#13 Adega On 45th, #846 Liber Farms, #751 Upper Bench), and all 6 MEDIUM amenity signals (#767 Volcanic Hills dog+kid, #370 Little Straw kid, #722 Three Sisters kid, #357 Lakeboat kid, #842 Lakeside kid — all from a single secondary source). **Reconciliation:** venue count **1,369 → 1,369** with an identical id set; exactly **29 field changes (28 website + 1 lake_view), 0 outside the approved set**; 0 non-winery and 0 out-of-region records changed; 0 stored-value mismatches, 0 non-`https://` URLs and 0 `:443` remnants; unauthenticated `PUT` → 401 confirmed before writing, with per-record guards on type/redirect/region/empty-website/expected pre-state and a post-write field comparison. **Winery website coverage 85 → 113 of 204** (Penticton 11/18, West Kelowna 13/17, Osoyoos 4/11). Tests **351 pass / 1 fail of 352**, the sole failure the pre-existing golf-selector assertion; **`node --check server.js` and `node --check db.js` both passed** and `require('./server.js')` was deliberately not used. Live spot-checks passed on #842 (website + Lake View badge), #412, #491, #441 and #473, and #530 Quails' Gate was confirmed to still have **no** website, proving the JS-domain exclusions held. The repository's `okanagan.db` was **not** modified (md5 `347b535f842883d5d2d264d9ef585479`). **Remaining: 91 wineries without a website** — 18 in these three regions plus **19 across the 6 untouched regions** (lake-country 9, peachland 4, kaleden 2, vernon 2, enderby 1, armstrong 1). **The JS-rendered empty-title problem has now blocked ~14 records across two batches and is the single highest-value fix**: a JS-capable check would likely convert most of them, including major wineries like Quails' Gate, Frind and Indigenous World. **[CORRECTED 2026-09-24: these domains are PARKED (114-byte `/lander` stubs), not JS-rendered, and are NOT the wineries' sites — the "a JS-capable check would convert them" recommendation in this entry is wrong. See the 2026-09-24 parked-domain entry.]** Amenity accuracy outside Oliver/Naramata remains largely unaudited, and the Hidden Gem / Local Favourite rules are still undefined.
* 2026-09-24 — Claude completed **winery fast-pass batch 5 — the final six untouched regions: Lake Country, Peachland, Kaleden, Vernon, Enderby and Armstrong (19 wineries)** — data only, written to **production through the live API**; no code, schema, homepage or deployment change. **This completes the untouched-region sweep: all 14 winery regions have now been audited.** **Written: 10 HIGH-confidence official winery websites** — #8 50th Parallel Tasting Room `50thparallel.com`, #32 Arrowleaf Cellars `arrowleafcellars.com`, #291 Intrigue Wines `intriguewines.com`, #445 O'Rourke Family Estate `orourkefamilyestate.com`, #489 Peak Cellars `peakcellars.com`, #258 Hainle Vineyards `hainle.com`, #811 Anthony Buchanan Wines `anthonybuchananwines.ca`, #80 Blind Tiger Vineyards `blindtigervineyards.ca`, #705 The Perch Winery `theperchwinery.com`, #47 Baccata Ridge `baccataridgewinery.ca`. **No amenity changes were proposed or written** — no description in these six regions carried a clear signal, and nothing met the HIGH bar within the fast-pass budget. **A near-miss worth remembering:** the automated scoring initially ranked **#32 Arrowleaf on `arrowleaf.com`, which is "Arrowleaf Real Estate — Real Estate Investment"**; the correct site, `arrowleafcellars.com`, was the second candidate. The cause was the same as `canter.com` in the Oliver batch — **"estate" had been left in the wine-evidence keyword set, where it matches "Real Estate"**; it has been removed. Two other same-name traps the check caught: `blindtiger.ca` is "Blind Tiger Properties" (executive rentals) and `first.ca` is "First Aerospace". At 19 wineries the candidate list was small enough to read directly rather than trust the scoring, which is how these were caught. **Deliberately NOT written:** the 1 MEDIUM website (#4 19 Bistro @ Fitz — a bistro record, no wine evidence), the **5 JS-rendered empty-title domains** (#207 Ex Nihilo, #250 Gray Monk, #216 Fitzpatrick Family, #215 First Estate, #194 Edge of the Earth), the 2 wineries whose candidates never responded (#343 Kraze Legz, #559 Row 188), and #7 50th Parallel Block One Restaurant. **#7/#8 and #445/#559 are each two records for one physical winery** (restaurant or secondary room vs. the main winery); only the primary record was given the URL, because assigning a winery's domain to its restaurant sub-record is a data-model decision the owner has not made. **Reconciliation:** venue count **1,369 → 1,369** with an identical id set; exactly **10 field changes, 0 outside the approved set**; 0 non-winery and 0 out-of-region records changed; 0 stored-value mismatches; all URLs `https://`; every excluded record verified still `website=None`. Unauthenticated `PUT` → 401 confirmed first, with per-record guards on type/redirect/region/empty-website and a post-write field comparison. **Winery website coverage 113 → 123 of 204** (lake-country 5/9, peachland 1/4, kaleden 1/2, **vernon 2/2 and enderby 1/1 complete**, armstrong 0/1). Tests **351 pass / 1 fail of 352**, the sole failure the pre-existing golf-selector assertion; `node --check server.js` and `node --check db.js` passed and `require('./server.js')` was not used. Live spot-checks passed on all six sampled pages. The repository's `okanagan.db` was **not** modified (md5 `347b535f842883d5d2d264d9ef585479`). **Remaining: 81 wineries without a website.** The **~13 JS-rendered empty-title domains now accumulated across batches 3–5 are the single highest-value follow-up** — Quails' Gate, Frind, Indigenous World, Gray Monk, CedarCreek, Ex Nihilo, Fitzpatrick and others are almost certainly correct domains that need only a JS-capable check, not more searching. **[CORRECTED 2026-09-24: these domains are PARKED (114-byte `/lander` stubs), not JS-rendered, and are NOT the wineries' sites — the "a JS-capable check would convert them" recommendation in this entry is wrong. See the 2026-09-24 parked-domain entry.]** Amenity accuracy outside Oliver/Naramata remains largely unaudited, and the Hidden Gem / Local Favourite rules are still undefined.
* 2026-09-24 — Claude **investigated the 19 "JS-rendered" winery domains and found the earlier diagnosis was WRONG — they are PARKED domains** — then wrote the 8 real official sites that the investigation uncovered (data only; production via the live API; no code, schema, homepage or deployment change). **The correction:** batches 3–5 recorded these domains as "JS-rendered, probably the real wineries" and recommended a JS-capable check as the highest-value follow-up. **Both claims were wrong.** Every one returns a 114-byte stub — `<script>window.onload=function(){window.location.href="/lander"}</script>` — the standard domain-parking signature: **16 of 19 PARKED, 2 empty stubs, 1 (`lariana.com`) real content but not the winery.** So `quailsgatewinery.com`, `frind.ca`, `graymonkwinery.com`, `cedarcreekestate.ca`, `indigenousworldwines.ca` and 11 others are **not** those wineries' sites, and a browser-based check would have converted nothing. The strict title-verification method had correctly withheld all 19, so none was ever written — the error was in the recorded explanation, not in the data. The four affected sentences in the batch 3/4/5 entries above have been annotated in place rather than rewritten. **Written: 8 official winery websites**, found by probing the wineries' actual domains instead: **#125** CedarCreek `cedarcreek.bc.ca`, **#716** The Vibrant Vine `thevibrantvine.com`, **#530** Quails' Gate `quailsgate.com`, **#228** Frind `frindwinery.com`, **#216** Fitzpatrick Family `fitzwine.com`, **#830** Four Shadows `fourshadowsvineyard.com`, **#250** Gray Monk `graymonk.com`, **#207** Ex Nihilo `exnihilovineyards.com`. Six were verified directly by the site's own `<title>`. **#250 and #207 could not be fetched directly — `graymonk.com` returns a Cloudflare "Just a moment..." challenge and `exnihilovineyards.com` returns 403 to a scripted request — and were instead verified through their own official pages in search** (Gray Monk's `/visit/` and `/contact/` pages carry the winery's address at 1055 Camp Road, Lake Country; Ex Nihilo's pages carry `tastingroom@exnihilovineyards.com`). **These 8 are no longer unresolved candidates.** **A trap worth remembering:** `cedarcreekwinery.com` also resolves with real content titled "Cedar Creek Winery", but it is a **different** winery — the Okanagan one is `cedarcreek.bc.ca`. Bot protection (403 / Cloudflare challenge) is a distinct condition from parking and from JS rendering; all three produce a "no usable title" result and must not be conflated again. **Reconciliation:** venue count **1,369 → 1,369** with an identical id set; exactly **8 field changes, all website, 0 outside the approved set**; 0 non-winery records changed; 0 stored-value mismatches; all URLs `https://`; **all 11 deliberately-excluded records verified still `website=None`** (#862 Ramification, #577 See Ya Later Ranch, #833 Garnet Valley Ranch, #869 Silkscarf, #844 Lariana, #438 Night Bird, #288 Indigenous World, #370 Little Straw, #215 First Estate, #194 Edge of the Earth, #90 Bordertown). Unauthenticated `PUT` → 401 confirmed first, with per-record guards on type/redirect/region/empty-website and a post-write field comparison. **Winery website coverage 123 → 131 of 204.** Tests **351 pass / 1 fail of 352**, the sole failure the pre-existing golf-selector assertion; `node --check server.js` and `node --check db.js` passed. Live spot-checks passed on all 8 venue pages, each rendering its "Visit Website" button. The repository's `okanagan.db` was **not** modified (md5 `347b535f842883d5d2d264d9ef585479`). **Remaining: 73 wineries without a website.** The 11 above stay unresolved — their real domains exist but were not found within the fast-pass budget, and the parked domains bearing their names must never be used.
* 2026-09-24 — Claude ran a **targeted website-discovery pass over the 11 wineries left unresolved by the parked-domain investigation** and wrote the 4 approved results (data only; production via the live API; no code, schema, homepage or deployment change). **Written: 4 official winery websites**, all previously empty — **#577** See Ya Later Ranch `https://sylranch.com/`, **#288** Indigenous World Winery `https://www.indigenousworldwinery.com/`, **#90** Bordertown Vineyards `https://bordertownwinery.com/`, **#844** Lariana Cellars `https://www.larianacellars.com/`. **All four are real sites that block or gate scripted fetches**, so the usual `<title>` check could not confirm them and each was verified another way: See Ya Later by its own `/the-vineyard/` and `/the-ranch/` pages (2575 Green Lake Road, Okanagan Falls; `info@sylranch.com`); Indigenous World by its official site details (Westbank, on-site Red Fox Club restaurant); Bordertown by its official listing titled "Bordertown Vineyards & Estate Winery | Osoyoos, BC"; and **Lariana by the BC Wine Institute directory**, which names `larianacellars.com` alongside the winery's address (9304 2nd Avenue, Osoyoos) and phone (250-498-9259). Lariana's `http://` form 302-redirects to `https://www.larianacellars.com/`, so the HTTPS form was stored for consistency with the other 134. **Two further wineries were resolved in this research but deliberately NOT written, at the owner's instruction, pending separate review: #370 Little Straw Vineyards `littlestraw.bc.ca` and #869 Silkscarf Winery `silkscarf-winery.com`.** Both were confirmed by direct `<title>` match on large real pages ("Home || Little Straw Vineyards", "silkscarf winery | Silkscarf Winery") and are the strongest evidence in this pass — they remain `website=None` and are the obvious next candidates. **A methodological gap worth carrying forward:** those two had been missed in every earlier batch because the candidate generator never produced **`.bc.ca` domains** or **hyphenated names**. Both patterns should be added if the sweep is ever re-run; they are not exotic in BC. **Still unresolved after this pass (5):** #862 Ramification Cellars, #833 Garnet Valley Ranch, #438 Night Bird Estate, #215 First Estate, #194 Edge of the Earth. `.com`, `.ca`, `.bc.ca`, `.wine`, hyphenated and `www`-prefixed variants were all tried plus targeted searches; several are small operations that plausibly have no site of their own, and **#194 appears to have only a Facebook page**. Per the standing rule, no directory or social profile was proposed as a winery's website. **Reconciliation:** venue count **1,369 → 1,369** with an identical id set; exactly **4 field changes, all website, 0 outside the approved set**; 0 non-winery records changed; 0 stored-value mismatches; all URLs `https://`; **all 5 unresolved records and both held records (#370, #869) verified still `website=None`**. Unauthenticated `PUT` → 401 confirmed first, with per-record guards on type/redirect/region/empty-website and a post-write field comparison. **Winery website coverage 131 → 135 of 204.** Tests **351 pass / 1 fail of 352**, the sole failure the pre-existing golf-selector assertion; `node --check server.js` and `node --check db.js` passed. Live spot-checks passed on all 4 venue pages, each rendering its "Visit Website" button. The repository's `okanagan.db` was **not** modified (md5 `347b535f842883d5d2d264d9ef585479`). **Remaining: 69 wineries without a website.**
* 2026-09-24 — Claude started the **Food & Drink section**: audited the directory, verified its misclassifications, shipped **URL-redirect protection (commit `b0c9b52`, Railway `ad5f18a4`, SUCCESS)** and then applied the **14 verified primary-category changes** to production. **The 841 question, answered:** the directory's "841 places" is not hard-coded anywhere — it is exactly `restaurant 558 + cafe 183 + brewery 38 + pub 37 + cocktail 25`, computed client-side in `public/scripts/app.js` from `/api/venues` after the homepage's Food & Drink card links to `/browse?types=restaurant,cafe,brewery,pub,cocktail`. **Wineries are already excluded** (they would make 1,045). The other 528 venues are outdoor 196, winery 204, beach 79, golf 49. Two related bugs were found and NOT fixed: `okanagan.html:368` ships a **hard-coded, stale `"1,069 places to explore"`** (the real active count is 1,369) which is visible until JS replaces it, and `server.js:9770` documents an **interval-poll hack** that rounds counts over 1000 down to "1000+" after CSS and MutationObserver approaches both failed. **The redirect protection — why it had to come first:** a venue's canonical URL is built from `CATEGORY_SLUGS[type]`, so changing `type` moves the URL and the old one would 404; `redirect_to` only covers venue-to-venue duplicates. All 14 targets were live and in the sitemap, so the reclassification would have broken 14 indexed URLs. **I had told the owner these changes were "pure data, zero code, zero risk" — that was wrong, and I corrected it before writing rather than after.** The fix adds `findActiveVenueBySlugAcrossTypes()` plus a fallback in the `GET /:region/:category/:slug` route, placed after the existing render/redirect block and before the 404, so it is only ever reached on a path that already 404s and no existing 200 or 301 can change. **The critical guard:** it returns a result only when EXACTLY ONE active venue matches, because **`(region, slug)` is NOT unique in production** — `penticton-golf-country-club`, `sumac-ridge-golf-country-club` and `vernon-golf-country-club` each exist as both a golf course and a separate clubhouse-restaurant record sharing region and slug, and all four URLs resolve correctly today. `redirect_to IS NULL` keeps retired venues with the existing venue-to-venue redirect. Test `Category-change redirect: the cross-type slug lookup resolves a moved venue and refuses to guess` covers the helper's guards rather than binding a port — **the shared harness's only listener runs inside the long 'HTTP routes' test, which fails on the pre-existing golf-selector assertion BEFORE reaching its `app.server.close()`, so port 3001 can stay bound. That is the root cause of the intermittent `EADDRINUSE :::3001` seen throughout this project.** **Verification pass before writing:** of 40 name-flagged suspects, 30 were checked in depth against our own `description`/`cuisine` fields plus official sources; **9 were name traps and correctly left alone** — #36, #99, #898, #909 are fish-and-chips/Asian/Cuban restaurants whose names merely contain "Cafe", #1028 Lynx Lounge and #1024 Sol Grill Room are described in our own data as restaurants, #231 and #282 are Indian restaurants using "Lounge" as branding, and #671 Bulldog Grand Cafe was already correctly typed `pub`. **"Lounge" proved to be no signal at all: 4 of 5 lounge-named suspects are ordinary restaurants**, so Cocktail Lounges stays at 25. A strong internal corroborator worth reusing: **11 of the 12 pub suspects already carried `cuisine='pub'`** in our own data. **Written (14 changes, production via the live API):** #2, #980, #1018, #919, #1065, #956, #903, #911, #912, #1045, #928, #978 and #1051 `restaurant → pub`; #163 Curious Cafe `pub → restaurant` (official sources describe it as a pan-European restaurant, not a café — my original flag suggesting "cafe" was wrong in direction though right that it was misclassified). **Reconciliation:** venue count **1,369 → 1,369**; exactly **14 field changes, all `type`, 0 outside the approved set**; **restaurant 558 → 546, pub 37 → 49 (+32%)**, cafe/cocktail/brewery unchanged, **F&D universe still exactly 841**; winery/outdoor/beach/golf untouched. **All 14 old URLs now 301 to their new canonical URL in one hop, all 14 new URLs 200, the sitemap carries 14/14 new URLs and 0/14 old ones, and the ambiguous golf pairs still return 200 on both forms.** Tests **352 pass / 1 fail of 353** (pre-existing golf-selector); `node --check` passed on both files; the repository's `okanagan.db` was not modified (md5 `347b535f842883d5d2d264d9ef585479`). **Deliberately NOT done:** the **7 secondary/multi-category memberships** (#151 Corduroy cafe+pub, #430 Naked, #576 Second Home, #25 Annie's, #982 Bufflehead cafe+restaurant, #690 Hatching Post, #989 BNA brewery+restaurant) and the multi-category plumbing. **The architecture audit for that is already done and its conclusion is: do NOT build a new system.** `collection_items (collection_id, content_type, content_id, note, position)` is already a generic many-to-many, `getOutdoorActivityVenues()` already de-duplicates with `GROUP BY v.id`, `POST /admin/collection-membership` is already the audited write path, and `dog_friendly` already proves a boolean column and a collection of the same name can coexist. The smallest version is 5 new `fd_*` collection kinds bootstrapped beside `ACTIVITY_COLLECTIONS`, with `venues.type` staying the canonical/primary category that drives URLs. **Three constraints for whoever does it:** new `fd_*` kinds MUST be added to `NON_DISCOVERY_COLLECTION_KINDS` or they leak into Build My Trip as discovery options; `/api/venues` currently exposes no collection data at all, so `rowToVenue` needs one added field before the client filter can see secondary categories; and `collection_items` has **no UNIQUE constraint and no index**. **Also open:** ~40 further name-flagged records were never verified beyond the 30, the directory UX audit's findings (6 controls per card including two Google-*search* links, a name that is not a link, no "View details" cue, 23 region chips + 12 attribute filters + 2 search inputs + a business-owner form all above the results) are recorded but unimplemented, and the 5 café + 2 brewery primary changes are still pending — all now safe thanks to the redirect.
* 2026-09-24 — **Food & Drink data classification is PRODUCTION-COMPLETE.** Second and final batch (data only; production via the live API; no code, homepage, schema or deployment change), following the multi-category plumbing shipped in **`61e6835`** and the category-redirect protection in **`b0c9b52`**. **Written this batch: 2 primary type changes + 8 secondary memberships across 7 venues.** Primary: **#690** The Hatching Post Brewery & Smokery and **#989** BNA Brewing, both `restaurant → brewery`. **Deliberately NOT changed: #151, #430, #25, #576 and #982 keep `restaurant` as their primary.** That reversed the original audit's recommendation, and the reason matters: those recommendations were made **before** multi-category existed, when the primary type had to carry a venue's whole identity. Once secondaries were available the rule became *change the primary only when it is categorically wrong, otherwise keep it and add the secondary* — the venue surfaces under both filters either way, and no live URL moves. New evidence supported it too: Frind's own booking platform lists **#25 as "Annie's Beach Restaurant"**, and **#430 Naked Cafe** serves dinner to 7pm with pizza and a full liquor licence alongside its coffee bar. A brewery filed as a restaurant, by contrast, is categorically wrong, which is why only those two moved. **The 8 memberships (batch `fd-categories-2026-09-24`):** #151 → `fd_cafes` + `fd_pubs`; #430, #25, #576, #982 → `fd_cafes`; #690, #989 → `fd_restaurants`. **Final effective categories — 7 venues now hold more than one:** **#151 Corduroy Cafe = restaurant + cafe + pub** (café downstairs, separate Baldy Bar upstairs), **#430 Naked Cafe = restaurant + cafe**, **#25 Annie's Beach Cafe = restaurant + cafe**, **#576 Second Home Cafe & Grill = restaurant + cafe**, **#982 Bufflehead Café = restaurant + cafe**, **#690 The Hatching Post = brewery + restaurant**, **#989 BNA Brewing = brewery + restaurant**. **Totals across both approved batches: 21 primary corrections** (14 in the first batch, 2 here, plus the 5 café primaries deliberately left alone) — more precisely, 16 primary type changes were written in total (14 + 2) and 5 candidate changes were consciously declined. **Final primary counts: restaurant 544, cafe 183, pub 49, cocktail 25, brewery 40.** **The unique Food & Drink universe remains exactly 841** — these were moves, not additions. **Effective counts now sum to 849** (restaurant 546, cafe 188, pub 50, cocktail 25, brewery 40), which is correct and expected: **F&D universe counts MUST use `COUNT(DISTINCT id)`, never a sum of per-category counts.** **Verification:** venue count **1,369 → 1,369** with an identical id set; exactly **2** type changes and **zero** non-type field changes across all 1,369 records; no duplicate memberships (0 venues with a repeated category, exactly 7 with more than one); multi-category filtering confirmed live with **0 duplicate rows** in any result — #151 is returned under `types=cafe`, `types=pub` and `types=restaurant` and appears **exactly once** when all three are selected. **URL/SEO:** both brewery URL moves 301 in **one hop** from their former `/restaurants/` URLs to `/breweries/`, the new URLs return 200, and the sitemap (2,246 URLs) carries both new canonical URLs and neither old one. The five preserved-primary venues kept their original URLs. The `b0c9b52` redirect implementation was not touched. Tests **354 pass / 1 fail of 355** (pre-existing golf-selector); `node --check` passed on `server.js` and `db.js`; **no homepage files changed**; the repository's `okanagan.db` was never opened — md5 `347b535f842883d5d2d264d9ef585479`, 0-byte diff. **Architectural notes to preserve:** `venues.type` remains the **canonical primary category** (it builds the URL via `CATEGORY_SLUGS`, breadcrumbs, schema.org type and sitemap entry) while the `fd_*` collections carry **only secondary categories**; the five kinds must stay in **`NON_DISCOVERY_COLLECTION_KINDS`** or they leak into Build My Trip discovery, because that list is queried live from `collections.kind`; **wineries remain excluded** from Food & Drink (Wine is its own section with its own `/wineries` hub); and **the root `app.js` is a stale, unserved duplicate — `/scripts/app.js` serves `public/scripts/app.js`**, which is the only client file that should ever be edited. **Still open:** the Food & Drink **directory UX redesign** (audited but unimplemented: 6 controls per card including two Google-*search* links, a venue name that is not a link, no "View details →" cue, 23 region chips + 12 attribute filters + 2 search inputs + a business-owner form all above the results), the stale hard-coded `"1,069 places to explore"` in `okanagan.html:368`, the interval-poll count-rounding hack at `server.js:9770`, and roughly 10 further name-flagged records from the original 40 that were never verified.
* 2026-09-24 — Claude ran a **missing-venue gap search and closed the brewery gap** (data only; production via the live API; no code, homepage, schema or deployment change). **Method:** the valley-wide inventory was rebuilt region × type, then the two categories with authoritative external directories were audited first — **breweries against BC Ale Trail** and **golf against GolfBC / Tourism Kelowna**. **Golf has no gaps**: every course those sources name was already among our 49. Breweries had a real gap. **A mandatory duplicate check against all 1,369 records cut the candidate list from 11 to 9** — **#1077 Ellison Field Taphouse** and **#53 Barley Mill Brew Pub** were already in the database. That check is not optional: near-misses included #545 Red Bird *Roasting* (a coffee roaster on the same street as Red Bird Brewing), #980 Copper *Mug Pub* in Penticton, three venues sharing Tin Whistle's multi-tenant plaza at 1475 Fairview Rd, and **#330 Kettle River Brewing at 731 Baillie Ave — literally next door to Jackknife at 727**. All distinct; none duplicates. **Written: 9 new brewery records** — **#1411** Rustic Reel Brewing Co, **#1412** Red Bird Brewing, **#1413** Copper Brewing Co., **#1414** Grey Fox Brewing, **#1415** Jackknife Brewing, **#1416** The Office Brewery, **#1417** Freddy's Brewpub (all Kelowna), **#1418** Tin Whistle Brewing and **#1419** La Petite Abeille Cider (Penticton) — each with a verified street address, and a website where one could be confirmed (7 of 9; Jackknife and The Office have no verified domain). Cideries are filed as `brewery`, matching the existing convention. **Plus 1 reclassification:** **#53 Barley Mill `pub → brewery` + `fd_pubs` secondary** — it has brewed on site since 1997 and also trades as a sports pub, so it is `["brewery","pub"]`. **#1417 Freddy's Brewpub** likewise received `fd_pubs`. **Two candidates were deliberately NOT added:** **#1077 Ellison Field Taphouse does NOT brew** — it pours other breweries' beer (Yellow Dog, BNA, Cannery, Tin Whistle, Copper, Abandoned Rail, Kelowna Beer Institute) at Kelowna airport pre-security, and is already correctly typed `pub`; it surfaced only because BC Ale Trail lists taphouses beside breweries. **#391 Maple Leaf Spirits is NOT a region discrepancy** — I had flagged it as possibly belonging to Penticton, and that was wrong: **19 of 51 Naramata-region venues carry a Penticton postal address**, including #823 Chain Reaction Winery on the same road and postal block (980 vs 948 Naramata Rd, V2A 8V1). BC Ale Trail simply uses the postal city; our `region=naramata` follows our own established convention. No change needed. **THE BADGE DECISION — important:** badge research was completed for 5 of the 9 (Rustic Reel `dog_friendly/patio/kid_friendly/great_groups`; Red Bird `dog_friendly/patio/kid_friendly/live_music`; Copper `dog_friendly/patio/kid_friendly`; Grey Fox `gluten_free` — it is a dedicated gluten-free brewery; Freddy's `kid_friendly/great_groups`) but **deliberately NOT written**. The homepage embeds **live badge counts** in its hidden `og-guides` SEO block (Kelowna "Patio (135)", "Kid-Friendly (97)", "Gluten-Free-Friendly (150)", …), so writing those values would move the frozen homepage's bytes. **All 12 `BOOL_FIELDS` on all 9 new venues are 0, and the homepage was verified byte-identical before and after the batch (md5 `735949b913fa03f210673785ae3bac29`).** The researched values are recorded here and can be applied whenever the owner accepts that hidden-block count shift. **Verification:** venues **1,369 → 1,378** (+9); **brewery 40 → 50** (9 new + #53 moving in — note 49 would be the figure if #53's move were not counted), **pub 49 → 48**, restaurant 544, cafe 183, cocktail 25 all unchanged; zero badge fields set on the new records; **only #53's type changed among existing venues, with zero other field changes across all 1,369**; no duplicate brewery names and no duplicate region+slug. All 9 new venue pages return 200; **#53's old `/penticton/pubs/…` URL 301s in one hop** to `/penticton/breweries/…` via the `b0c9b52` redirect; sitemap **2,246 → 2,255** carrying all 9 new URLs plus #53's new one and none of its old one; multi-category filtering returns #53 and #1417 exactly once under both `brewery` and `pub`. The repository's `okanagan.db` was never opened — md5 `347b535f842883d5d2d264d9ef585479`, 0-byte diff. **Still open from the gap search:** the search was NOT exhaustive across all 20 regions and 9 types — only breweries and golf were audited against authoritative directories. The thinnest remaining signals are **cocktail lounges (25 valley-wide, 18 of them in Kelowna; West Kelowna has 0)**, **Peachland (1 café, 0 pubs)**, and **Coldstream (16 venues beside Vernon's 160)**.
* 2026-09-24 — Claude **simplified the `/whats-on` filter surface and shipped it** — **commit `d8f5ea6962e2c70cfeafa277d907d3f713fd11db`** "Simplify What's On filters and layout" (Railway deployment **`68d05da6-f29c-444f-b897-1542a1e677e7`, status SUCCESS**; `server.js` and `tests/server.test.js` only). **The problem:** the page opened with roughly 23KB of filter markup before the first event — twelve 1376×768 category image tiles, four region accordions, four step headings and a "Show N results" CTA — so a phone visitor had to scroll past the entire filter form to reach a single event. The brief was to let people browse immediately and expose advanced filtering only when they need it. **What the page is now:** a compact search field, one horizontal category chip row, a Regions popover and a Date popover, then the live result count with the removable active-filter chips and Clear all directly beneath it, then the results. Four new builders — `whatsOnSearchHtml()`, `whatsOnCategoryChipsHtml()`, `whatsOnFilterBarHtml()` and `whatsOnResultBarHtml()` — plus a `compact` mode on the existing `whatsOnDateStepHtml()` for the Date popover. **The filter contract did not move.** Every control still exposes `data-region` / `data-category` + `aria-pressed`, so the existing client script and the shared `OUTDOOR_FILTER_CLIENT_PREDICATE_SRC` keep working untouched. **Region grouping is still derived from `FOOTER_REGION_GROUPS`** — the accordion groups moved into the Regions popover with their markup intact, they were not re-authored or hardcoded. **Date presets are still server-side round trips** and **preserve existing region selections** (verified live: "This Weekend" from `?regions=kelowna,penticton` → `?when=this-weekend&regions=kelowna,penticton`). **Clear all intentionally continues to clear only the client-side region/category/search filters and to preserve the `?when=` date window** — that is HEAD's behaviour (`clearAll` still filters out `data-when` chips), it was explicitly kept, and it should not be "fixed" without a decision. **Removed:** the twelve category image tiles (0 `<img>` above the results now; the artwork itself is untouched and still served from `/images/whats-on/`) and the four step headings — the only heading above the results is a 1×1 `visually-hidden` "Results" label for screen readers. **New on this page:** client-side search over the already-rendered cards, built from data already in the markup (name, region label, category labels, meta and description), and an "All" chip that is a category *reset* control (`data-category-all`), not a thirteenth category. **Two real defects were found by the visual pass and fixed before the commit:** `type="search"` made Chrome draw its own clear glyph on top of the page's own, giving two overlapping ×'s (suppressed with `::-webkit-search-cancel-button`); and making `.whatson-pop` `position: static` at ≤640px to get a full-width sheet also removed the panel's positioning context, so `top: calc(100% + 6px)` resolved against `<body>` and the Regions panel opened **368px below its button**, floating over the results — fixed by making `.whatson-controls` the positioning context instead (re-measured: 6px below the button). Also removed dead wiring: `getElementById('whatsOnCategoryStatus')` was always `null` once the category step became a chip row. **Measured (same twelve seeded events, before vs after):** body bytes before the first event card **27,457 → 16,773**; category tile images **12 → 0**; visible headings above the results **4 → 0**. **`/outdoors` verified unchanged**, including its original activity tile images and three-step layout: with identical seeded data, `/outdoors`, `/outdoors?regions=…&activities=…`, `/outdoors/hiking`, `/outdoors/viewpoints` and `/kelowna/outdoors` all render **byte-identical** to `HEAD~1`, and live it still shows 20 region chips, 9 activity tiles with images, the three step headings, "1 selected" statuses, live chip counts and a working Clear all, with `body.className` = `golf-page outdoor-page` and **zero** `whatson`-prefixed classes. **Homepage `/` verified unchanged** — byte-identical locally, and live it still carries the hidden `og-guides` SEO badge block with **zero** occurrences of `whatson` in the HTML; `okanagan.html` and `public/` were untouched (last changed in `61e6835`). **Live verification after deploy (382 events).** Desktop: search (`wine` → 28, `market` → 9, `kelowna` → 61, nonsense → the no-results state), category chips, Regions popover open/close, multi-select (`?regions=kelowna,penticton`), the "Regions · 2" badge, the Date popover, preset round trips, counts (382 → "198 of 382" → "51 of 116" → "14 of 116"), the active-filter chip rows and Clear all all behave correctly. Mobile (386px CSS viewport): **no horizontal page overflow** (`scrollWidth === innerWidth === 386`, zero elements wider than the viewport outside the chip row), the category chip row scrolls horizontally as intended (`scrollWidth 2323 > clientWidth 354`), the popover opens 6px below its button at full content width, and the first event card sits at y≈465 in a 780px viewport. **No site-origin console errors** — the only exceptions on the page come from a Chrome extension's `content.js`. **Railway is running `d8f5ea6`**; the previous deployment (`8100af6`) is `REMOVED`. **Tests: 356 pass / 1 fail of 357.** Nine test assertions describing the old structure were updated (the date "1 selected" step status → the Date popover badge, the `clearAll` and chip-collection needles, the ordering markers, and the category step status → the "All" chip releasing). The sole failure is the pre-existing golf-selector assertion, confirmed by running the suite at `HEAD` with `server.js` untouched (354 pass / 1 fail of 355) — deliberately not fixed. **Note on the ordering assertions:** `s6markup` strips `<script>` but **not** `<style>`, so bare class-name markers like `whatson-cat-row` matched inside the inline stylesheet in `<head>` and broke the order checks; the assertions now use `class="whatson-cat-row"`. **Environment note:** the browser tool's window resize does not take effect here (the window resizes but the content viewport stays at desktop width), so mobile was verified by rendering the page in a same-origin 390px iframe — a real 386px CSS viewport with real media-query evaluation, not a simulation. `okanagan.db` unchanged (md5 `347b535f842883d5d2d264d9ef585479`) and absent from the commit.
* 2026-09-24 — Claude **simplified the `/outdoors` explorer and shipped it** — **commit `0489492f7c9cd9959e2f387afd84dc76171ccb54`** "Simplify Outdoors filters and layout" (Railway deployment **`deccf6ee-2ec3-4393-ad48-c9f3366df532`, status SUCCESS**; `server.js` and `tests/server.test.js` only). **The problem:** the landing opened with ~28KB of filter markup before the first destination — nine 1376×768 activity image tiles, a region accordion, three step headings ("Choose Region(s)" / "Choose Activity(s)" / "Results") and a "Show N results" CTA — putting the first card **2.1 screens** down at 386px. **What the page is now:** breadcrumb → H1 → a one-sentence intro → a compact search field → a horizontal activity chip row (`All` + the live activities) → a compact **Activity guides** row → a single Regions popover → a compact result bar → the cards. Five new builders (`outdoorsSearchHtml`, `outdoorsActivityChipsHtml`, `outdoorsActivityGuidesHtml`, `outdoorsFilterBarHtml`, `outdoorsResultBarHtml`) plus `renderOutdoorsSimplifiedStyles()`. **Outdoors deliberately has NO date filter** — destinations are not time-sensitive, so there is no Date control, no preset, no `?when=` and no date state anywhere; both new tests assert this across `data-when`, `when=`, `whatsOnDate`, `Choose Dates`, `whatson-date` and `dateWindow`, and that `parseOutdoorFilterQuery` still returns only `{regions, activities}` when handed `when=today&from=…`. **The filter contract did not move.** Activity controls still carry **`data-activity`** — *not* `data-category`, which is What's On's contract; `data-activity` is what `?activities=`, `parseOutdoorFilterQuery`, `selected('activity')` and `data-remove-activity` all key off, so switching would have meant rewriting the filtering logic. Region chips still carry `data-region` + `aria-pressed`, region grouping is still derived from `FOOTER_REGION_GROUPS` (the accordion moved into the popover with its markup intact), and **`OUTDOOR_FILTER_CLIENT_PREDICATE_SRC` is untouched** — it is shared with What's On and must stay one implementation. Multi-select, contextual chip counts, the URL query, pushState/popstate, `#outdoorRegionStatus` and `#outdoorActivityStatus` (both now `visually-hidden` live regions, still updated by the same `updateStepStatus()` calls) all behave as before. **New:** client-side search over the already-rendered cards, built from data already in the markup (name, region label, activity labels, `.venue-meta`, `.golf-desc`). It adds **no query parameter**, so every existing URL still means what it meant before, and it ANDs with the shared predicate. `outdoorSummaryText` and `OUTDOOR_SUMMARY_CLIENT_SRC` gained an **optional 5th `searchActive` flag** so a search-only state reads "N of T" instead of claiming the full total; both twins changed identically and every existing 4-argument call produces the same string. `All` is a reset control (`data-activity-all`), not a tenth activity — it is pressed exactly when no activity is chosen, and it is filtered out of the `chips` array so it is never a filter. **No image asset was deleted.** `renderOutdoorActivityShowcaseHtml`, `outdoorActivityCardHtml` and `outdoorActivityImagePath` are **kept, exported and still tested**; all nine WebPs still resolve and are still served from `/images/outdoors/` (verified 200 live for all nine). Only the tiles' *placement* on the landing was removed. The nine activity landing pages keep their **only inbound internal link** through the compact Activity guides row, live-gated exactly as the tiles' Guide links were (an activity below `MIN_ACTIVITY_VENUES` has no page, so it gets no link rather than a 404). **Scoping:** the landing's stylesheet is its own function rather than part of `renderOutdoorThemeStyles()`, which ships on `/whats-on` and every outdoor page — so none of these rules reach them (asserted by two tests). The shared `.outdoor-filter-chip` rules are deliberately NOT modified, because What's On's chips use them. **Two defects found by visual QA and fixed before committing:** the Regions panel needed a desktop `min-width: 520px` (at the default 280px the 20 canonical chips stacked one per row), and `.outdoors-act-row` needed an explicit **`flex-wrap: nowrap`** because the row also carries `.category-region-selector`, whose `flex-wrap: wrap` made the chips stack into a tall block on a phone instead of scrolling sideways. **Measurements — note the two datasets, do not mix them.** Like-for-like local, identical seeded data: body bytes before the first card **21,155 → 13,805**, tile images **9 → 0**, visible step headings **3 → 0**. Live-to-live (205 destinations): **28,084 → 19,824** bytes. Same-viewport geometry: at **386px the first card moved 1661px → 582px**, and 320px gives 641px; desktop was 1366px at a 1600px-wide viewport before and 525px at 1440px after (different widths, so not a clean pair — the clean desktop figure is **483px at 1200px** from the local run). The mobile chip row scrolls (`scrollWidth 1406 > clientWidth 350`) with **zero page-level horizontal overflow** at 320/386/1440px and zero elements wider than the viewport outside that row. **Byte-identical to before, with identical data:** `/`, `/okanagan.html`, `/whats-on` (bare and `?when=this-week&regions=kelowna`), `/golf`, `/beaches`, `/wineries`, `/trip`, `/browse`, `/kelowna/restaurants`, `/sitemap.xml`, `/outdoors/hiking`, `/outdoors/viewpoints`, `/kelowna/outdoors` and `/vernon/outdoors` — **`/outdoors` is the only page that changed**. **Live verification after deploy (205 destinations).** Desktop: 9 chips with real counts (Hiking 57, Viewpoints 43, Nature 51, Cycling 33, Winter 51, Adventure 19, Fishing 12, Camping 50, Water 13), 9 guide links, Regions popover opening 6px below its button at 520px wide with all 20 canonical chips flat and the group headers hidden (existing desktop behaviour preserved), multi-select → `?regions=kelowna,vernon` "62 of 205", + hiking "22 of 205", + viewpoints "24 of 205", **+ search "canyon" → "4 of 205"**, and Clear all returning to bare `/outdoors` with search emptied, `All` re-pressed and both statuses hidden. What's On re-verified live and unchanged (13 category chips, Date popover with all four presets, `?when=this-weekend` round trip, zero Outdoors markup or CSS). Homepage unchanged (92,299 bytes, hidden `og-guides` block present, zero `outdoors-*` occurrences). All of `/`, `/whats-on`, `/outdoors`, `/outdoors/{hiking,water,camping}`, `/kelowna/outdoors`, `/vernon/outdoors`, `/golf`, `/beaches`, `/wineries`, `/trip`, `/browse`, `/sitemap.xml` return 200. **No site-origin console errors** — the only exceptions come from a Chrome extension's `content.js`. **Tests: 358 pass / 1 fail of 359** (baseline at `df79531` was 356/357). Precise accounting: **3 new tests, 1 renamed and rewritten, 11 existing tests with updated assertions, none of the coverage lost.** The renamed one was `What's On simplification does not disturb the Outdoors explorer`, which asserted that Outdoors *keeps* its step headings and image tiles — this change inverts that, so it became `What's On and Outdoors keep separate simplified surfaces`, now asserting leakage in **both** directions, the single shared predicate, and that Outdoors carries no date artefact. The sole failure is the pre-existing golf-selector assertion, reproduced at `HEAD` and deliberately not fixed. **Two deliberate deviations from the brief, flagged at the time and still open for a one-line change:** the H1 stayed **"Outdoors in the Okanagan"** rather than "Outdoor Adventures" (it also feeds `<title>` and the meta description on an indexed page, so renaming it is an SEO change, not a layout simplification), and the count kept the established wording **"N outdoor destinations"** rather than "outdoor adventures" (the brief permitted the existing wording, and it is shared verbatim with the client twin). **Environment note (unchanged from the What's On work):** the browser tool's window resize does not take effect here, so both mobile and wide-desktop rendering were verified by loading the page in same-origin iframes at 320 / 386 / 1200px — real CSS viewports with real media-query evaluation, not simulations. `okanagan.db` unchanged (md5 `347b535f842883d5d2d264d9ef585479`) and absent from the commit.
* 2026-09-24 — Claude shipped **two follow-up changes to the `/outdoors` landing** — **commit `3d049670b77f467ae7f44cbeadc366add60752f4`** "Update Outdoors heading and remove Activity Guides" (Railway deployment **`72a8f7df-9d80-4574-aab6-77ca4cfa8421`, status SUCCESS**; `server.js` and `tests/server.test.js` only), closing both items left open by choice in `0489492`. **(1) The heading rename.** `heading` on the landing is now **"Outdoor Adventures"** instead of the "`<Plural>` in the Okanagan" pattern the other hubs use. Because `heading` also builds `title`, the rename was taken **deliberately consistent across all five surfaces** (owner chose this over an H1-only change): `<h1>Outdoor Adventures</h1>`, `<title>Outdoor Adventures | Okanagan Roam</title>`, `og:title`, `twitter:title` and the **ItemList schema `name`**. **Unchanged:** the breadcrumb still reads "Outdoor Destinations" (it comes from `CATEGORY_LABELS`, not from `heading`), the canonical stays `https://okanaganroam.com/outdoors`, and the meta description is untouched. No other category moved — `/golf` still renders "Golf Courses in the Okanagan" and `/beaches` "Beaches in the Okanagan". A new assertion pins the ItemList `name`, which was previously unasserted, so all five surfaces now stay in lockstep. **SEO note recorded at the time:** `/outdoors` is an indexed page and the new `<title>` drops the "Okanagan" geographic keyword; the owner accepted this in favour of full consistency. **(2) The Activity guides row was removed entirely** — the "Activity guides" label, all nine `/outdoors/<activity>` links, `outdoorsActivityGuidesHtml()` and its export, and exactly the **7 CSS rules that existed solely for that row** (`.outdoors-guides`, `-label`, `-links`, `.outdoors-guide-link` plus its `:hover` and `:focus-visible`). Nothing else was touched: the activity chips and the `All` reset control, the Regions popover and its 20 canonical `FOOTER_REGION_GROUPS` chips, search, the result bar and counts, the removable filter tags and Clear all, `data-activity` / `data-region` / `aria-pressed`, the URL query and pushState/popstate, `#outdoorRegionStatus` and `#outdoorActivityStatus`, and the shared `OUTDOOR_FILTER_CLIENT_PREDICATE_SRC` all behave exactly as before. **Consequence recorded deliberately:** the landing now carries **no links to the nine `/outdoors/<activity>` pages** — it was their only inbound internal link outside the sitemap. Those pages are **untouched and still render** (`/outdoors/{hiking,cycling,winter,water,camping}` all 200, each with its own H1, the `renderOutdoorActivitySelector()` nav and the "All Outdoors" back-link), and **all nine remain in the sitemap**, so nothing is orphaned from crawlers — but that discovery path from the hub is gone. Restoring it in some other form is a separate decision. **Diff discipline:** with identical seeded data `/outdoors` differed from `0489492` by **exactly 22 lines** — the 5 renamed title surfaces, the 7 removed CSS rules (plus the blank line they left) and the 4 removed markup lines — while `/`, `/whats-on`, `/golf`, `/beaches`, `/outdoors/hiking`, `/outdoors/cycling`, `/kelowna/outdoors`, `/vernon/outdoors`, `/wineries`, `/trip`, `/browse`, `/kelowna/restaurants` and `/sitemap.xml` were all **byte-identical**. No unrelated change was made. **Live verification after deploy (205 destinations).** All five heading surfaces confirmed live; `outdoors-guides` / `outdoors-guide-link` / "Activity guides" all absent, and the landing's list of `href="/outdoors/<activity>"` links is **empty**. Desktop (1440px): first card at **y=490**, no overflow, `?regions=kelowna,vernon` → "62 of 205" with the "· 2" badge, + hiking → "22 of 205" with `All` released and the status reading "1 selected", + search "canyon" → "4 of 205", and Clear all returning to bare `/outdoors` at "205 outdoor destinations" with the search box emptied and `All` re-pressed. Mobile (382px): no page overflow, the chip row still scrolls (`scrollWidth 1406 > clientWidth 350`), the popover opens 6px below its button at full content width with no overflow, first card at **y=465**, and zero elements wider than the viewport outside the chip row. Homepage unchanged (92,299 bytes, hidden `og-guides` block present, zero `outdoors-*` occurrences); What's On unchanged (810,683 bytes, 14 category chips, Date control present, zero Outdoors leakage); `/golf` and `/beaches` keep their own headings. **No site-origin console errors.** **Tests: 358 pass / 1 fail of 359**, unchanged — the assertions naming the old heading or the guides row were updated, and the replacements assert that the landing links to no `/outdoors/<activity>` page *and* that those pages still render. The sole failure remains the pre-existing golf-selector assertion. `okanagan.db` unchanged (md5 `347b535f842883d5d2d264d9ef585479`) and absent from the commit.

## Open Tasks

* **Pinterest — batch #39–52 published; remaining follow-ups (2026-09-23):**
  * **Batch complete.** All 14 Pins (#39–52) are **PUBLISHED and verified live**; per-Pin live URLs are in `docs/PINTEREST_CONTENT_PLAN.md`. Nothing from this batch is awaiting publication. Images stay in `pinterest-assets/` (untracked by design).
  * **Cosmetic, still open (owner decision):** #44's 🌅 renders as a bordered tile rather than blending into the type; #51's 🥾 sits at the start of line 2 instead of the end of line 1. Wording and emoji are exact in both — only placement/rendering differs. Neither was reworked, and both are now live as-is.
  * **Duplicate risk — RESOLVED.** The live-account audit harvested all 37 historical Pins, so the batch is no longer unverified against Pins 1–37. Two true duplicates were found and replaced before publication (#45 → Kelowna Golf, #47 → Okanagan Live Music); #49 was cleared.
  * **Observed, not acted on:** the account carries **two live Pins with the identical title** “Fall in Vernon, BC 🍎🍂 | Okanagan Weekend Guide” — `1124422232001497647` (the one recorded as #38) and `1124422232001496909` (unrecorded). This predates the batch; worth checking whether #38 was published twice.
  * **Next batch:** #53 onward is undrafted. Run the `weekly-pinterest` skill, which defaults to PREVIEW.

* **District Wine Village — remaining venues on HOLD (2026-09-23):**
  * **SPACEDOG Wines** — the winery exists (winemaking team Pascal Madevon with co-founder Todd Heintz) but its District Wine Village page lists hours as **"Coming Soon"** and it is **not yet operating** at the Village. Nothing for a visitor to do there yet; revisit when DWV posts hours.
  * **Workshop Spirits / Yellow Dog Spirits** — surfaced as a separate potential gap during the audit. Castanet names *Workshop Spirits* as a sibling brand already at the Village, while DWV's own distillery page calls it *Yellow Dog Spirits*; the two names may describe the same operation. **Not researched — requires its own verification pass.** Note **#798 Yellow Dog Brewing Co (penticton) is a different business** and must not be merged with it.

* **SEO backlog / status (2026-09-22):**
  * **SEO Phase 1 — sitemap discovery: COMPLETE** (`4776d5e`, Railway `daab6e06`; sitemap 2,244 → 2,248).
  * **H1 Step 1 — event → related events: COMPLETE** (`fc6f57c`, Railway `91135192`; ~2,500 contextual links across 638 of 639 event pages).
  * **H1 Step 2 — region-page event blocks: COMPLETE** (`50eaada`, Railway `da8263d2`; 59 new internal links — 44 event links + 15 secondary links — across 15 of 20 region pages). 14 regions carry 3 links, coldstream 2; apex, baldy, enderby, kaleden and lumby fall below the `MIN_REGION_EVENTS = 2` threshold and render no block (their pages are byte-identical to before). The earlier audit's "16 regions qualify" figure counted baldy on the strength of a valley-wide event; the region-scoped predicate excludes those.
  * **H1 Step 3 — venue-page event blocks: COMPLETE** (`e5cb16f`, Railway `ce5c12ff`; 18 direct event links across 7 venue pages, max 3 each, `venue_id`-only). Enabled by the `whatson-2026-09-venue-link-1` data write, which linked 44 events to 13 existing venues (23 -> 67 venue-linked events). **H1 is now complete across all three steps.** Remaining upside is data, not rendering: 572 events still carry only `venue_name_text`, and the largest of those venues (Creekside Theatre 34, Kelowna Community Theatre 28, Vernon & District PAC 38 under two spellings, Venables Theatre 15, Prospera Place, SOEC, libraries, arenas) are **not venue types in the directory at all**, so they cannot be linked without a taxonomy decision.
  * **Not recommended:** activity-page → event links — no data relationship exists between events and outdoor activities, only categories.
  * **Next measurement checkpoint:** re-check Search Console once Google re-reads `/sitemap.xml` (last read Sep 21 at 1,515 URLs) — watch discovered pages approach 2,248 and whether the 639 event URLs leave "Discovered – currently not indexed". Consider requesting access to the `sc-domain:` property and exporting the full Performance CSV (the UI table caps at 1,000 rows).

* **What's On — owner decisions pending (2026-09-22; read-only research done, bodies rehearsed and gate-clean in `whatson-research/import/hold_audit_bodies.json`):** (1) Pine and Peak Artisan Market, Baldy, Oct 3 — same source URL and date as live #73 Larix Trail Race, so the frozen same-source-date gate rejects it unless the Baldy poster image URL is accepted as `source_url`; (2) Special Olympics Canada Winter Games Kelowna, Feb 28 – Mar 4 2028 — dates confirmed on the Tourism Kelowna major-events page; decide whether far-future events are imported now; (3) Harness Racing at Desert Park Raceway, 22 race days Sep 27 – Dec 14 2026 (Sun/Mon 2 pm, organiser-confirmed) — one series card like the hockey series, or skip; (4–5) Feminist Horror Festival 2026 — Thursday film nights (5 dates, UBCO UNC 106) and Wednesday public talks (4 dates, Okanagan Regional Library) are not live while the Oct 23 SKYNSUIT talk already is (#484): choose two series cards beside #484, one festival card, or skip.
* **What's On — evidence needed (recheck later):** New York New Year 2026 (Festivals Kelowna, Stuart Park, fireworks 9 pm — no dated 2026 listing yet; recheck in November), National Indigenous Peoples Day 2027, Canada Day Kelowna 2027, Peachland Canada Day 2027, Osoyoos Cherry Fiesta 2027 (2027 editions unannounced), Indigenous Veterans Day / Remembrance Day ceremonies (Nov 8 / Nov 11; no 2026 ceremony times or venues published).
* **What's On — needs a code change before it can be done (not approved yet):** the owner asked for the label on #387's Oct 11 occurrence (id 1033) to become exactly "Honey's Birthday Brunch"; the occurrence API has no label/time edit (`POST …/occurrences` skips existing date+time keys, `PATCH …/occurrences/:occId` accepts `status` only). Proposed: extend the PATCH allow-list to `label` (and optionally `end_time`/`source_ref`) for scheduled occurrences, logged through `event_enrichment_log`, with tests; then a bearer-guarded edit.
* **What's On — separate homepage approval:** the homepage mood card "What's On" still anchors to `#happeningSoon`; retargeting it to `/whats-on` is a homepage change and has not been approved.
* **What's On — content still outstanding:** (category tile art done in `1ba5e38`); the Summerland Jets (BCHC team 21) now have an official feed and could be added as a home-game series; recurring DMO rows classified as venue programmes (trivia, karaoke, open mics, weekly patio music, happy hours) were excluded by rule and are listed in `review.csv` (`holdout` bucket).
* **What's On — operating notes:** production events expire automatically when their last local end date passes (page stays 200 + noindex, drops from listings and the sitemap); nothing removes rows. Cancel or postpone a date with `PATCH /api/events/:id/occurrences/:occId {status}` (the last scheduled date of an event needs `event_status`), add dates with `POST /api/events/:id/occurrences`; there is no in-place occurrence edit — move a sitting by upserting the new date/time and cancelling the old one (as done for #387).

* **Task: Location enrichment has reached its practical stopping point under current free-tool methodology (updated 2026-09-18; do not re-research the IDs listed below without new evidence or new tooling).**

* **Current state:** 1,042 active venues; 1,004 complete (address+lat+lng); 38 missing. Of those 38: 19 are confirmed MOBILE/ROAMING and should NOT be enriched with a fixed address:
  `94, 130, 131, 156, 158, 196, 208, 304, 379, 405, 453, 507, 531, 569, 592, 642, 644, 652, 809`
  The remaining 19 are UNRESOLVED (not mobile, not safely write-ready):
  `185, 225, 260, 311, 337, 450, 459, 511, 547, 581, 650, 691, 702, 766, 806, 1019, 1051, 1066, 1069`
  — of these, 9 have a confirmed fixed address but no independently-verified building-level coordinate (185, 225, 260, 450, 511, 547, 581, 1019, 1051); 4 have genuine conflicting/branch-ambiguous evidence (311, 702, 766, 806); 5 have insufficient evidence to establish current status at all (459, 650, 691, 1066, 1069); 1 (337) is unresolved specifically due to multi-entity/multi-domain identity confusion. Current WRITE-READY count is **0**.
* **Why this is not the same as "permanently unenrichable":** the 19 unresolved venues were only checked against free web search and OpenStreetMap/Overpass — several (especially the 9 "address confirmed, no coordinate" cases) could plausibly be resolved with a paid geocoding API, a direct phone call to the business, or a manual map lookup, none of which were available this session. Re-attempting the exact same free-tool research on these 38 IDs is unlikely to produce new results and should be avoided unless a new source/tool becomes available.
* **The 66-venue task previously listed here (as of 2026-09-15) is now stale and superseded by the above** — that count did not reflect the 5 successful writes or the mobile/unresolved triage completed 2026-09-18.
* **Suggested next phase (only if new tooling becomes available):** revisit the 9 "address confirmed, no coordinate" IDs first with a real geocoding API; revisit the 4 conflicting/branch-ambiguous IDs by contacting the business directly to confirm which branch a record represents; leave the 19 confirmed-mobile IDs alone entirely.

## In-Progress Work (not yet committed)

### Claude — guarded `/admin/correct-phone` endpoint (2026-09-15)

* **Status: local-only, uncommitted. Both previously-open items below are now resolved — 58/58 tests passing, stable across repeated runs. Ready for ChatGPT/human review before commit.**
* **Why:** Phone reconciliation on the venue-duplicate/identity audit (IDs 328, 185, and others) found there was no guarded, audited, precondition-checked way to correct an already-populated phone number — only an unauthenticated `PUT /api/venues/:id` could do it. This work adds that missing guarded path.
* **What changed (working tree, `git status --short` shows `M server.js`, `M tests/server.test.js`, plus an unrelated `M okanagan.db` local test artifact — see note below):**
  * `server.js`: new `guardedPhoneCorrectUpdate()` function (mirrors the existing `guardedRegionCorrectUpdate()` pattern; atomic UPDATE with NULL-safe `expected_current_phone` precondition; writes one `venue_enrichment_log` audit row per real change) and a new `POST /admin/correct-phone` route (same bearer-token auth as every other admin route; allowlists exactly `id`, `expected_current_phone`, `corrected_phone`, `reason`, `batch_id`). Zero existing lines modified — confirmed via `git diff`, both hunks are pure insertions.
  * `server.js`: one follow-up fix — `corrected_phone` is now trimmed once and the TRIMMED value is what gets stored (previously validated-as-non-empty-after-trim but stored untrimmed, so padded input could leave stray whitespace in the DB).
  * `tests/server.test.js`: new fixtures (`test-phone-fixture`, `test-null-phone-fixture`, `test-populated-phone-for-null-check`) and test coverage folded into the existing single `'HTTP routes: ...'` test (a second `startServer()`/`server.close()` cycle in this file is documented to break later fetches, so all admin-endpoint coverage lives inside that one test). Covers: 401 wrong token, 400 validation (unexpected key, missing/empty required fields), 404 unknown id, 409 mismatch on a populated field (now also asserts zero new audit-log rows), 409 mismatch on a NULL live phone, **409 the reverse case — `expected_current_phone: null` asserted against a POPULATED live phone** (just added), 200 NULL-precondition match + audit row, 200 success path + full audit-row field verification, a sequential stale-precondition case (simulates the "process acted on a stale read" scenario this atomicity guards against), and a no-op case.
* **Test result: 58/58 passing** (`npm test`), re-run 4 times in a row with no flakiness. No regressions in any pre-existing test.
* **Both previously-open items are now done:**
  1. **`corrected_phone` trimming is now tested.** New assertion sends `"  +1 250-555-8765  "` (padded) and confirms both the HTTP response and the stored DB value are the trimmed `"+1 250-555-8765"`, with no stray whitespace.
  2. **503 (unauthenticated-endpoint-not-configured) is now tested, via a genuinely isolated child process.** Isolation mechanics (documented in-line in `tests/server.test.js` above the test itself): `server.js` and `db.js` are copied byte-for-byte (unmodified — `fs.copyFileSync`, no rewriting) into a fresh `os.tmpdir()` subdirectory; that copy is launched as a separate `node <copy>/server.js` child process with (a) its own `process.env` — built from a shallow copy of the parent's env with `ENRICHMENT_ADMIN_TOKEN` explicitly `delete`d, so it's unset regardless of what the parent's real environment contains, (b) its own port (3098, distinct from the shared harness's 3001), and (c) its own SQLite file, since `db.js` resolves `DB_PATH` relative to its own `__dirname` — now the temp directory, not the project root, so it creates a brand-new empty DB there rather than touching the project's `okanagan.db` or the shared test run's copy. The test polls `/robots.txt` (a harmless public route) until the child is actually listening, then asserts `POST /admin/correct-phone` returns 503. Both the temp directory and the child process are torn down in a `finally` block (confirmed manually afterward: no leftover temp directories, no orphaned process). Runs in ~250ms.
* **No production data was changed.** All testing ran against the local, throwaway `okanagan.db` fixture file that the test suite deletes and reseeds on every run (per that file's own header comment) — never against Railway's `/data` volume. No request was made to `okanaganroam.com` during this work. `ENRICHMENT_ADMIN_TOKEN` was never requested or used — only a hardcoded fixture string (`'test-fixture-admin-token'`) local to the test process.
* **No commit, push, or deployment was performed.** Everything above is an uncommitted working-tree change only; `git status` confirms nothing is staged.
* **For ChatGPT to review before this is committed:**
  * Worth a second opinion on whether folding most `/admin/correct-phone` coverage into the single existing HTTP-routes test (rather than a separate test file) is the right tradeoff — it was done specifically to avoid a documented `startServer()`/`server.close()` double-cycle failure in this file, not for lack of a cleaner option. (The 503 test is its own separate top-level test, since it never touches the shared `app`/server instance at all.)
  * Worth confirming the child-process isolation approach for the 503 test (spawn + temp-dir file copy + separate port) is an acceptable pattern for this codebase's test conventions, versus e.g. refactoring `server.js` to make `ENRICHMENT_ADMIN_TOKEN` re-readable per-call instead of captured once at module load (a larger, out-of-scope change not attempted here).
  * `okanagan.db` shows as modified in `git status` purely as a side effect of running `npm test` locally (it's a disposable fixture DB per the test file's own design) — not a real change, safe to ignore/not stage.
  * This is still an uncommitted, local-only diff. Nothing has been staged or committed pending this review.

### Claude — Milestone 1 (scenic hero + "What are you in the mood for?") final rendered QA (2026-09-16)

* **Status: QA evidence-gathering pass only. Nothing fixed. Branch: `feature/homepage-milestone-1-hero-mood`, commit `3bf5eb8`.**
* **Setup note:** this branch's committed `okanagan.db` fixture currently crashes the server on every request (`no such column: great_groups`). Confirmed via `git diff main...feature/homepage-milestone-1-hero-mood -- okanagan.db db.js` (empty) that this is a pre-existing issue on `main` itself, unrelated to Milestone 1. QA was performed against a freshly rebuilt local dev database (same approach `tests/server.test.js` already uses) with representative fixture data; the tracked `okanagan.db` was restored to its committed state afterward and nothing was committed from that setup.

**Viewport results:**

| Viewport | Result |
|---|---|
| 1440×900 | PASS |
| 1280×800 | PASS |
| 390×844 | PASS |
| 375×812 | PASS |

**Detailed checks:**

* **Hero visual result — PASS.** Full-bleed cinematic placeholder image, white headline/copy legible over the image (a real contrast bug from earlier implementation work — app.css's global `h1,h2,h3,.display{color:var(--ink)}` rule beating inherited white — was already fixed prior to this QA pass), compact search box clearly secondary to the headline, no clipping or overflow at any of the four widths.
* **"What are you in the mood for?" visual result — PASS.** All six cards render with the intended two-tier hierarchy: Eat/Drink/Hidden Gems are larger, portrait (3:4), stronger type; Golf/What's On/Explore are smaller, widescreen (16:9), quieter type. Desktop: 3+3 grid. Mobile: primary cards stack single-column with a shortened aspect ratio (not "giant"); secondary row becomes a horizontal-scroll strip rather than three more full-width stacked cards.
* **All six mood-card functional-link results — PASS.**
  * Eat → presses the existing `restaurant`+`cafe` type-chips and reveals filtered results (verified: 3 matching fixture venues).
  * Drink → presses `winery`+`brewery`+`cocktail`+`pub` type-chips (verified: 5 matching fixture venues).
  * Hidden Gems → anchors to the real, existing `#hiddenGems` section.
  * Golf → links to the real, existing golf category page (`/kelowna/golf`, verified HTTP 200).
  * What's On → anchors to the real, existing `#happeningSoon` section.
  * Explore → anchors to the real, existing `#exploreRegions` section.
* **Hero search result — PASS.** Verified on a fresh page load: typing "gelato" and submitting correctly delegates to the existing `#searchInput`/`#searchBtn` search implementation and filters to the matching venue (1 result).
* **Horizontal-overflow result — PASS, none at any of the four widths** (`document.documentElement.scrollWidth === clientWidth` confirmed at 1440, 1280, 390, 375).
* **Unexpected page-load auto-scroll result — PASS, none** (`window.scrollY === 0` on load confirmed at all four widths).
* **Console errors — PASS, no new errors.** Exactly one console error is present at every width, and it is a **pre-existing** error (`initBlock12`, the "Live Google Places search" toggle — `TypeError: Cannot read properties of null (reading 'addEventListener')`), confirmed unrelated to Milestone 1 since its target markup (`#liveSearchToggle`) doesn't exist anywhere on `main` either. No additional/new console errors were introduced.
* **Duplicate Hero/Mood section check — PASS.** Exactly one `.hero-scenic` element, exactly one `#moodCards` section, and exactly six `.mood-card` elements confirmed on the rendered page.

**Final PASS/FAIL summary:** all checks above are **PASS**. No functional failures found.

**Readiness assessment: Claude considers Milestone 1 ready for review.** The one console error present is confirmed pre-existing and out of scope. The one blocking issue found (the committed `okanagan.db` fixture crashing the server) is also confirmed pre-existing on `main` and unrelated to this milestone's code — worth flagging separately since it currently prevents anyone else from running this branch (or `main`) locally without first rebuilding the local database.

### Claude — Milestone 2 (Hidden Gems editorial + Explore the Okanagan) implementation + QA (2026-09-16)

* **Status: implemented, tested, QA'd, ready for review. Branch: `feature/homepage-milestone-1-hero-mood` (redesign branch continues on this branch), built directly on top of Milestone 1 (commit `eb248e6`).**
* **Scope:** items 3 and 4 of the approved homepage architecture only — Hidden Gems (editorial/curated presentation) and Explore the Okanagan (major photo-led visual destination section). Hero and Mood cards (Milestone 1) were not touched. Full page reordering (moving these two sections ahead of the wizard, per the final target architecture) was explicitly out of scope for this milestone and is deferred.
* **TDD:** new/updated tests were written first in `tests/server.test.js` and confirmed to fail for the expected reason (missing implementation) before any production code changed: rating-glyph removal on Hidden Gems cards, presence of a placeholder image + working link for each of the 8 curated Explore regions, and a live HTTP check that a new region placeholder image is actually servable. All pre-existing tests were left passing throughout.

**1. Hidden Gems (editorial redesign):**
* `renderHiddenGemsHomepageHTML()`'s query and `HIDDEN_GEM_HOMEPAGE_BLURBS` (the existing, real, curated editorial-blurb dataset) are completely unchanged — no fabricated venue data, no new data source.
* `hiddenGemHomepageCardHtml()`: the star rating is no longer shown at all (de-emphasizing directory-style metadata per the approved spec); the region label remains as a small, muted kicker line.
* Layout moved from a horizontal-scroll strip of small cards to a static, larger 3-column (desktop) / 1-column (mobile) grid, with larger typography on the editorial blurb, so the section reads as a curated feature rather than "more to browse sideways."
* Venue links are unchanged and still canonical (`/{region}/{category}/{slug}`), verified working (200) against real fixture venue pages.
* **Bug found and fixed (pre-existing, not introduced by this milestone, but directly blocking this section's visual QA):** the homepage's own injected stylesheet never defined the `.compact-band`'s base height/layout rule — that rule only existed in the separate `SEO_PAGE_CSS` used by venue/category/region pages, which the homepage doesn't load. This collapsed the color band to the label's own line-height and made the "Hidden Gem" badge overlap the venue name. Fixed with a narrow rule scoped only to `.hidden-gem-card .compact-band`/`.compact-band-label` — `SEO_PAGE_CSS` and every page that uses it are untouched.

**2. Explore the Okanagan (visual destination redesign):**
* `renderExploreRegionsHTML()`'s data (the curated 8-region list, `REGION_LABELS`, `REGION_TAGLINES`) is completely unchanged.
* Markup redesigned into large photo-led destination cards (new `.region-card` family, deliberately not reusing `.region-tile` so Browse by Category's shared tile CSS is completely unaffected): full-bleed placeholder image, bottom gradient-scrim overlay, region name + tagline — visually consistent with Milestone 1's mood-card language.
* Desktop: 4-column grid (≥1100px), 2-column (≥640px), 1-column (mobile). Links unchanged and verified working (200) against real region pages for all 8 destinations.
* New placeholder images at `public/images/regions/<region>.png` (8 files, 640×800, distinct gradient per region) — explicitly temporary, documented in the now-updated `public/images/PLACEHOLDER_IMAGES.md`, not final photography, not depicting any specific real business.

**Test results:** 63/63 passing (58 pre-existing + 5 new/updated for this milestone), including the new live check that `/images/regions/kelowna.png` is actually servable (200, `image/png`).

**Rendered QA at 1440×900, 1280×800, 390×844, 375×812 — all PASS:**
* Hidden Gems visual result: PASS at all four widths (editorial card design, no clipping, no badge/text overlap after the fix above).
* Explore visual result: PASS at all four widths (large photo-card grid, correct responsive column counts, no clipping).
* Functional links: PASS — both Hidden Gems venue links (`/naramata/cafes/chabendo-gelato`, `/kelowna/breweries/buffalo-rouge-brewing-co`) and all 8 Explore region links resolve to real, working pages (200).
* Horizontal overflow: PASS, none at any of the four widths (`scrollWidth === clientWidth`).
* Unexpected page-load auto-scroll: PASS, none (`scrollY === 0` on load at all four widths).
* Console errors: PASS, no new errors. Exactly one error present at every width, confirmed identical to the pre-existing `initBlock12` ("Live Google Places search") error already documented under Milestone 1's QA — not a regression.
* Duplicate-section check: PASS — exactly one `.hero-scenic`, one `#moodCards`, one `#hiddenGems`, and one `#exploreRegions` on the page.
* Section order: PASS — `#hiddenGems` correctly precedes `#exploreRegions` in the rendered page, matching the approved architecture's item 3-then-4 order. (Both sections' position relative to the wizard/search is unchanged from Milestone 1 and is not part of this milestone's scope.)
* Existing deeper homepage functionality: PASS — wizard/search (`#searchInput`), trip tray (`#tripTray`), map (`#okMap`), sitemap (`/sitemap.xml`), and robots.txt all confirmed present/working, unmodified.

**Known limitations:**
* All Explore the Okanagan and Hidden-Gems-adjacent imagery referenced by this milestone (the 8 new `public/images/regions/*.png` files) are temporary generated placeholders, not final AI-generated or real photography — see `public/images/PLACEHOLDER_IMAGES.md` for exact replacement paths/dimensions.
* The pre-existing `initBlock12` console error and the pre-existing `okanagan.db`/`great_groups` local-fixture issue (both documented under Milestone 1) remain unresolved, as neither directly blocks this milestone beyond what was already noted.
* Full architectural reordering (Hidden Gems/Explore the Okanagan moving ahead of the wizard, Build Your Trip CTA, Weather de-emphasis, Spotlight/Featured retirement) remains for a future milestone, per `docs/HOMEPAGE_IMPLEMENTATION_PLAN.md`.

**Readiness assessment: Claude considers Milestone 2 ready for review.**

### Claude — Milestone 3 (Build Your Perfect Okanagan Trip + Browse/Search repositioning) implementation + QA (2026-09-16)

* **Status: implemented, tested, QA'd. Remains UNCOMMITTED on `feature/homepage-milestone-1-hero-mood` — working-tree changes only, not yet staged or committed, pending explicit authorization.**
* **Scope:** items 5 and 6 of the approved homepage architecture — "Build Your Perfect Okanagan Trip" (a new large immersive CTA) and Browse/Search (repositioning/reframing the existing wizard), plus the associated homepage hierarchy change this requires. Hero, Mood, Hidden Gems, and Explore the Okanagan (Milestones 1-2) were not touched.
* **Final section order (now matches the approved architecture in full):** Hero → Mood → Hidden Gems → Explore the Okanagan → Build Your Perfect Okanagan Trip → Browse/Search (`#directory`) → existing deeper directory content (Happening Soon, Browse by Category, Weather, Spotlight, Featured, Results, etc., unchanged and unmoved relative to each other).

**1. Build Your Perfect Okanagan Trip:**
* New `renderBuildTripCTAHTML()` — a large immersive section with a placeholder background image, heading, supporting copy, and two actions: "Start building your trip" and "View the interactive map."
* Reuses the existing trip-tray (`#tripTrayToggle`/`#tripTrayPanel`, `localStorage`-backed) and Leaflet map (`#mapToggleBtn`/`#mapPanel`) exactly as they already work — no new trip data model, no new panel, no change to the existing multi-stop Google Maps route logic. The two CTA buttons are a few lines of orchestration only: they open the real, existing panels rather than reimplementing anything.

**2. Browse/Search repositioning:**
* The existing wizard (`#directory`) itself — its markup, IDs, filter/search/results logic — is completely unchanged.
* A new "Browse & Search the Okanagan" heading (matching the visual language of every other section) was added directly above the existing search box, purely additive, to reframe the wizard as a deliberate deeper-discovery section rather than the page's dominant opening.
* The wizard physically moved later in the page (after Build Your Trip) via the same anchor-splice technique already used for every prior reorder — no rewrite of its internal behavior.

**Bug found and fixed during QA (real interaction bug, not pre-existing):** the "Start building your trip" button correctly opened the trip tray on click, but the button's own click event then continued bubbling up to a pre-existing, unrelated document-level "click outside closes the tray" listener, which immediately closed what was just opened. Fixed with a single `e.stopPropagation()` call in the Milestone 3 CTA click handler in `public/scripts/app.js` — no change to the trip tray's own logic, data model, or `localStorage` behavior. Full regression suite re-run after the fix: **65/65 passing.**

**Rendered QA at 1440×900, 1280×800, 390×844, 375×812 — all PASS:**
* Build Your Perfect Okanagan Trip: visual result and functional behavior (both buttons, verified against a freshly-cached browser profile after first misdiagnosing a stale-cache false negative) PASS at all four widths.
* Browse/Search: visual result (new heading) and functional behavior (existing search still returns correct filtered results) PASS.
* Trip planner and map: both confirmed to open the real, existing panels correctly; no new state introduced.
* Horizontal overflow: PASS, none at any width.
* Unexpected page-load auto-scroll: PASS, none.
* Duplicate IDs/content: PASS — exactly one each of `.hero-scenic`, `#moodCards`, `#hiddenGems`, `#exploreRegions`, `#buildTrip`, `#directory`; zero duplicate element IDs page-wide.
* Console errors: PASS, no new errors. Exactly one error present at every width, confirmed identical to the pre-existing `initBlock12` ("Live Google Places search") error already documented under Milestones 1-2 — unrelated, not changed by this milestone.
* Links/assets: PASS — golf category page, a Hidden Gems venue page, a region page, the new region image, the new `trip-cta.png` image, `sitemap.xml`, and `robots.txt` all verified 200.

**Known limitations:**
* `public/images/trip-cta.png` is a temporary generated placeholder, not final photography — documented in `public/images/PLACEHOLDER_IMAGES.md`.
* A pre-existing, unrelated cosmetic issue was observed (not fixed, not introduced by this milestone): the floating "Open Now" pill (a fixed-position element from earlier work) visually overlaps section eyebrow text at certain scroll positions on mobile — same category of pre-existing floating-badge behavior noted in earlier milestones.
* The pre-existing `initBlock12` console error and the pre-existing `okanagan.db`/`great_groups` local-fixture issue (documented under Milestone 1) remain unresolved and unrelated.

**No schema changes, no API changes, no changes to Hero/Mood/Hidden Gems/Explore the Okanagan. No deploy, no merge, no change to main, no Railway configuration change, no database change.**

**Readiness assessment: Claude considers Milestone 3 ready for review. It remains uncommitted on `feature/homepage-milestone-1-hero-mood`, pending explicit authorization to commit and push.**

## Change Log

* 2026-09-08 — Initial shared AI handoff file created to establish coordination between Claude and ChatGPT.
* 2026-09-08 — Claude confirmed the shared AI handoff workflow is ready.
* 2026-09-08 — Claude reviewed current repository/production state and identified continued location enrichment (152 active venues missing address/lat/lng) as the highest-priority next task; added details and a suggested approach under Open Tasks.
* 2026-09-08 — Claude executed the ready 11-venue enrichment batch (IDs 469, 841, 857, 863, 907, 918, 967, 1000, 1002, 1004, 1041). All 11 succeeded with zero anomalies. Complete-location count: 900 → 911. Missing-location count: 152 → 141. Active/redirect counts and all 17 redirects confirmed unchanged. Manifest intentionally left unchanged (separate task).
* 2026-09-16 — Claude completed final rendered QA for homepage redesign Milestone 1 (scenic hero + "What are you in the mood for?", branch `feature/homepage-milestone-1-hero-mood`, commit `3bf5eb8`). All checks PASS at 1440×900/1280×800/390×844/375×812: hero and mood-card visuals, all six mood-card links, hero search, no horizontal overflow, no unexpected auto-scroll, no duplicate sections, and no new console errors (one pre-existing, unrelated `initBlock12` error confirmed present on `main` too). Milestone 1 considered ready for review. Separately flagged: the branch's (and `main`'s) committed `okanagan.db` fixture currently crashes the server (`no such column: great_groups`), unrelated to this milestone.
* 2026-09-16 — Claude implemented and QA'd homepage redesign Milestone 2 (Hidden Gems editorial redesign + Explore the Okanagan visual destinations) on `feature/homepage-milestone-1-hero-mood`, built on top of Milestone 1. TDD: new tests written and confirmed failing before implementation. 63/63 tests passing. Hidden Gems: rating de-emphasized, editorial blurb given visual priority, moved to a static larger grid; same real query/data and approved blurbs reused unchanged. Explore the Okanagan: promoted to a major photo-led destination-card grid (new `.region-card` family, isolated from Browse by Category's shared tiles), same curated region data/taglines reused unchanged; 8 new placeholder images added. Found and fixed one pre-existing, milestone-blocking bug (missing `.compact-band` height rule on the homepage stylesheet, scoped fix only). All rendered QA PASS at the four standard viewports: visuals, all functional links, no horizontal overflow, no auto-scroll, no new console errors (same pre-existing `initBlock12` error only), no duplicate sections, correct Hidden-Gems-before-Explore order. Milestone 2 considered ready for review.
* 2026-09-16 — Claude implemented and QA'd homepage redesign Milestone 3 (Build Your Perfect Okanagan Trip CTA + Browse/Search repositioning) on `feature/homepage-milestone-1-hero-mood`, built on top of Milestone 2 — remains uncommitted, pending authorization. Final section order now matches the full approved architecture: Hero → Mood → Hidden Gems → Explore the Okanagan → Build Your Perfect Okanagan Trip → Browse/Search (`#directory`) → existing deeper directory content. The new CTA reuses the existing trip tray, map, `localStorage`, and Google Maps route entirely — no new trip data model. The wizard's own search/filter/results behavior is unchanged; only a new reframing heading was added above it. Found and fixed one real interaction bug during QA: the CTA's "Start building your trip" click bubbled into the existing document-level outside-click handler and immediately closed the trip tray it had just opened; fixed with a single `e.stopPropagation()` in the Milestone 3 handler in `public/scripts/app.js`. Full regression suite re-run after the fix: 65/65 passing. Rendered QA PASS at 1440×900/1280×800/390×844/375×812: no horizontal overflow, no duplicate IDs/content, all representative links/assets/sitemap/robots checks passed, no new console errors (same pre-existing `initBlock12` error only). One pre-existing, unrelated cosmetic issue observed and left unchanged: the floating "Open Now" pill overlaps section eyebrow text at some mobile scroll positions. No schema/API/database/Railway/main changes. Milestone 3 considered ready for review, still uncommitted.
* 2026-09-18 — Claude executed a 5-venue location-enrichment batch via the guarded `/admin/enrich-venue` endpoint (IDs 95, 154, 535, 536, 724), each individually phone- or place-verified immediately before writing. All 5 succeeded with zero collateral field changes. Missing-location count: 43 → 38. Then completed a full research/classification pass on the remaining 38: 19 confirmed MOBILE/ROAMING (excluded), 19 UNRESOLVED (documented per-ID reasoning), 0 currently WRITE-READY. Two initial WRITE-READY findings (IDs 158, 337) were caught and corrected by a targeted identity audit before any write was attempted — see Open Tasks for the corrected classification and reasoning. Active/phone/sitemap counts confirmed unchanged throughout except for the 5 intended writes. Manifest/Open-Tasks section updated to reflect current state and prevent repeat research; no other files or venue data changed.
* 2026-09-19 — Claude consolidated the site's footer implementations: the approved homepage footer is now the canonical footer on `/`, `/browse`, `/trip`, venue pages, region pages, category pages, guide pages, event pages, and the events index. Extracted the shared CSS into a new `renderCanonicalFooterStyles()` function (single source of truth going forward) and replaced the old minimal `siteFooter()` one-liner on all SEO page types with the same `renderHomeFooterHTML(true)` markup the homepage uses. Fixed a full-bleed rendering bug found during visual testing (SEO pages' `body{max-width:900px}` was boxing the footer into a centered rectangle). `renderGuideFooterHTML()` (the separate SEO cross-link block) was intentionally left as-is, not merged into the visual footer. Homepage's own rendered output confirmed byte-identical before/after. Tests: 214/215 passing (1 pre-existing, unrelated `EADDRINUSE` failure). Committed as `73a4906a2fcd772ee2d17ff00b532bdb89767970`, pushed to `main`, and confirmed auto-deployed and live on production (Railway deployment `a24c977e-0ce9-4409-9785-275bc063def7`, SUCCESS) across all tested page types. Mobile verification was code-level only (unchanged, already-approved `@media` rules) rather than a fresh live screenshot, due to a browser-automation environment limitation — flagged as worth a follow-up live mobile check.
* 2026-09-19 — Claude researched, rewrote and (after explicit human approval of all 49 texts) wrote new experience-led `description` copy to production for every active Golf venue: 49/49 (39 outdoor courses, 10 indoor/simulator venues), via the existing authenticated `PUT /api/venues/:id`, description field only. Live reconciliation: 49 descriptions changed and byte-equal to the approved manifest, 0 other fields changed (auto `updated_at` only), 0 non-Golf venues changed, active count 1,091 unchanged, no duplicate slugs. `/golf` split verified at 39/10, all regional and all 49 venue pages 200, homepage content unchanged. Tests 221/222 (known pre-existing port-3001 `EADDRINUSE` only). No code, UI, asset, schema or local-DB changes; nothing deployed. Flagged for separate decision: the regex-on-description indoor/outdoor classification in `isIndoorGolfVenue()` is fragile now that several outdoor clubs operate simulators. Simplex Sportszone bay count left unstated pending official confirmation (owner statement indicates 7 studios).
* 2026-09-20 — Claude completed the owner-approved **Beaches second-pass inventory write** to production (batch `beaches-second-pass-2026-09-20`), after a read-only preflight against the live DB (fresh `railway ssh` snapshot; no slug/name collisions across all 1,193 rows including redirects; unique slugs; all regions in scope; the only "same address" hits were Gellatly Dog Beach sharing the street-level string "Gellatly Rd, West Kelowna, BC" already used by three distinct existing venues). **9 additions** (`POST /api/venues`, explicit slugs, `type='beach'`, each given a `dog_friendly` membership; canary first): 1196 Coral Beach North (lake-country, canary), 1197 Poplar Point Dog Beach (Sutherland Bay Park) (kelowna), 1198 Lake Avenue Beach Access (kelowna; the "Mushroom Beach" nickname was dropped as unsourced), 1199 Downtown Sails Dog Beach (Kerry Park) (kelowna; off-leash only during seasonal designated hours that the City does not publish — copy tells visitors to check posted hours, no hours stated), 1200 Gellatly Dog Beach (west-kelowna), 1201 Peach Orchard Dog Beach (summerland; civic address from the District dogs page, 14877 Lakeshore Dr S — the District GIS "Dog Beach" record says "LAKESHORE DR N"; discrepancy recorded in the manifest, GIS value not substituted), 1202 Skaha Lake Dog Beach (Lakeside Road) (penticton), 1203 Goodman Park Dog Swimming Area and 1204 Lions Park Dog Swimming Area (osoyoos; Town-designated dog swimming areas, described as such — never as beaches — with the Town's own location wording, no invented street address). **6 updates**: `dog_friendly` membership removed from 1139 Coral Beach Park and 1169 Peach Orchard Beach Park (badges moved to the new split venues; descriptions unchanged, cross-references kept); `hidden_gem` added to 1182 Rotary Beach Park (Oliver) on the Town's own "hidden gem" wording; description-only `PUT` on 1145 Kal Beach, 1155 Kirkland Beach and 1185 Sovereign Park appending the strictly sourced sentence "The District of Coldstream does not list [venue] among the parks where dogs are permitted." (the District page states dogs are permitted only in its listed parks; a flat "dogs are not permitted" was deliberately not used). Optional Pumphouse/Mabel Lake wording tweaks were reviewed and **not** written. **Production before → after:** venues 1,193 → 1,202 (active 1,154 → 1,163; redirects 39; new ids 1196–1204, 0 removed); beach 63 → 72; dog_friendly 18 → 25; hidden_gem 13 → 14 (beach 4 → 5); local_favorite 11 and advisory 2 unchanged; audit rows 46 → 58. Reconciliation from read-only DB snapshots: of the pre-existing 1,193 rows exactly 3 changed (the three Coldstream descriptions), 0 non-beach rows changed, 9/9 new rows byte-equal to the reviewed manifest, membership deltas exact, 0 duplicate `(region,type,slug)`, 0 duplicate memberships; all 9 new pages 200 with badge + Get Directions; `/beaches` 72 cards; sitemap 72 beach URLs; homepage byte-identical (Cloudflare hash aside). Safeguards as in Phase 3: token read from the Railway env into a chmod-600 scratch file, never printed, deleted after; unauthenticated POST/PUT → 401 and an authenticated no-op probe run first; each record re-fetched immediately before its write and field-compared after. The reviewed manifest, preflight checker, write script and before/after snapshots live in the session scratchpad (`pre2/`), not the repo. **Local inspection mirror (separate step, same day):** the same 9 additions and 6 updates were replayed into the localhost:3001 inspection database (`scratchpad/inspect3002/okanagan.db`, the copy the running dev server serves) through the same guarded local API paths, with update targets resolved by `region/slug` because local ids differ (local 1197–1205; sha256 `0402096d…` → `e200f4dd…`). Local after: 121 venues (72 beach + 49 golf), dog_friendly 25, hidden_gem 8 (beach 5), audit 56, no duplicates, only the 9 created + 3 updated rows touched, local homepage byte-identical. **The local inspection dataset intentionally contains only golf + beach rows, so its Hidden Gems total is 8 rather than production's 14.** Repository `okanagan.db` untouched throughout (`62fc1338…`); no code changes, no further production DB changes, no push and no deploy occurred during the write or the mirror. Not done, by decision: beach "At a glance" cards.
* 2026-09-20 — Claude completed the owner-approved Golf + Beaches venue-page polish (code only; no data, schema, homepage or footer-markup changes). Golf and Beach venue pages now use the hero as the single title treatment (the hero carries the `<h1>`; the header below opens with the category/region meta line, e.g. "Golf Course · Armstrong" / "Beach · Kelowna", so the name is no longer repeated), with a lightly polished per-type gradient hero (light sweep, inset rule; green for golf, navy for beach; no imagery — no course-specific image exists, and the Gemini mood image is generic). Golf-only "At a glance" card driven by `GOLF_AT_A_GLANCE_FACTS` in `server.js` (keyed by `region/slug`, ≤6 items, values ≤60 chars, every value restated from the approved 2026-09-19 descriptions — regex extraction was tried and rejected as unreliable); simulator venues read "Indoor Golf" in the hero, meta line and Good to Know Type (JSON-LD stays `GolfCourse`; classification via the existing `isIndoorGolfVenue()`). Two pre-existing bugs fixed in the shared golf theme: footer links were navy-on-navy on every golf/beach page (`body.golf-page a` outranked `.home-footer-col a`; fixed by restating the footer's colours at higher specificity — markup untouched), and unbreakable StoryMaps website URLs forced horizontal scrolling on nine RDCO beach pages at phone width (`overflow-wrap: anywhere` on Good to Know links). `/`, `/trip`, `/browse` byte-identical; listing pages differ only by the footer CSS lines. Tests 260/261 (known port-3001 `EADDRINUSE` only). Not done, by decision: beach "At a glance" cards (the approved beaches manifest holds structured facts if ever wanted); the beaches second-pass inventory write (9 dog-beach adds, 6 updates) was on HOLD at the time of this entry; it was subsequently completed on 2026-09-20 and is documented in the newer `beaches-second-pass-2026-09-20` entry.
* 2026-09-19 — Claude completed the Beaches content/accuracy pass (owner-approved after visual review): 63 experience-led descriptions written from the verified research manifest plus newly verified official text (RDCO park StoryMaps, Tourism Vernon, Visit Westside, Visit Penticton, City of Kelowna), validated by a claim-grounding checker (no street addresses in copy, every claim tied to a cited fact, numbers present in facts, no unsupported atmosphere words); Antlers Beach and the other 8 RDCO parks now link to their park-specific RDCO StoryMaps instead of the generic directory; 63/63 venues have Get Directions (Summerland GIS and Lake Country map coordinates added for Rotary Beach, Powell Beach and Pioneer Park; Cosens Bay and Gyro Beach resolve to the access road / park name only); new `dog_friendly` collection kind (bootstrapped in `db.js`, rendered with the existing `.chip` badge on cards and venue pages, restriction in the note, deliberately not the `venues.dog_friendly` boolean so the frozen homepage's guide counts are untouched) — 18 beaches qualify; Hidden Gems: Sarsons, Cedar Creek, Raymer Bay, Powell Beach; Local Favourites: Boyce-Gyro, Paddlewheel (explicit official/DMO/local-guide wording only; Manitou's cited "hidden gem" source could not be located and was not assigned); advisory notice redesigned as a restrained "Check before you go" note with an official-source link. Tests 255/256 (known HTTP-routes fixture only). **All of this data lives only in the local inspect DB: production has no beach rows yet; the production write (63 venues via `POST /api/venues`, 24 memberships, 2 advisories) is the next, separately approved step.**
* 2026-09-19 — Claude implemented Beaches Phase 2 (`type='beach'`, `/beaches` + regional + venue routes on the Golf design system, `advisories` collection for temporary conditions, trip-planner exclusion, homepage byte-identity protection, 14 new tests; 252/253 passing with only the known pre-existing HTTP-routes fixture failure). Owner-approved launch inventory: 63 beaches, 3 HOLD, 20 EXCLUDE (research kept). No schema change; production and local `okanagan.db` unchanged; placeholder descriptions only on the local inspect instance; final descriptions and the production write are separate approval phases.
* 2026-09-21 — Claude wrote the owner-approved Nature & Wildlife (11 venues 1337–1347 + 3 cross-lists), Viewpoints & Lookouts (5 venues 1348–1352 + 1 cross-list) and Camping (31 venues 1353–1383 + 6 cross-lists) data to production via the guarded API; no code or deployment. Nature 30 → 44, Viewpoints 33 → 39, Camping 2 → 39.
* 2026-09-22 — Claude deployed `e508a16` (activity pages list outdoor + beach members via `OUTDOOR_ACTIVITY_VENUE_TYPES`; golf excluded), `d556721` (Outdoors explorer landing: Choose Region(s) + Choose Activity(s) → Results, activity cards as filter toggles, landing universe = activity universe) and `698a350` (Nature/Viewpoints/Camping card art). Each verified live; homepage, Golf, Beaches, Trip and sitemap URL set unchanged.
* 2026-09-22 — Claude wrote the Winter-resort expansion (17 venues 1384–1400; enrichments 1240/1233/1234/1235/483; Winter 34 → 51) and the Nature/Viewpoints/Camping enrichment (6 venues 1401–1406; 16 memberships; 8 append-only enrichments; Nature 51, Viewpoints 43, Camping 50) to production via the guarded API; no code or deployment. Open flags: 1335 region, 1394/1395 adventure cross-lists, 1389 Ravenwood 26/27 details, held Coldstream Park / Adams Bird Sanctuary.
* 2026-09-22 — Claude shipped What's On v1: `dd6a044` (schema, local-date helpers, data layer + duplicate gate, bearer-guarded API, detail page + sitemap, `/whats-on` date step; deployed with 0 events) and `6568501` (`engines.node` → `24.x` so production runs tzdata 2026c — America/Vancouver permanent −07:00; Node 24.21.0 verified live). Tests 347 (346 pass, one pre-existing failure).
* 2026-09-22 — Claude imported What's On data to production in four owner-approved batches (all via `railway run`, rehearsed offline first, reconciled through public reads): canary 10/79 (ids 1–10), v1 82/193 (11–92), v2 534/760 (93–626), v3 12/75 (627–638) — 638 events / 1,107 occurrences, sitemap 1,606 → 2,244. Two approved occurrence edits followed: #387 Oct 11 sitting → 11:30–13:30 (occurrence 1033; 646 cancelled) and #548 + Dec 5–6 (occurrences 1109–1110) → 1,109 scheduled occurrences. No existing venue or homepage data changed.
* 2026-09-22 — Claude shipped the What's On category tile art: `1ba5e38` (12 × 1376×768 WebP under `public/images/whats-on/` + the shell test's image assertions), Railway `cb24656f`; verified live. No application code, homepage or data changes.
* 2026-09-22 — Claude audited SEO read-only (technical + Search Console via the owner's own browser session) and shipped SEO Phase 1: `4776d5e` adds `/whats-on`, `/outdoors`, `/golf` and `/beaches` to sitemap.xml (2,244 → 2,248 URLs; `/trip` and the retired `/events` excluded), Railway `daab6e06`. No homepage, data or content change.
* 2026-09-22 — Claude shipped SEO H1 Step 1: `fc6f57c` adds a "More events in {Region}" block to event detail pages (server.js only), Railway `91135192`. 638 of 639 event pages carry it, max 4 links, ~2,500 new internal links, no self/cross-region/expired links; canonical, robots, JSON-LD, homepage, hubs and sitemap all unchanged. Production stays at 639 events / 1,133 scheduled occurrences / 2,248 sitemap URLs.
* 2026-09-22 — Claude shipped SEO H1 Step 2: `50eaada` adds an "Upcoming events in {Region}" block to region hub pages (`server.js` + one test), Railway `da8263d2`. 15 of 20 regions carry it (14 × 3 links, coldstream 2), 59 new internal links, all canonical, none to expired/cancelled/postponed events; the 5 regions below the two-event threshold render byte-identical pages. Homepage, hubs, event pages and sitemap unchanged; production stays at 639 events / 1,133 scheduled occurrences / 2,248 sitemap URLs. Tests 347/348 (known golf-selector failure). Mid-span date display left for a separate change.
* 2026-09-22 — Claude enriched 44 events with real `venue_id` values (batch `whatson-2026-09-venue-link-1`, data only, no code or deployment): 13 owner-approved exact venue-name matches, written as `{venue_id, venue_name_text: null}` because the model enforces XOR. 44/44 returned 200; 23 -> 67 venue-linked events, 616 -> 572 text-only; a 639-row fingerprint was identical before and after; 5 HOLD/OROLO events deliberately untouched.
* 2026-09-22 — Claude shipped SEO H1 Step 3: `e5cb16f` adds an "Events at {Venue}" block to venue detail pages (`server.js` + one test), Railway `ce5c12ff`. 7 venue pages carry it (max 3 events each, `venue_id`-only), 18 new internal links, all 200 and self-canonical, none to expired/cancelled/postponed events; the 11 single-event venues render byte-identical pages. Homepage, hubs, region/category/event pages and sitemap unchanged; production steady at 639 events / 1,133 scheduled occurrences / 2,248 sitemap URLs. Tests 348/349 (known golf-selector failure). **H1 complete across all three steps.**
* 2026-09-22 — Claude fixed the H1 mid-span date display: `e49e7fe` adds the shared `upcomingEventDateLabel()` used by all three H1 blocks, so an in-progress multi-day event reads "Until {end date}" instead of a start date that has passed (#93 Skillful, #632 Joining Hands, #638 Penticton Beer Week). Railway `a1d6c109`. Selection and ordering unchanged, 636 other events keep their previous label; no data, sitemap, schema or homepage change. Tests 349/350 (known golf-selector failure).
* 2026-09-22 — Claude added the Pinterest domain verification tag: `9c5c775` adds `<meta name="p:domain_verify" …>` to `okanagan.html`'s head beside the Google and Bing tags, Railway `39495743`. It had to go in the static homepage file rather than `pageHead()` — the homepage is not server-rendered, so a `pageHead()` change would have touched 2,248 pages and still not verified. One line, homepage (and `/browse`) only; every other page byte-identical, sitemap and data unchanged. Tests 349/350 (known golf-selector failure).
* 2026-09-23 — Claude added 3 District Wine Village venues to Oliver (data only, no code or deployment): #1407 Apricus Cellars, #1408 Foolish Wine, #1409 Neighbourhood Outpost, created via `POST /api/venues` with all 12 amenity booleans at 0 so the frozen homepage stayed byte-identical. Active venues 1,365 → 1,368, sitemap 2,248 → 2,251, Oliver wineries 46 → 48, breweries 2 → 3; events unchanged at 639 / 1,133; no badges applied. SPACEDOG Wines (not yet open) and Workshop Spirits / Yellow Dog Spirits remain HOLD.
* 2026-09-23 — Claude imported Field of Screams XIII (#640, `vernon`, data only, batch `whatson-2026-09-fall-seasonal-1`): one date-range occurrence 2026-09-25 → 2026-11-01 at 18:30, held at Historic O'Keefe Ranch. Organizer-verified after third-party calendars disagreed by a day; no nightly occurrences inferred. Events 639 → 640, occurrences 1,133 → 1,134, sitemap 2,251 → 2,252; no venue, badge or code change, homepage byte-identical.
* 2026-09-23 — Claude created the `weekly-pinterest` skill (`.claude/skills/weekly-pinterest/SKILL.md`, PREVIEW by default) and ran it to draft, generate, QC and record Pinterest batch **#39–52** — 14 Pins across all 10 boards, every destination URL verified 200, all images 848 x 1264 verified from the downloaded file and hash-checked into `pinterest-assets/` (untracked). #46, #47 and #49 were regenerated once for overlay typography; final v2 prompts and hashes recorded, discarded hashes named. Full per-Pin records added to `docs/PINTEREST_CONTENT_PLAN.md`, all recorded as generated and awaiting approval at that point (all 14 were published later the same day — see the entry below). No code, database, homepage or deployment changes; Pinterest never opened; Pin #38 remains the only published Pin.
* 2026-09-23 — Claude published Pinterest batch **#39–52 — all 14 Pins live and verified** (14/14 found on the account, 0 missing, titles matching the plan). Beforehand, a read-only audit of the 37 historical Pins found two true duplicates, replaced in `b5ae623`: #45 → **Kelowna Golf** (`/kelowna/golf`, 20 verified courses) and #47 → **Okanagan Live Music** (`/whats-on?categories=live-music`, 140 events); #49 was cleared. Live URLs recorded in `docs/PINTEREST_CONTENT_PLAN.md` (`c7a0091`). AI-disclosure left OFF and untouched, no board created or modified, #38 not republished, `pinterest-assets/` still untracked; no code, database or deployment change.
* 2026-09-23 — Claude completed the Adventure category work (data only, no code or deployment): added **#1410 Okanagan Parasail** with an `activity_adventure` membership, taking the Adventure collection from **18 to 19** members, and verified the venue live in production; wrote verified phone numbers for the 8 owner-approved Adventure venues **#1242, #1300, #1302, #1303, #1304, #1305, #1306, #1308** (`phone` was the only field sent, each number confirmed against the operator's own published contact details). The Adventure records with missing coordinates, missing phones, the incomplete address on **#1307** and the open resort/park taxonomy questions were **deliberately left unchanged**, and **no speculative coordinates were added**. Per the owner's instruction to finish the category rather than chase full coverage, Adventure was treated as complete and work moved on. Repository `okanagan.db` unchanged.
* 2026-09-23 — Claude shipped the dedicated valley-wide Wine hub: **`ed91d4a`** "feat: add dedicated valley-wide /wineries hub" (Railway **`3dbe1623`**, SUCCESS). The homepage Wine mood card and footer Wine link now point at **`/wineries`**, replacing the earlier `/browse?types=winery` routing (itself a fix for the original Oliver-only `/oliver/wineries` bug). `/wineries` lists **all 204 Okanagan wineries**, is **winery-only** (no restaurants, cafés, breweries, pubs, cocktail lounges or other types), uses the **Beaches-style regional selector** whose options link to the 14 existing `/{region}/wineries` pages so region selection filters the results, and was **added to the sitemap**. Theming is **hub-only**: `winery` was not added to `THEMED_CATEGORY_TYPES`; a new `HUB_ONLY_THEMED_TYPES` set is read only by `renderCategoryAllRegionsPage()`, which passes an explicit `themed` override into the shared helpers, so **existing winery region pages and individual winery pages keep their original styling**. The owner-approved back-links were added — regional winery pages get **`← All Wineries`**, individual winery pages get **`← {Region} Wineries`**. Live verification: `/wineries` **HTTP 200**, 204 winery cards and 0 of any other type, all 14 region pages 200 with card counts summing to exactly 204, `/wineries` in the sitemap, and the live homepage differing **only in the two Wine `href` values**. `okanagan.db` unchanged. Tests **350 pass / 1 fail of 351** — the Wine tests pass, and the one failure is the known pre-existing golf-selector failure in test **#281**, unrelated to Wine (it fails identically as `#280` at HEAD).
* 2026-09-23 — Claude added the winery engagement controls: **`49c4211`** "feat: add winery engagement controls" (Railway **`b413eaef`**, SUCCESS). A read-only audit of `/wineries` found that Favorite and Add to Trip were present on the hub's 204 cards but **absent from all 14 winery region pages and all 204 winery venue pages**, because `venueCardHtml` and `renderVenuePage` gated the controls on the same flag as the visual theme and `winery` is hub-only themed — an unforeseen consequence of `ed91d4a`. Fixed by separating the two concerns: a new `ENGAGEMENT_ONLY_TYPES` set plus `usesEngagementControls()`, an `actions` option on `venueCardHtml` **defaulting to `themed`** so every existing caller is byte-identical, and a `renderEngagementControlStyles()` injected only on those pages (nothing added to the shared `SEO_PAGE_CSS`). No `app.js`, `app.css` or trip tray was needed — `golfFavTripScriptBody()`'s standalone branch already drives the same `okanaganFavorites` / `okanaganTrip` keys the homepage Trip Planner reads. Live after deploy: 204/204 region cards and 14/14 sampled venue pages carry both controls, unthemed; the homepage, the `/wineries` hub, a golf region page and a restaurant region page are byte-identical pre vs post deploy; winery pages changed by addition only. Tests **351 pass / 1 fail of 352**, the failure being the pre-existing golf-selector assertion. `okanagan.db` unchanged and not in the commit — note it was accidentally mutated mid-session by running `node -e "require('./server.js')"` from the repo root and was restored before committing; use `node --check` instead.
* 2026-09-23 — Claude completed the Oliver + Naramata winery work (data only, no code, commit or deployment): **5 approved amenity changes** (#172 Deep Roots `kid_friendly`+`lake_view`, #843 Lang `lake_view`, #852 Mocojo `lake_view`, #845 Laughing Stock `patio`; #845 and #843 `dog_friendly` deliberately held as unresolved) and **46 official winery websites** (44 written, 2 skipped because #1407 Apricus and #1408 Foolish Wine already held correct URLs). Oliver+Naramata website coverage **2 → 46 of 81**, so the "Visit Website" button now renders on those venue pages. Websites were established by generating 726 candidate domains and accepting one only when the site's own `<title>` named the winery, showed wine evidence and was not parked — which correctly rejected `aasha.com` (hiring software), `chainreaction.com` (bicycles), `deeprootsmusic.ca`, `elephantisland.com` (horse racing), `lang.com` (calendars), `opera.com` and `canter.com` (real estate). Reconciliation across all 1,369 venues: **49 field changes, 44 website + 5 amenity, 0 outside the approved sets**, no non-winery or out-of-region records touched. Tests 351 pass / 1 fail of 352 (pre-existing golf-selector assertion). Repository `okanagan.db` unchanged. Recorded alongside: Picnic/Food/Tasting Room/Tours have **no fields** in the schema; Hidden Gem and Local Favourite are collections with **no documented rule** (1 and 0 members in scope, 0 Local Favourites site-wide), so none were assigned; 4 ambiguous domains, 27 unverified websites and 73 of 81 unresolved amenity records remain open.
* 2026-09-23 — Claude completed winery fast-pass batch 3 (Kelowna, Okanagan Falls, Summerland — 58 wineries audited; data only, written to production via the live API, no code or deployment change): **40 field changes across 39 venues** — 39 HIGH-confidence official winery websites plus **#858 Observatory `lake_view` 0→1**. No MEDIUM or unverified website matches and no other amenity changes were written. Verification: venue count **1,369 → 1,369**, exactly 40 changes with **0 outside the approved set**, no non-winery or out-of-region records touched. **Winery website coverage 46 → 85 of 204.** Tests 351 pass / 1 pre-existing golf-selector failure; `node --check server.js` and `db.js` passed; live spot-checks confirmed "Visit Website" rendering. Repository `okanagan.db` untouched. 119 wineries still lack a website, 65 of them across 9 untouched regions.
* 2026-09-23 — Claude completed winery fast-pass batch 4 (Penticton, West Kelowna, Osoyoos — 46 wineries audited; data only, written to production via the live API, no code or deployment change): **29 field changes across 28 venues** — 28 HIGH-confidence official winery websites plus **#842 Lakeside Cellars `lake_view` 0→1**. #412 Mission Hill stored as `https://www.missionhillwinery.com/` after stripping a `:443` crawl artifact. MEDIUM websites, the 8 JS-rendered empty-title domains, the 3 non-responding candidates and all 6 MEDIUM amenity signals were **excluded**. Three preflight corrections: **#866 Roche demoted** (`rochewinery.com` is a California "Roche Sonoma Winery"), #491 Pentage and #441 Nk'Mip promoted after a stripped glyph and an `/About-Us` landing respectively. Reconciliation: venue count **1,369 → 1,369**, exactly 29 changes with **0 outside the approved set**, no non-winery or out-of-region records touched, 0 non-https URLs. **Winery website coverage 85 → 113 of 204.** Tests 351 pass / 1 pre-existing golf-selector failure; `node --check server.js` and `db.js` passed; live spot-checks passed. Repository `okanagan.db` untouched. 91 wineries still lack a website, 19 of them across 6 untouched regions.
* 2026-09-24 — Claude completed winery fast-pass batch 5 — the final six untouched regions (Lake Country, Peachland, Kaleden, Vernon, Enderby, Armstrong; 19 wineries audited; data only, written to production via the live API, no code or deployment change): **10 HIGH-confidence official winery websites written, 0 amenity changes**. **This completes the untouched-region sweep — all 14 winery regions are now audited.** The 1 MEDIUM website, 5 JS-rendered empty-title domains, 2 non-responding candidates and the #7 Block One restaurant record were excluded; #7/#8 and #445/#559 are each two records for one physical winery, so only the primary record received the URL. Near-miss caught by reading the candidates directly: **#32 Arrowleaf was initially ranked on `arrowleaf.com`, a real-estate firm** — the correct site is `arrowleafcellars.com`, and "estate" has been removed from the wine-evidence keyword set that caused it (same root cause as `canter.com` earlier). Reconciliation: venue count **1,369 → 1,369**, exactly 10 changes with **0 outside the approved set**, no non-winery or out-of-region records touched, every excluded record verified still empty. **Winery website coverage 113 → 123 of 204**; Vernon and Enderby now complete. Tests 351 pass / 1 pre-existing golf-selector failure; `node --check server.js` and `db.js` passed; live spot-checks passed. Repository `okanagan.db` untouched. 81 wineries still lack a website, with ~13 JS-rendered domains the highest-value remaining fix. **[CORRECTED 2026-09-24: these domains are PARKED (114-byte `/lander` stubs), not JS-rendered, and are NOT the wineries' sites — the "a JS-capable check would convert them" recommendation in this entry is wrong. See the 2026-09-24 parked-domain entry.]**
* 2026-09-24 — Claude corrected the "JS-rendered domains" finding and wrote the 8 real winery websites it uncovered (data only, production via the live API, no code or deployment change). **The 19 domains recorded in batches 3–5 as "JS-rendered, probably the real wineries" are in fact PARKED** — 114-byte `/lander` stubs (16 parked, 2 empty, 1 unrelated) — so `quailsgatewinery.com`, `frind.ca`, `graymonkwinery.com`, `cedarcreekestate.ca` and the rest are not those wineries' sites, and the recommended JS-capable check would have converted nothing. None had been written, so no bad data reached production; the four affected sentences in the earlier entries were annotated in place. Probing the wineries' actual domains produced **8 HIGH-confidence official sites**: #125 CedarCreek `cedarcreek.bc.ca`, #716 The Vibrant Vine, #530 Quails' Gate `quailsgate.com`, #228 Frind `frindwinery.com`, #216 Fitzpatrick `fitzwine.com`, #830 Four Shadows, #250 Gray Monk and #207 Ex Nihilo — the last two verified through their own official pages in search because Cloudflare and a 403 block direct fetches. Reconciliation: venue count **1,369 → 1,369**, exactly 8 changes with **0 outside the approved set**, all 11 excluded records confirmed still empty. **Winery website coverage 123 → 131 of 204.** Tests 351 pass / 1 pre-existing golf-selector failure; `node --check` passed on both files; live spot-checks passed. Repository `okanagan.db` untouched. 73 wineries still lack a website.
* 2026-09-24 — Claude ran a targeted website-discovery pass over the 11 wineries left unresolved by the parked-domain investigation and wrote 4 official sites (data only, production via the live API, no code or deployment change): **#577 See Ya Later Ranch `sylranch.com`, #288 Indigenous World `indigenousworldwinery.com`, #90 Bordertown `bordertownwinery.com`, #844 Lariana Cellars `larianacellars.com`**. All four block or gate scripted fetches, so each was verified by its own official pages or — for Lariana — by the **BC Wine Institute directory**, which lists the domain with the winery's Osoyoos address and phone. **#370 Little Straw `littlestraw.bc.ca` and #869 Silkscarf `silkscarf-winery.com` were also resolved, by direct title match, but deliberately NOT written at the owner's instruction** and remain empty pending review. Both had been missed earlier because the candidate generator never produced **`.bc.ca` or hyphenated** domains — a gap worth fixing before any re-run. Reconciliation: venue count **1,369 → 1,369**, exactly 4 changes with **0 outside the approved set**, all 5 unresolved and both held records confirmed still empty. **Winery website coverage 131 → 135 of 204.** Tests 351 pass / 1 pre-existing golf-selector failure; `node --check` passed on both files; live spot-checks passed. Repository `okanagan.db` untouched. 69 wineries still lack a website; 5 remain unresolved, with #194 Edge of the Earth apparently Facebook-only.
* 2026-09-24 — Claude began the Food & Drink section: **`b0c9b52`** "feat: 301 old category URLs after a venue type change" (Railway **`ad5f18a4`**, SUCCESS), then applied **14 primary-category changes** to production (data only). **The "841 places" is not hard-coded** — it is exactly `restaurant+cafe+brewery+pub+cocktail`, computed client-side, and already excludes wineries. The redirect had to ship first because a venue's URL comes from `CATEGORY_SLUGS[type]`, so all 14 sitemapped URLs would otherwise have 404'd; I had wrongly called the changes "pure data, zero code" and corrected that before writing. Its key guard: the cross-type slug lookup returns a result only when exactly one active venue matches, because **`(region, slug)` is not unique** — three golf clubs keep a clubhouse-restaurant record sharing the course's region and slug. Verification of 30 suspects left **9 name traps correctly unchanged** ("Lounge" proved to be no signal — 4 of 5 lounge-named venues are ordinary restaurants), while 11 of 12 pub suspects were corroborated by their own `cuisine='pub'`. Written: 13 × `restaurant → pub` plus #163 Curious Cafe `pub → restaurant`. Reconciliation: venue count **1,369 → 1,369**, exactly 14 `type` changes with **0 outside the approved set**, **restaurant 558 → 546, pub 37 → 49**, **F&D universe still 841**, all 14 old URLs 301 in one hop to new URLs that 200, sitemap carrying 14/14 new and 0/14 old. Tests 352 pass / 1 pre-existing golf-selector failure; repo `okanagan.db` untouched. The 7 secondary/multi-category memberships and the plumbing are deliberately not started — the architecture audit concluded `collection_items` already provides everything needed, so no new system should be built.
* 2026-09-24 — **Food & Drink data classification is production-complete** (data only, production via the live API, no code or deployment change). Final batch: **2 primary changes** — #690 The Hatching Post and #989 BNA Brewing, both `restaurant → brewery` — plus **8 secondary memberships** across 7 venues. **#151, #430, #25, #576 and #982 deliberately kept `restaurant` as their primary**, reversing the original audit: those recommendations predated multi-category, and once secondaries existed the rule became *change the primary only when it is categorically wrong*. New evidence backed it — Frind's own booking platform lists #25 as "Annie's Beach **Restaurant**", and #430 serves dinner with a liquor licence. **7 venues now hold multiple effective categories:** #151 restaurant+cafe+pub; #430, #25, #576, #982 restaurant+cafe; #690 and #989 brewery+restaurant. **Final primary counts: restaurant 544, cafe 183, pub 49, cocktail 25, brewery 40; unique F&D universe exactly 841** (effective counts sum to 849, which is correct — **universe counts must use `COUNT(DISTINCT id)`**). Across both batches, 16 primary type changes were written and 5 candidate changes consciously declined. Verified: 1,369 venues unchanged, exactly 2 type changes with zero non-type field changes, no duplicate memberships, multi-category filtering returning **0 duplicate rows** with #151 appearing exactly once under all three of its categories, both brewery URL moves 301 in one hop with the sitemap carrying the new canonical URLs and neither old one. Tests 354 pass / 1 pre-existing golf-selector failure; no homepage files changed; repo `okanagan.db` untouched (md5 `347b535f842883d5d2d264d9ef585479`). Plumbing `61e6835`, redirect protection `b0c9b52`. The directory UX redesign remains open.
* 2026-09-24 — Claude closed the **brewery coverage gap** (data only, production via the live API, no code or deployment change): **9 new brewery records** (#1411–#1419 — Rustic Reel, Red Bird, Copper, Grey Fox, Jackknife, The Office, Freddy's Brewpub in Kelowna; Tin Whistle and La Petite Abeille Cider in Penticton) plus **#53 Barley Mill `pub → brewery` + `fd_pubs`** (brews on site since 1997, also a sports pub). Audited against **BC Ale Trail**; **golf was checked against GolfBC/Tourism Kelowna and has no gaps**. A duplicate check against all 1,369 records **cut the candidate list from 11 to 9** — Ellison Field Taphouse (#1077) and Barley Mill (#53) already existed, and Jackknife's address sits next door to our existing Kettle River Brewing. **#1077 was excluded because it does not brew**, and **#391 Maple Leaf Spirits was confirmed NOT a region discrepancy** (19 of 51 Naramata-region venues carry Penticton postal addresses). **Badges were researched for 5 venues but deliberately NOT written**: the homepage embeds live badge counts in its hidden SEO block, so all 12 BOOL_FIELDS were left at 0 and **the homepage verified byte-identical** (md5 `735949b913fa03f210673785ae3bac29`). Verified: venues **1,369 → 1,378**, **brewery 40 → 50**, **pub 49 → 48**, other F&D counts unchanged, only #53's type changed among existing venues with zero other field changes, all 9 pages 200, #53's old URL 301s in one hop, sitemap 2,246 → 2,255. Repo `okanagan.db` untouched. The gap search was not exhaustive — cocktail lounges, Peachland and Coldstream remain the thinnest signals.
* 2026-09-24 — Claude simplified the What's On filter surface: **`d8f5ea6`** "Simplify What's On filters and layout" (Railway **`68d05da6`**, SUCCESS; `server.js` + `tests/server.test.js` only). The page opened with ~23KB of filter markup before the first event — twelve 1376×768 category image tiles, four region accordions and four step headings — and now opens with a compact search field, one horizontal category chip row, Regions and Date popovers, and a compact result bar carrying the live count plus the removable active-filter chips and Clear all. The filter contract is unchanged (`data-region` / `data-category` + `aria-pressed`, shared `OUTDOOR_FILTER_CLIENT_PREDICATE_SRC` untouched); **region grouping is still derived from `FOOTER_REGION_GROUPS`**; **date presets remain server-side round trips and preserve existing region selections**; and **Clear all intentionally still clears only region/category/search while preserving the `?when=` date window**. Body bytes before the first event card **27,457 → 16,773**, tile images **12 → 0**, visible headings above the results **4 → 0** (the tile artwork is untouched and still served from `/images/whats-on/`). Two defects found in visual QA were fixed before committing: a duplicate native `type="search"` clear glyph, and mobile popovers opening 368px below their button because `.whatson-pop` went `position: static` — now anchored to `.whatson-controls`. `/outdoors` verified **byte-identical** to `HEAD~1` across five URL shapes and unchanged live (activity images and three-step layout intact); homepage `/` byte-identical with zero `whatson` occurrences. Live desktop and mobile verification passed, with **no horizontal page overflow** on mobile and **no site-origin console errors**. Tests **356 pass / 1 fail of 357**, the failure being the pre-existing golf-selector assertion (reproduced at `HEAD`). `okanagan.db` unchanged.
* 2026-09-24 — Claude simplified the Outdoors explorer: **`0489492`** "Simplify Outdoors filters and layout" (Railway **`deccf6ee`**, SUCCESS; `server.js` + `tests/server.test.js` only). `/outdoors` opened with ~28KB of filter markup before the first destination — nine 1376×768 activity image tiles, a region accordion, three step headings and a "Show N results" CTA — and now opens with a compact search field, a horizontal activity chip row (`All` + live activities), a compact **Activity guides** row, a single Regions popover and a compact result bar. **No date filter**: destinations are not time-sensitive, so there is no Date control, preset, `?when=` or date state anywhere. The filter contract is unchanged — activity controls keep **`data-activity`** (Outdoors' contract, not What's On's `data-category`), region chips keep `data-region` + `aria-pressed`, region grouping is still derived from `FOOTER_REGION_GROUPS`, and `OUTDOOR_FILTER_CLIENT_PREDICATE_SRC` is untouched and still shared with What's On; `#outdoorRegionStatus` and `#outdoorActivityStatus` remain as visually-hidden live regions. New client-side search (name, region label, activity labels, meta, description) adds no query parameter and ANDs with the shared predicate; both summary twins gained an optional `searchActive` flag so a search-only state reads "N of T". **No image asset was deleted** — the showcase renderer and `outdoorActivityImagePath` are retained, exported and still tested, all nine WebPs still serve from `/images/outdoors/`, and the nine activity pages keep their only inbound link through the guides row. Like-for-like local body bytes before the first card **21,155 → 13,805** (live-to-live **28,084 → 19,824**); at 386px the first card moved **1661px → 582px**; the chip row scrolls on mobile and wraps on desktop with no page-level horizontal overflow. Two defects found in visual QA were fixed before committing: the Regions panel needed a desktop `min-width`, and `.outdoors-act-row` needed an explicit `flex-wrap: nowrap` because the row also carries `.category-region-selector`. With identical data, `/`, `/whats-on`, `/golf`, `/beaches`, `/wineries`, `/trip`, `/browse`, `/sitemap.xml`, `/outdoors/<activity>` and `/<region>/outdoors` are all **byte-identical**; `/outdoors` is the only page that changed. Live desktop and mobile verification passed with no site-origin console errors. Tests **358 pass / 1 fail of 359** (3 new, 1 renamed and rewritten, 11 updated, no coverage lost), the failure being the pre-existing golf-selector assertion. `okanagan.db` unchanged. Still open by choice: the H1 stayed "Outdoors in the Okanagan" and the count kept "N outdoor destinations".
* 2026-09-24 — Claude closed the two open Outdoors items: **`3d04967`** "Update Outdoors heading and remove Activity Guides" (Railway **`72a8f7df`**, SUCCESS; `server.js` + `tests/server.test.js` only). The landing's heading is now **"Outdoor Adventures"**, applied consistently to the `<h1>`, `<title>`, `og:title`, `twitter:title` and the ItemList schema `name` (the breadcrumb "Outdoor Destinations", the canonical and the meta description are unchanged, and no other hub moved). The **Activity guides row was removed entirely** — label, all nine links, `outdoorsActivityGuidesHtml()` and its export, and exactly the 7 CSS rules dedicated to it. All activity/region filtering, search, counts, accessibility statuses, URLs and the shared predicate are untouched. **Consequence:** the landing no longer links to the nine `/outdoors/<activity>` pages; those pages still render, still inter-link through `renderOutdoorActivitySelector()` and are all still in the sitemap. With identical data `/outdoors` differed by exactly 22 lines and 13 other pages were byte-identical. Live: all five heading surfaces correct, no guides markup or links, first card y=490 desktop / y=465 at 382px with no overflow, filters + search + Clear all all behaving, homepage and What's On unchanged, no site-origin console errors. Tests **358 pass / 1 fail of 359**, the failure being the pre-existing golf-selector assertion. `okanagan.db` unchanged.
